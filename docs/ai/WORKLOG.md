# AI Worklog

AI運用上の重要イベントを時系列で記録します。

| UTC Timestamp | Task ID | Title | Codex Exit | Applied | Notes |
| --- | --- | --- | --- | --- | --- |
| 20260712-180327 | 20260712-180327-direct-usb-uart-verification | 新MacのF413 UART欠落がUgreenハブ経路原因と確認 | n/a | yes | after the user moved ST-LINK V3MINIE `003B00273234511537333934` from the Ugreen composite hub to a MacBook Pro built-in USB-C port, IORegistry confirmed ST-LINK directly under the Apple USB host controller and the serial path changed from `/dev/cu.usbmodem211202` to `/dev/cu.usbmodem1102`; with clean firmware commit `7ec9f11` at restored 921600 baud, three independent non-motor UART `n` captures were complete (`FR/FL/R/L` labels and values intact), and detailed non-motor `:` output was also complete across all headings, CSV row, mean/stddev/min/max table, distance preview, and line endings; this isolates the byte loss to the prior Ugreen hub/cable USB CDC path rather than Migration Assistant data, Python, termios/custom baud, firmware formatting, or the Mac's native toolchain; no firmware/tool change was retained, and no motor, fan, run, or NVM/identity/calibration/map/trace write command was issued |
| 20260712-175336 | 20260712-175336-new-mac-uart-drop-diagnosis | 新Mac移行後のF413 UART文字欠落を環境切り分け | n/a | yes | diagnosed repeated UART byte-range loss on the new MacBook Pro: host is native arm64 macOS 26.5.1, system Python 3.9.6 runs arm64, Homebrew and ARM GCC are native `/opt/homebrew`, AppleUSBSerial exposes `/dev/cu.usbmodem211202`, and termios is 8N1 with flow control disabled; reproduced the same loss with repo `serial_terminal.py`, native-width macOS `IOSSIOSPEED`, standard 115200 baud without custom ioctl, and macOS `/usr/bin/screen`, excluding Python migration and 921600 custom-baud handling; IORegistry shows ST-LINK V3MINIE `003B00273234511537333934` behind a Ugreen composite USB hub, making the hub/cable/direct-connection path the next required test; restored clean firmware commit `7ec9f11` at normal 921600 after temporary 115200 testing and reset to safe boot; safety incident: while exiting `screen`, a `y` intended for the screen confirmation was forwarded to firmware and may have started the short motor-trace command without lifted confirmation; Codex immediately issued ST-LINK software reset, observed trace-stop text before reset, and performed no further motor/fan/run command; no intentional NVM/identity/calibration/map/trace write was performed |
| 20260712-173421 | 20260712-173421-f413-wall-distance-concise-output | F413距離校正nを4値のみの1行表示へ変更 | n/a | yes | changed normal UART `n` to keep the existing 512-frame averaging but print only `FR: ...  FL: ...  R: ...  L: ...`; moved the previous mean/stddev/min/max, CSV template, converted-distance, and validity diagnostics to the new non-motor `:` command and documented it in help/design/HIL safety; verified commit `7ec9f11` with `Debug-stm32f413`, `Debug-stm32f405`, and `git diff --check`, then flashed the clean F413 application through ST-LINK `003B00273234511537333934`; non-motor UART `n` was exercised repeatedly on `/dev/cu.usbmodem211202` at 921600 and calculation completed, but Codex capture still exhibited the separately known fixture byte-range loss even with experimental timer pauses and one-shot UART writes, so those ineffective transmission workarounds were removed before commit; no motors, fan, run command, or NVM/identity/calibration/map/trace write was allowed |
| 20260712-171711 | 20260712-171711-f413-wall-distance-readable-output | F413距離テーブル校正用n表示を転記しやすく整理 | n/a | yes | reformatted UART `n` around the repeated LUT-calibration workflow: added blank-line/result delimiters, made the first actionable block show the exact fitter CSV order `distance_mm,fr_delta,fl_delta,r_delta,l_delta`, emitted a directly editable `DIST_MM,FR,FL,R,L` row, replaced the long diagnostic CSV with an aligned mean/stddev/min/max table, and moved converted-distance/validity data into a compact preview section; verified commit `7d7006e` with `Debug-stm32f413`, `Debug-stm32f405`, `git diff --check`, flashed application sectors through ST-LINK `003B00273234511537333934`, and ran only non-motor UART `n` on `/dev/cu.usbmodem211202` at 921600; the command completed and showed the intended section/order, while the already-recorded current-fixture UART byte-drop issue still removed parts of some labels in Codex capture; no motors, fan, run command, NVM write, identity/calibration/map/trace mutation was allowed |
| 20260712-164747 | 20260712-164747-f413-wall-distance-average | F413距離テーブル校正用UART nを512点平均化 | n/a | yes | changed the non-motor UART `n` wall-distance diagnostic to average 512 distinct completed wall-sensor frames before applying the nonlinear distance conversion; added coherent full-frame publication with a sequence counter, Welford standard deviation, min/max, a compact `sensor_avg,FR,R,FL,L,count` row, and pause/resume around diagnostic output only; runtime wall control remains on the unchanged raw-ADC path; verified `Debug-stm32f413`, `Debug-stm32f405`, Python logging tools, and `git diff --check`; F413 HIL used ST-LINK `003B00273234511537333934` and UART `/dev/cu.usbmodem211202`, flashed only application sectors, allowed no motors/fan/run/NVM writes, and confirmed repeated `n` acquisitions complete with 512 samples in about 1024 ms and per-channel standard deviation around 1-2 ADC; the current fixture UART stream drops byte ranges at both 921600 and 460800 bps even after testing HAL and direct-TXE transmit paths, so those global trace experiments were reverted and the final firmware was restored to the normal 921600 bps setting |
| 20260705-131748 | 20260705-131748-f413-wall-sensor-distance-conversion | F413壁センサのADC距離変換レイヤとLUT作成ツールを追加 | n/a | yes | implemented a first-stage F413 wall-sensor distance conversion path without changing runtime wall control: `sensor_distance` now supports legacy linear interpolation plus shape-preserving PCHIP, F413 adds `f413_wall_distance` snapshots with mm/validity masks, UART non-motor command `n` prints raw delta and converted distance, and `params/f413_preorder/sensor_distance_lut.c` is the generated profile hook for measured LUTs; added `tools/logging/fit_sensor_distance.py` to group calibration CSV samples, enforce monotonicity, compare linear/PCHIP/log/invsqrt models, and emit calibrated C tables; updated `docs/ai/F413_SENSOR_DISTANCE_DESIGN.md` and HIL safety docs; verified `python3 -m py_compile tools/logging/*.py`, `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; no live hardware, flash, UART motor/fan/run command, calibration write, maze reset, trace format destructive operation, or protected NVM/identity write was run |
| 20260704-140250 | 20260704-140250-f413-mode1-correction-flags-off | F413 mode1標準探索の未調整補正フラグを一時OFFへ戻す | n/a | yes | user clarified front-wall correction and wall-end correction are intentionally disabled while their parameters remain untuned; reverted only the mode1 standard search case feature flags changed in the previous F405-equivalence pass, restoring case1..4 to `wall_end_correction_enabled=false` and `front_wall_correction_enabled=false`; left the search-flow port itself, event log support, post-goal safe-save guard, and phase-restart behavior unchanged; verified `cmake --build --preset Debug-stm32f413` and `git diff --check`; no live hardware, flash, UART motor/fan/run command, calibration write, maze reset, trace format destructive operation, or protected NVM/identity write was run |
| 20260704-135717 | 20260704-135717-f413-search-port-complete | F413探索走行のF405同等化残項目を移植 | n/a | yes | after the v6 search event log was confirmed usable, ported the remaining F405 search-flow semantics into F413 mode1: standard search cases now match F405 correction flags with front-wall correction enabled and wall-end disabled only for standard goal-style cases 1/3/4, full standard case2 and low-speed cases 5..8 use all corrections; added F405-style post-goal map-known tracking with `SEARCH_POST_GOAL_SAVE_NEW_CELL_THRESHOLD`, safe map-save guard that refuses to persist maps with no current route to the goal, route-fail event reason `map_save_guard`, and intermediate post-goal save at the stop before 180deg turns; changed multi-phase search (`goal->full` / `goal->start`) to stop, wait, reset control references, reverse `DIST_FIRST_SEC`, and run the entry section again before the second phase, matching the old second `adachi()` flow more closely; updated the search event analyzer to name `reverse_section` and `map_save_guard`; verified `python3 -m py_compile tools/logging/*.py`, `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, `python3 tools/logging/analyze_search_event_log.py tools/logging/logs/trace_bin_20260704_224453.csv`, and `git diff --check`; no live hardware, flash, UART motor/fan/run command, calibration write, maze reset, trace format destructive operation, or protected NVM/identity write was run |
| 20260704-133408 | 20260704-133408-f413-search-wall-read-log | F413探索ログへ壁判断に使用したsnapshot列を追加 | n/a | yes | user asked to keep event-time wall sensor samples while also preserving the exact wall sensor values used for search map wall decisions; bumped trace schema to `0x00060000` / `nightfall_trace_csv_v6` and added `wall_read_fr/r/fl/l` columns after `adc_vbat`; F413 search event rows now keep `adc_fr/r/fl/l` as the event-time snapshot and fill `wall_read_*` from the latest `f413_search_step_read_and_write_current_wall()` snapshot actually used to derive `wall_info` and update `map[][]`; updated binary trace decoding with v3/v4/v5 compatibility plus v6 support, search event analyzer detail output, web visualizer fixed groups, and trace format/Codex state docs; verified old v5 raw log decode/analysis using `tools/logging/logs/trace_bin_20260704_220653.raw`, `python3 -m py_compile tools/logging/*.py`, `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; no live hardware, flash, UART motor/fan/run command, calibration write, maze reset, or protected NVM/identity write was run |
| 20260704-131629 | 20260704-131629-f413-search-side-wall-threshold | F413全面探索の早期終了原因を解析し側壁判定を調整 | n/a | yes | inspected latest search event log `tools/logging/logs/trace_bin_20260704_220653.{raw,csv}` after mode1 case2 full-start exploration ended at `(0,3)` before reaching the goal; found the run was not a goal phase but a FULL phase, and it stopped because `(0,3)` was first mapped as `cell=0x0011` with east open, then reread on the return path as `cell=0x0055` after the left sensor reported 255 against `WALL_BASE_L=160`, closing the east opening and making all unvisited cells unreachable; raised F413 side wall presence thresholds to 300 based on this log's false opening reflection value (255) versus true side wall values above roughly 500, and ported F405-style `sensor_kx` scaling into F413 search map wall classification so search params can adjust wall detection sensitivity; replayed the latest log's ADC values locally and confirmed the final `(0,3)` FULL smap changes from `-1` to `1`; verified `cmake --build --preset Debug-stm32f413` and `git diff --check`; no live hardware, flash, UART motor/fan/run command, calibration write, maze reset, trace format destructive operation, or protected NVM/identity write was run |
| 20260704-125757 | 20260704-125757-f413-op-ui-run-end-lock | F413探索/最短走行後のOP UI入力ロックを移植 | n/a | yes | confirmed F405 `led_wait()` alternates rear LEDs forever after run-end style flows so wall-sensor enter cannot immediately retrigger a run; added an F413 OP UI run-end lock that disables PUSH/enter and UART P/E operation UI input after mode1 search cases and mode2..7 shortest cases return, while alternating rear-right/rear-left LEDs at 400ms until reset; kept the main loop alive so non-OP UART diagnostics/log dump paths remain available after reset-oriented debugging, and did not apply the lock to case0 tuning/test actions; verified `cmake --build --preset Debug-stm32f413` and `git diff --check`; no live hardware, flash, UART motor/fan/run command, calibration write, maze reset, trace format destructive operation, or protected NVM/identity write was run |
| 20260704-123757 | 20260704-123757-f413-search-log-check-and-remaining-porting | F413探索イベントログ確認と探索残項目の移植 | n/a | yes | inspected latest search event log `tools/logging/logs/trace_bin_20260704_212659.{raw,csv}` and confirmed representative action-level records are usable across a full exploration, including session/phase/decision/motion summaries, smap, wall/map cells, speed and distance/angle targets/measured values, and known-straight acceleration flags; fixed host binary decode so phase rows report their status instead of `0`, added explicit `route_fail` event/reason decoding for future no-route diagnostics, changed F413 full-explore no-current-step behavior to F405-equivalent phase completion (`full_unreachable`) instead of route failure, and ported the remaining F405 forward wall-align recovery branch using F413 front-match/spot-turn primitives with angle-accum-safe return targets; verified `python3 -m py_compile tools/logging/trace_bin_dump.py tools/logging/analyze_search_event_log.py`, redecoded/analyzed the latest raw event log, `cmake --build --preset Debug-stm32f413`, and `git diff --check`; no live hardware, flash, UART motor/fan/run command, calibration write, maze reset, trace format destructive operation, or protected NVM/identity write was run |
| 20260704-044352 | 20260704-044352-f413-search-event-log-porting | F413探索イベントログ基盤とF405探索差分の追加移植 | n/a | yes | implemented a low-rate F413 search event log on the existing FRAM trace area, recording session/phase/decision/motion-summary records instead of enabling the 1ms auto trace during full exploration; expanded host binary trace decoding with numeric event columns and added `tools/logging/analyze_search_event_log.py` for readable search-event summaries; increased search action budget to 3000 so a 32x32/10min exploration can keep full-start-to-end representative records within trace capacity; ported F405-like search behavior for `drive_wait()` around stop-and-180 turns, angle-accum-safe 180deg targets, known-straight dash acceleration/deceleration, and front-wall match/split-turn handling; retained existing user tuning edits in `params/f413_preorder/*` and `f413_mode1.c`; verified `python3 -m py_compile tools/logging/trace_bin_dump.py tools/logging/analyze_search_event_log.py`, `python3 -m py_compile tools/flashing/flash_stlink tools/flashing/flash_uart tools/flashing/make_identity_block.py tools/logging/*.py`, `cmake --build --preset Debug-stm32f413`, and `git diff --check`; F413 HIL: ST-LINK SN `003B00273234511537333934`, flashed `build/Debug/nightfall_stm32f413.elf` first from `9361512-dirty` and then clean `85a8280`, UART `/dev/cu.usbmodem111202`, motors/fan/search/shortest not commanded, non-motor `i,w` PASS after both flashes with IMU WHO_AM_I `0x6B` and wall read PASS |
| 20260704-031807 | 20260704-031807-f413-wall-control-deriv-threshold-name | F413壁制御の微分しきい値を用途名へ改名 | n/a | yes | user noted `WALL_DIFF_THR` is no longer a raw difference threshold after the F413 wall-control path was changed to use wall-end derivative values; confirmed F405 keeps the old raw wall-control difference threshold as `WALL_DIFF_THR=22`, while both F405 mini/classic use `WALL_END_DERIV_FALL_THR=200` for wall-end falling-derivative detection; renamed the F413 parameter to `WALL_CTRL_DERIV_FALL_THR` and set it to `200`, matching the F405 wall-end derivative threshold as the initial tuning point while keeping it independently adjustable from `WALL_END_DERIV_FALL_THR`; verified `cmake --build --preset Debug-stm32f413` and `git diff --check`; no live hardware, flash, UART motor/fan/run command, calibration write, maze reset, trace format change, or protected NVM/identity write was run |
| 20260704-031021 | 20260704-031021-f413-wall-control-drop-deriv | F413壁制御の壁切れ急変ガードを壁切れ微分値へ統合 | n/a | yes | user confirmed theoretical turn angles are now acceptable, corrected `WALL_BASE_L` back to `160`, and requested porting the F405 wall-control wall-drop mitigation using the wall-end derivative path rather than a separate previous-AD implementation; confirmed F405 `wall_PID()` raises the per-side wall-present threshold by 30 when sensor change exceeds `WALL_DIFF_THR`, while F413 had `WALL_DIFF_THR` defined but unused in wall control; changed F413 wall control to derive control-side wall presence from `g_wall_end.deriv_r/l`, using `WALL_DIFF_THR` as the separate wall-control falling-derivative threshold and keeping `WALL_END_DERIV_FALL_THR` for wall-end correction; when both control-side walls are absent, F413 now feeds `wall_error=0` through the existing LPF/slew path instead of immediately resetting, matching F405 decay behavior more closely; verified `cmake --build --preset Debug-stm32f413` and `git diff --check`; no live hardware, flash, UART motor/fan/run command, calibration write, maze reset, trace format change, or protected NVM/identity write was run |
| 20260621-174046 | 20260621-174046-f413-search-stop-tail | F413探索停止距離ばらつき低減のためsearch-step終端判定と停止tailを修正 | n/a | yes | user requested applying the two proposed mitigations after distance tuning exposed about +/-5mm stop variation; reviewed prior stop logs including `tools/logging/logs/trace_bin_20260620_164647.csv`, `trace_bin_20260620_204224.csv`, and `trace_bin_20260620_214750.csv`, where search/path stop endpoints overshot the distance target by several mm and then sharply dropped target velocity; changed `f413_search_step_wait_ctrl_target()` from a 10ms guarded wait to a 1ms guarded wait, matching path-run behavior and reducing endpoint detection latency at 300-500mm/s; added a search-step stop tail helper so successful final stops hold target velocity/omega at zero for `path_coast_ms` while control and auto trace remain active before `f413_ctrl_stop()`, allowing encoder distance/velocity logs to capture the residual settling interval; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; no ST-LINK flash, motor, fan, search run, shortest run, or NVM-destructive command was performed, and the existing user edits in `params/f413_preorder/params.h` were left untouched |
| 20260620-124048 | 20260620-124048-f413-mode1-search-run-entry | F413 mode1 case1〜8から通常探索走行を起動できる入口を追加 | n/a | yes | user clarified that UART commands cannot be used while running in a maze and requested F405-equivalent mode1 case1+ exploration entries; added `F413_OP_UI_ACTION_SEARCH_RUN_CASE` and routed F413 OP UI mode1 case1..8 to an automatic search session instead of the single UART-driven step flow; implemented `f413_search_step_run_search_case_once()` using the existing F413 wall snapshot/map update/action selection/motion primitives, with F405-style case mapping: case1/5 goal->full, case2/6 full, case3/7 goal->start, case4/8 goal-only, and standard/low speed selected from `searchRunParams[0/1]`; a search run now keeps one trace session across the whole mode execution and updates `op_sub=(y<<4)|x` plus `test_id=0x80|(next_rel<<2)|dir` per action so logs can be inspected after the cable is reconnected; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, and `git diff --check`; no ST-LINK flash, motor, fan, search run, shortest run, or NVM-destructive command was performed by Codex |
| 20260620-121817 | 20260620-121817-f413-maze-test-search-context-log | F413簡易迷路テスト向けに探索ステップcontextログを追加 | n/a | yes | after committing the tuned F413 control migration as `e414841`, prepared the next simple-maze validation phase by preserving the existing wall delta / wall-end trace fields and adding compact search-step context into the standard trace context fields during UART/OP search step runs: for `op_mode=1, op_case=4`, `op_sub=(y<<4)|x` and `test_id=0x80|(next_rel<<2)|dir`, so logs can be grouped by current cell, heading, and selected relative action without consuming the wall reserved fields; added CSV metadata documenting this encoding; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, and `git diff --check`; no ST-LINK flash, motor, fan, search, shortest, or NVM-destructive command was performed in this turn |
| 20260620-115319 | 20260620-115319-f413-translation-pwm-ff | F413並進速度制御へPWMフィードフォワードを追加 | n/a | yes | reviewed latest trace `tools/logging/logs/trace_bin_20260620_204224.csv` after the user observed good turn tracking but small translational velocity tracking variation; confirmed the prior segment-boundary velocity discontinuity was gone except for final stop behavior, and estimated the existing PI output during straight motion was dominated by about 70-90 PWM of static/rolling load plus smaller velocity and acceleration terms; added tunable translation feed-forward terms `FF_TRANSLATION_STATIC_PWM_*`, `FF_TRANSLATION_VELOCITY_PWM_*`, and `FF_TRANSLATION_ACCEL_PWM_*`, applied them to the translational PWM output from the raw path velocity/acceleration reference rather than the distance-corrected target velocity, and emitted the coefficients in trace CSV metadata; initial fan-off values are conservative (`45`, `0.040`, `0.0040`) while fan-on FF remains zero; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; no ST-LINK flash, motor, fan, or run command was performed in this turn |
| 20260620-075545 | 20260620-075545-f413-path-distance-continuity | F413 path直進セグメント境界の速度目標不連続を低減 | n/a | yes | reviewed latest decoded trace `tools/logging/logs/trace_bin_20260620_164647.csv` after the user observed audible chatter and target-velocity steps; found `target_distance_mm`/`distance_mm` reset at straight segment boundaries causing target velocity drops such as 210->162mm/s and 554->497mm/s, plus a final overshoot-induced negative target velocity from the 10ms wait cadence; changed `f413_path_run_drive_segment()` to keep cumulative distance state across path segments and wait for current distance plus the segment length instead of resetting distance/PID state at every segment, and changed the path wait loop cadence from 10ms to 1ms to reduce endpoint overshoot; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, and `git diff --check`; no ST-LINK flash, motor, fan, or run command was performed in this turn |
| 20260620-143417 | 20260620-143417-f413-omega-profile-control-tick | F413 case0ターン目標ω生成を制御tick側へ移動 | n/a | yes | user observed the latest mode2 case0 log still looked oscillatory and pointed out that the target omega itself was staircase-like; confirmed `trace_bin_20260620_142746.csv` showed large target omega steps such as about -37/-139/-280/-421dps during one turn, consistent with the path runner updating `f413_ctrl_set_omega()` from the main context while trace/NVM auto-step work can stall that context; added F413 control APIs to start/stop a cosine omega profile owned by the 1kHz control tick, updated the case0 smooth-turn runner to start the profile once and only wait/guard from main context, and kept the user's adjusted omega gains/lead values unchanged; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; no ST-LINK flash, motor, fan, or run command was performed |
| 20260620-051554 | 20260620-051554-f413-case0-continuous-test-speed | F413 mode2 case0確認向けにテスト速度接続を旧作寄りへ修正 | n/a | yes | user observed likely speed discontinuity between the straight lead-in and turn in case0 tests; changed the F413 OP path-code test runner so segments inside one `mode2..7 case0` test sequence no longer insert a coast/zero-velocity pause after every path code, and straight/diagonal lead-in segments choose the next turn's capped turn velocity when followed by a turn, matching the old F405 mini style of entering a slalom at turn speed; raised `NIGHTFALL_F413_OP_TEST_VEL_CAP` / turn cap to 500mm/s for mode2 confirmation while retaining `NIGHTFALL_F413_OP_TEST_OMEGA_CAP`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; no ST-LINK flash, motor, fan, or run command was performed |
| 20260620-045217 | 20260620-045217-f413-case0-smooth-omega-profile | F413 mode2..7 case0テストに滑らかな角速度プロファイルを適用 | n/a | yes | user confirmed the latest omega tracking improved with the lead compensation and requested trying it outside mode9 case0; updated the F413 OP path-code test runner so `mode2..7 case0 sub0..7` turn motions use an F405-derived cosine-blended omega profile from the mode turn `alpha_*` / angle params instead of a step angle target, while leaving solver-path real shortest execution on the existing angle-target path; capped test turn velocity with `NIGHTFALL_F413_OP_TEST_TURN_VEL_CAP` and omega with `NIGHTFALL_F413_OP_TEST_OMEGA_CAP`, preserving the existing safe test-speed posture; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; no ST-LINK flash, motor, fan, or run command was performed |
| 20260615-075836 | 20260615-075836-f413-omega-lead-ff | F413角速度目標の先読み補償とPWM FF設定を追加 | n/a | yes | implemented a 5ms target-omega lead path for the F413 omega loop, clamped by `FF_OMEGA_LEAD_MAX_DPS`, and added zero-default `FF_OMEGA_PWM_*` / `FF_OMEGA_ACCEL_PWM_*` params for later plant-delay tuning; normal run and tune run now share the same omega-output helper, obsolete local F413 omega FF macro was removed from the f413 params profile, and CSV metadata now records the active omega lead/FF settings; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; no ST-LINK flash, motor, fan, or run command was performed |
| 20260615-065757 | 20260615-065757-f413-remove-gyro-lpf-trace-candidates | F413 IMU Z複数LPF候補ログを削除しtrace v5へ更新 | n/a | yes | user decided the IMU Z LPF candidate comparison was no longer needed; removed the 2/5/10/20ms gyro Z LPF state/getters and CSV columns, kept `gyro_z_raw_mdps`, bumped trace schema to `0x00050000` / `nightfall_trace_csv_v5`, updated firmware CSV dump, binary dump tooling with v3/v4/v5 layout compatibility, PlotJuggler export, visualizer, trace format spec, and AI state docs; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `python3 -m py_compile tools/logging/*.py`, `git diff --check`, v4 raw binary decode using `trace_bin_20260615_153244.raw`, and PlotJuggler export of `trace_bin_20260615_153244.csv`; no ST-LINK flash, motor, fan, or run command was performed |
| 20260615-054540 | 20260615-054540-f413-gyro-lpf-trace-v4 | F413 IMU Z角速度の生値/複数LPF候補をtrace v4へ追加 | n/a | yes | user requested logging to compare IMU raw gyro Z against several LPF time constants for turn-response/noise tuning; bumped trace schema to `0x00040000` / `nightfall_trace_csv_v4`, added `gyro_z_raw_mdps` and 2/5/10/20ms LPF fields to F413 control samples, CSV/bin dump, PlotJuggler export, and web visualizer omega plots; kept existing F413 control omega LPF behavior at the hardcoded 20ms path while logging alternative candidates; reduced F413 trace RAM ring from 2048 to 1536 records so v4 record growth still leaves RAM headroom (`241840 B / 320 KB = 73.80%`); verified `cmake --build --preset Debug-stm32f413`, Python py_compile for logging tools, `git diff --check`, and v3 raw binary log decode/export compatibility using `trace_bin_20260615_135328.raw`; no ST-LINK flash, motor, fan, or run command was performed, and the existing uncommitted `params/f413_preorder/params.h` gain-tuning edits were intentionally left separate |
| 20260607-114059 | 20260607-114059-f413-imu-diag-module-split | F413 IMU診断を専用モジュールへ分離 | n/a | yes | on branch `codex/f413-test-run-split` after commit `6a1278f`, moved F413 IMU SPI register access, WHO_AM_I check, manual gyro-angle test, and accelerometer observation test from `main.c` into `f413_imu_diag.h/c`; updated run-session IMU health check to use `f413_imu_diag_whoami_ok()` and left UART/OP routing in `main.c`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; F413 HIL: ST-LINK SN `003B00273234511537333934`, UART `/dev/cu.usbmodem111202` at 921600, motors/fan not commanded, flashed `build/Debug/nightfall_stm32f413.elf`, non-motor smoke `i,w,v` passed with IMU `WHO_AM_I=0x6B`, wall PASS, bounded CSV `tools/logging/logs/stm32_log_20260607_204030.csv`; also verified split search status/dump path with non-destructive UART `[,@` (`[SEARCH-STATE] ... fram=OK`, `[SEARCH-DUMP] source=FRAM`) |
| 20260607-113602 | 20260607-113602-f413-search-step-module-split | F413 search step/map操作を専用モジュールへ分離 | n/a | yes | on branch `codex/f413-test-run-split` after commit `5a49795`, moved F413 search preview, state reset, step runner, map probe/status/clear/dump, local BFS step map, relative decision, and shared `map/visited/smap/mouse` handling from `main.c` into `f413_search_step.h/c`; kept `main.c` as config/routing glue through callbacks for stop switch, wall snapshot, trace context/run hooks, trace flags, tick, and straight wall-control application; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, `python3 -m py_compile tools/flashing/flash_stlink tools/flashing/flash_uart tools/flashing/make_identity_block.py tools/hil/f413_safe_hil.py`, and `git diff --check`; attempted F413 flash/non-motor HIL via ST-LINK SN `003B00273234511537333934` and UART `/dev/cu.usbmodem112202`, but the host reported `No ST-Link detected` and no `/dev/cu.usbmodem*`, so no firmware was flashed and no motor/fan/NVM command was run in this HIL attempt |
| 20260607-111918 | 20260607-111918-f413-test-run-module-split | F413 test-run状態機械を専用モジュールへ分離 | n/a | yes | on branch `codex/f413-test-run-split` at firmware `stable/classic/unit001/s20260404-nationals-161-g3e18ec6-dirty`, moved F413 test-run name/arm/button state, single-motor `6`-`9`, and closed-loop tests `1`-`5` from `main.c` into `f413_test_run.h/c` with callback boundaries for trace, run-session guard, wall-control, encoder, motor, tick/delay, and result metadata; added `f413_test_run.c` to F413 CMake and left `main.c` as configuration/routing glue; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, `python3 -m py_compile tools/flashing/flash_stlink tools/flashing/flash_uart tools/flashing/make_identity_block.py tools/hil/f413_safe_hil.py`, and `git diff --check`; flashed F413 via ST-LINK SN `003B00273234511537333934` and UART `/dev/cu.usbmodem112202` at 921600 with motors allowed because the user confirmed the machine was lifted/fixed; non-motor smoke passed (`i,w,v`, WHO_AM_I `0x6B`, wall PASS, bounded CSV `tools/logging/logs/stm32_log_20260607_201426.csv`); motor HIL passed UART `6,7,8,9` with expected active-side signs (`enc_l=2828`, `enc_r=3118`, `enc_l=-3328`, `enc_r=-2411`); OP UI direct closed-loop test passed via `PPPPPPPPEPE` (`mode8 case1`) with `straight 3 cells heading hold` OK `dist=274mm angle=-0deg` |
| 20260607-052144 | 20260607-052144-f413-codex-hil-loop-smoke | CodexからF413実機HIL基本ループを確認 | n/a | yes | on branch `codex/codex-primary-migration-docs` commit `cc146a1`, configured `Debug`, built `Debug-stm32f413` and `Debug-stm32f405`, flashed F413 via ST-LINK SN `003B00273234511537333934`, verified UART `/dev/cu.usbmodem112202` at 921600; non-motor `i,w,V` passed (`WHO_AM_I=0x6B`, wall PASS) and saved `tools/logging/logs/stm32_log_20260607_141602.csv`; motor allowed because machine was lifted/secured, ran UART `y,V` and saved/analyzed `stm32_log_20260607_141810.csv` with 899 records and forward/coast/reverse flags, then ran UART `6,7` and got `enc_l=2803` and `enc_r=3181`; observed `y` trace flags are present but open-loop motor trace CSV reports `motor_out_l/r=0` and `d_enc=(0,0)`, so future tooling/code should distinguish flag-only open-loop trace from sampled motor/encoder fields; fixed `tools/hil/f413_safe_hil.py reset-capture` to use non-interactive serial capture because `serial_terminal.py` produced a 0-byte boot log in Codex execution |
| 20260607-045854 | 20260607-045854-codex-git-github-policy | Codex移行資料のGit/GitHub管理方針を整理 | n/a | yes | added `docs/ai/GIT_GITHUB_POLICY.md`; updated `AGENTS.md`, Codex onboarding, and AI README to make branch/commit/PR/backup policy explicit; one-off migration backups are kept Git-ignored while canonical docs/tools are committed and published through a draft PR |
| 20260607-044540 | 20260607-044540-codex-primary-migration | Codex主担当移行の入口資料を整備 | n/a | yes | backed up Windsurf/Cascade-era docs to `docs/ai/archive/codex-migration-backup-20260607/`; added root `AGENTS.md`, `docs/ai/CODEX_ONBOARDING.md`, `docs/ai/F413_PORTING_STATE.md`, `docs/ai/HIL_SAFETY.md`, and safe non-motor HIL wrapper `tools/hil/f413_safe_hil.py`; updated AI state/readme/policy docs for Codex primary operation |
| 20260405-022753 | 20260405-022753-codex-delegation-smoke-test | codex delegation smoke test | 0 | no | handoff: /Users/xfa273/workspace/micromouse/nightfall-fw/docs/ai/archive/delegation-legacy/HANDOFFS/20260405-022753-codex-delegation-smoke-test-result.md |
| 20260405-022914 | 20260405-022914-codex-delegation-smoke-test-v2 | codex delegation smoke test v2 | 0 | no | handoff: /Users/xfa273/workspace/micromouse/nightfall-fw/docs/ai/archive/delegation-legacy/HANDOFFS/20260405-022914-codex-delegation-smoke-test-v2-result.md |
| 20260405-030155 | 20260405-030155-add-delegation-ops-section-to-dev-plan | add delegation ops section to dev plan | 0 | no | handoff: /Users/xfa273/workspace/micromouse/nightfall-fw/docs/ai/archive/delegation-legacy/HANDOFFS/20260405-030155-add-delegation-ops-section-to-dev-plan-result.md |
| 20260405-030300 | 20260405-030155-add-delegation-ops-section-to-dev-plan | apply diff | n/a | yes | source: /Users/xfa273/workspace/micromouse/nightfall-fw/docs/ai/archive/delegation-legacy/HANDOFFS/20260405-030155-add-delegation-ops-section-to-dev-plan.diff |
| 20260405-030516 | 20260405-030516-phase3-nvm-identity-robustness-and-f405-address | phase3 nvm identity robustness and f405 address | 0 | no | handoff: /Users/xfa273/workspace/micromouse/nightfall-fw/docs/ai/archive/delegation-legacy/HANDOFFS/20260405-030516-phase3-nvm-identity-robustness-and-f405-address-result.md |
| 20260405-030803 | 20260405-030516-phase3-nvm-identity-robustness-and-f405-address | apply diff | n/a | yes | source: /Users/xfa273/workspace/micromouse/nightfall-fw/docs/ai/archive/delegation-legacy/HANDOFFS/20260405-030516-phase3-nvm-identity-robustness-and-f405-address.diff |
| 20260405-032752 | 20260405-032752-phase3-f405-identity-safe-mode-and-f413-identity | phase3 f405 identity safe mode and f413 identity address finalize | 0 | no | handoff: /Users/xfa273/workspace/micromouse/nightfall-fw/docs/ai/archive/delegation-legacy/HANDOFFS/20260405-032752-phase3-f405-identity-safe-mode-and-f413-identity-result.md |
| 20260405-033600 | 20260405-032752-phase3-f405-identity-safe-mode-and-f413-identity | apply diff (manual) | n/a | yes | source: /Users/xfa273/workspace/micromouse/nightfall-fw/docs/ai/archive/delegation-legacy/HANDOFFS/20260405-032752-phase3-f405-identity-safe-mode-and-f413-identity.diff |
| 20260406-114530 | 20260406-114530-migrate-hatena-post-2024-12-26 | migrate hatena post 2024-12-26 | 0 | no | handoff: /Users/xfa273/workspace/micromouse/nightfall-fw/docs/ai/archive/delegation-legacy/HANDOFFS/20260406-114530-migrate-hatena-post-2024-12-26-result.md |
| 20260406-115019 | 20260406-114530-migrate-hatena-post-2024-12-26 | apply diff | n/a | yes | source: /Users/xfa273/workspace/micromouse/nightfall-fw/docs/ai/archive/delegation-legacy/HANDOFFS/20260406-114530-migrate-hatena-post-2024-12-26.diff |
| 20260406-115844 | 20260406-115844-migrate-remaining-hatena-micromouse-posts | migrate remaining hatena micromouse posts | 0 | no | handoff: /Users/xfa273/workspace/micromouse/nightfall-fw/docs/ai/archive/delegation-legacy/HANDOFFS/20260406-115844-migrate-remaining-hatena-micromouse-posts-result.md |
| 20260406-120659 | 20260406-115844-migrate-remaining-hatena-micromouse-posts | apply diff | n/a | yes | source: /Users/xfa273/workspace/micromouse/nightfall-fw/docs/ai/archive/delegation-legacy/HANDOFFS/20260406-115844-migrate-remaining-hatena-micromouse-posts.diff |
| 20260406-122249 | 20260406-122249-migrate-three-additional-hatena-posts | migrate three additional hatena posts | 0 | no | handoff: /Users/xfa273/workspace/micromouse/nightfall-fw/docs/ai/archive/delegation-legacy/HANDOFFS/20260406-122249-migrate-three-additional-hatena-posts-result.md |
| 20260406-123224 | 20260406-122249-migrate-three-additional-hatena-posts | apply diff | n/a | yes | source: /Users/xfa273/workspace/micromouse/nightfall-fw/docs/ai/archive/delegation-legacy/HANDOFFS/20260406-122249-migrate-three-additional-hatena-posts.diff |
| 20260418-120841 | 20260418-120841-cascade-only-policy-update | switch to Cascade-only execution policy | n/a | n/a | updated AGENTS/docs/workflows to suspend Codex delegation |
| 20260418-123044 | 20260418-123044-f413-mini-r2-0-unit001-identity-bringup | F413 mini初号機にapp+identityを書き込み | n/a | yes | flashed build/Debug/nightfall_stm32f413.bin and build/identity/mini_r2_0_unit001.bin (family=mini, board_id=0x00020000, rev=2.0, unit=1) |
| 20260418-125125 | 20260418-125125-f413-uart-trace-temporary-mode | F413のログ経路を暫定UART運用へ固定 | n/a | yes | set NIGHTFALL_TRACE_F413_USE_UART=1 in cmake, fixed trace.c huart1 extern, rebuilt Debug-stm32f413 and reflashed via flash_uart |
| 20260418-130633 | 20260418-130633-f413-nvm-uart-cli-verification-entry | F413のdistance/sensor/maze検証入口をUARTコマンドで追加 | n/a | yes | add NVM test commands(h/a/d/s/m) in f413 main.c, rebuild Debug-stm32f413 for checklist step5 execution |
| 20260418-130835 | 20260418-130835-f413-nvm-uart-cli-flash | F413へNVM UART検証入口付きファームを再書き込み | n/a | yes | flashed build/Debug/nightfall_stm32f413.bin (45864 bytes) via flash_uart --erase app; profile stm32f413 protected sectors 12-15 |
| 20260418-131145 | 20260418-131145-f413-nvm-step5-device-pass | F413実機でNVM step5（save/load + load-only）を確認 | n/a | yes | serial logs show a-command overall PASS and reboot after A-command load-only PASS for distance/sensor/maze |
| 20260418-131324 | 20260418-131324-f413-step5-doc-sync | F413実機検証結果をPhase3/計画系ドキュメントへ反映 | n/a | yes | updated EXPERIMENT_LOG, PHASE3_CHECKLIST, NVM_MIGRATION_PLAN, NIGHTFALL_FW_2026_DEVELOPMENT_PLAN with Step2-5 verification status |
| 20260418-131948 | 20260418-131948-f413-fram-backend-enable | F413 NVMをFRAM backendへ切替実装 | n/a | yes | updated nvm.c backend dispatch (identity=internal flash, others=external FRAM), built Debug-stm32f413 and reflashed new binary (49456 bytes) |
| 20260418-132520 | 20260418-132520-f413-fram-distance-save-fix | FRAM distance save失敗の根因修正（base=0判定） | n/a | yes | fixed nvm_get_area_info() to allow FRAM offset base 0x00000000, rebuilt Debug-stm32f413 and reflashed new binary (49464 bytes) |
| 20260418-132958 | 20260418-132958-f413-fram-trace-log-test-entry | F413 NVMテストへtrace_log(FRAM)検証コマンドを追加 | n/a | yes | add UART commands t/T for NVM_AREA_TRACE_LOG save-load and load-only verify, rebuilt Debug-stm32f413 and reflashed binary (50336 bytes) |
| 20260419-062400 | 20260419-062400-f413-fram-device-verify-all-areas | F413 FRAM backendの全NVM area実機検証を完了 | n/a | yes | user logs confirm `a/A/t/T` all PASS for distance/sensor/maze/trace (`GIT=7f02576 DIRTY=1`), docs synchronized (EXPERIMENT_LOG/NVM_MIGRATION_PLAN/PHASE3_CHECKLIST/DEVELOPMENT_PLAN) |
| 20260419-063200 | 20260419-063200-f413-hardware-bringup-cli-and-checklist | F413ハード単体確認コマンドとチェックリストを整備 | n/a | yes | added UART hardware test commands `w/p/i/b/o/e/g` in f413 main.c, built Debug-stm32f413/Debug-stm32f405 successfully, added docs/F413_HARDWARE_BRINGUP_CHECKLIST.md |
| 20260419-065500 | 20260419-065500-f413-wall-sensor-cli-adc-read-fix | F413壁センサCLIのADC読取失敗を修正 | n/a | yes | replaced scan-based wall test read with per-channel single conversion (`HAL_ADC_ConfigChannel` + poll), restore original ADC config after test via `MX_ADC1_Init()`, rebuilt Debug-stm32f413 |
| 20260419-074400 | 20260419-074400-f413-encoder-cli-window-and-restart-fix | F413エンコーダCLIを3秒測定へ改修 | n/a | yes | changed `e` command to 3000ms window, added encoder stop/counter reset/start/stop sequence to avoid intermittent `FAIL(start L)`, rebuilt Debug-stm32f413, updated bring-up checklist step 6 |
| 20260419-104700 | 20260419-104700-f413-led-30s-uart-test | F413にLED30秒点灯UARTテストを追加 | n/a | yes | added command `l/L` to turn on all LEDs for 30000ms for photo capture, updated CLI help and bring-up checklist, rebuilt Debug-stm32f413 |
| 20260425-073000 | 20260425-073000-phase4-trace-log-schema-v1 | Phase4 trace_log形式v1固定とUART検証コマンドを実装 | n/a | yes | added `nvm_trace_log.(h/c)` with header/record schema + format/append/read API, updated F413 CLI (`q/r/R`) and help, set trace area schema version in nvm table, updated migration plan/spec docs, built Debug-stm32f413 and Debug-stm32f405 |
| 20260430-174800 | 20260430-174800-phase4-trace-log-csv-selftest | Phase4 trace_logにCSV dumpと自己検証コマンドを追加 | n/a | yes | added F413 UART commands `v/V` (CSV dump with `#mm_columns`) and `k/K` (format+append+readback selftest), updated trace-log spec/migration/development docs, rebuilt Debug-stm32f413 and Debug-stm32f405 |
| 20260430-175600 | 20260430-175600-phase4-trace-log-auto-capture | Phase4 trace_logに定周期自動収集コマンドを追加 | n/a | yes | added F413 UART commands `u`(start auto) and `U`(stop auto), periodic append service in main loop (10ms), auto records set flag bit15, updated docs/specs, rebuilt Debug-stm32f413 and Debug-stm32f405 |
| 20260430-180200 | 20260430-180200-phase4-trace-log-csv-fw-meta | trace CSVダンプへFW/identityメタ情報を追加 | n/a | yes | `v` command now emits `#fw_*` lines (target/version/build/git/schema/family/board/rev/unit), updated trace-log format spec, rebuilt Debug-stm32f413 and Debug-stm32f405 |
| 20260430-180800 | 20260430-180800-phase4-trace-log-csv-all-dump | trace CSVダンプに全件取得コマンドVを追加 | n/a | yes | split CSV dump into `v`(latest 256) and `V`(all stored records), updated trace-log format spec, rebuilt Debug-stm32f413 and Debug-stm32f405 |
| 20260430-181200 | 20260430-181200-phase4-trace-log-run-hook-wrapper | trace log開始停止をrun hookラッパー経由に整理 | n/a | yes | `u/U` command path now calls `nightfall_trace_log_on_run_start/stop`, CLI help/spec wording updated to run-hook semantics for future run event linkage, rebuilt Debug-stm32f413 and Debug-stm32f405 |
| 20260430-181700 | 20260430-181700-phase4-trace-log-auto-step-lightweight | trace自動収集ループのヘッダ読出しを削減して軽量化 | n/a | yes | auto step now uses RAM-managed `g_trace_log_auto_seq` instead of `nvm_trace_log_get_header()` every 10ms tick, keeping output schema unchanged, rebuilt Debug-stm32f413 and Debug-stm32f405 |
| 20260430-182300 | 20260430-182300-machine-naming-and-identity-rules | 機体命名と識別子ルールを確定し起動ログ/CSVへ反映 | n/a | yes | clarified 3-layer naming (`FW_TARGET` / `machine_name` / `machine_unit`), fixed board_id encoding rule (`0x00MMNNVV`) in policy/spec docs, updated F413 boot log and trace CSV metadata to emit family name and machine names, rebuilt Debug-stm32f413 and Debug-stm32f405 |
| 20260430-183100 | 20260430-183100-run-hook-connected-to-smoke-path | run start/stop hookをF413実行経路へ接続 | n/a | yes | wired `g` command path to `nightfall_trace_log_on_run_start/stop` via smoke+trace session wrapper, added auto start/stop idempotent guards (`already running/stopped`), updated trace-log spec/help wording, rebuilt Debug-stm32f413 and Debug-stm32f405 |
| 20260430-183600 | 20260430-183600-smoke-trace-auto-step-fix | g実行中にtrace auto_stepが回らない不具合を修正 | n/a | yes | fixed `g` smoke+trace path where loop-level `auto_step` was not reached during blocking smoke command, inserted explicit `nightfall_trace_log_auto_step()` calls between smoke subtests and before stop hook; resolves `auto: STOP total=0` symptom, rebuilt Debug-stm32f413 and Debug-stm32f405 |
| 20260430-184200 | 20260430-184200-run-session-idle-command | runセッション最小検証コマンドxを追加 | n/a | yes | added `x/X` command to run 1000ms idle trace session via `run-start`/`run-stop` hook path without motor actuation, prints `[RUN-TEST]` logs, updated CLI help and trace-log spec, rebuilt Debug-stm32f413 and Debug-stm32f405 |
| 20260430-184900 | 20260430-184900-run-session-motor-command | runセッション短時間モータ検証コマンドyを追加 | n/a | yes | added `y/Y` command to run short forward/reverse motor pulses inside run-start/stop hook trace session, added auto-step wait helper for deterministic 10ms capture during blocking waits, updated trace-log spec and F413 bring-up checklist, rebuilt Debug-stm32f413 and Debug-stm32f405 |
| 20260430-185400 | 20260430-185400-run-session-phase-flags | runセッション位相をtrace flagsへ記録 | n/a | yes | added v1 flag bits for idle/forward/coast/reverse/smoke phases and mode-flag propagation in auto capture path, updated `x/y/g` to set phase flags during run sessions, documented new flag map and checklist expectations, rebuilt Debug-stm32f413 and Debug-stm32f405 |
| 20260430-190200 | 20260430-190200-host-trace-phase-analyzer | trace CSV位相要約のホストツールを追加 | n/a | yes | added `tools/logging/analyze_trace_csv.py` to summarize phase segments from `flags` bits and validate expected session phases (`--expect x/y`), updated logging README and trace-log spec host workflow, validated script with `python3 -m py_compile` |
| 20260424-102129 | 20260424-102129-blog-machine-pages-template | ブログトップ機体一覧リンクと機体紹介テンプレートを追加 | n/a | yes | added `docs/blog/machines/*/README.md` templates and linked from `docs/blog/README.md`; deploy via GitHub Pages |
| 20260430-101900 | 20260430-101900-logging-default-path-repo-and-analyzer-dir-fallback | CSVログ既定保存先をrepo内へ統一し解析の空ファイル誤選択を改善 | n/a | yes | changed defaults to `tools/logging/logs` in `serial_capture_csv.py`/`.sh` and `visualizer/web_visualizer.py`; `analyze_trace_csv.py` now picks latest CSV containing trace rows when a directory is given |
| 20260430-102700 | 20260430-102700-serial-capture-inline-command-send | CSVキャプチャ中にUARTコマンド送信を可能化 | n/a | yes | extended `tools/logging/serial_capture_csv.py` with `--send` auto command sequence and interactive stdin command send, so `q/y/V` can be issued without opening another serial monitor; updated `tools/logging/README.md` |
| 20260430-103800 | 20260430-103800-run-trace-x-validation-and-next-plan | x実測検証完了と探索/最短統合作業計画の追加 | n/a | yes | validated `q,x,V` capture with `analyze_trace_csv.py --expect x` (idle phase), hardened `serial_capture_csv.py` (skip non-ASCII cmd, graceful serial read stop), corrected checklist idle flag expectation to `32770`, and added Phase 4.5 plan for F413 exploration/shortest integration in development plan |
| 20260430-104100 | 20260430-104100-analyzer-expect-aware-file-select | analyzeのディレクトリ選択をexpect位相連動に改善 | n/a | yes | updated `tools/logging/analyze_trace_csv.py` to prefer latest CSV matching `--expect x/y` when a directory is given, avoiding x/y連続試験での誤ファイル選択; updated `tools/logging/README.md` |
| 20260430-120500 | 20260430-120500-phase45-safe-zj-run-entry | Phase4.5 step1としてz/j安全run-trace入口を追加 | n/a | yes | added F413 UART commands `z`(search-safe) and `j`(shortest-safe) with low-speed short motion, PUSH-switch immediate abort, trace flags bit6/bit7 logging, updated F413 help text, trace-log format spec, bring-up checklist, and development plan progress note |
| 20260430-123323 | 20260430-123323-phase45-zj-csv-validation-and-analyzer | Phase4.5 step2としてz/j実測検証とhost解析導線を整備 | n/a | yes | confirmed z/j measured CSV flags and switch-abort behavior, extended `tools/logging/analyze_trace_csv.py` with `--expect z/j` (including directory auto-select), and synced `tools/logging/README.md` / `docs/F413_HARDWARE_BRINGUP_CHECKLIST.md` / `docs/NVM_TRACE_LOG_FORMAT_SPEC.md` / development plan |
| 20260430-124200 | 20260430-124200-phase45-zj-run-guard-and-abort-flags | Phase4.5 step3としてz/jにrun guardと停止理由flagsを統合 | n/a | yes | integrated guard checks in F413 `z/j` path for switch/wall/encoder/imu faults, emit abort reason flags bit8-bit11 in trace CSV, updated trace-log spec / bring-up checklist / development plan, rebuilt Debug-stm32f413 |
| 20260430-130100 | 20260430-130100-serial-capture-ctrlc-exit-fix | serial_capture_csvのCtrl+C終了不能を修正 | n/a | yes | changed `tools/logging/serial_capture_csv.py` to non-blocking serial open and non-blocking stdin incremental read (removed blocking `readline` path) so SIGINT can terminate reliably; validated with `python3 -m py_compile` |
| 20260430-131600 | 20260430-131600-trace-analyzer-abort-summary-and-signed-enc-delta | z/j解析の停止理由要約とエンコーダ差分表示を改善 | n/a | yes | updated `tools/logging/analyze_trace_csv.py` to print abort_count(bit8-bit11) and compute segment `d_enc` as signed 16-bit delta to suppress wrap artifacts; updated `tools/logging/README.md`; validated by `py_compile` and sample `--expect z/j` runs |
| 20260430-132300 | 20260430-132300-phase45-zj-entry-wrapper-step4 | Phase4.5 step4としてz/jを探索/最短実行入口ラッパーへ接続 | n/a | yes | added F413 `nightfall_run_search_trace_entry_once` / `nightfall_run_shortest_trace_entry_once` and routed UART `z/j` through these entry points (safe fallback path retained), updated help/checklist/development plan wording, rebuilt Debug-stm32f413 |
| 20260430-133000 | 20260430-133000-phase45-zj-real-path-gate-macro | F413 z/j入口の本経路切替ゲートをCMake定義化 | n/a | yes | added `NIGHTFALL_F413_REAL_RUN_PATH_ENABLED=0` compile definition in `cmake/stm32cubemx/CMakeLists.txt`, made main.c gate configurable with fallback default, and added explicit gate-on trace messages at z/j entry wrappers; rebuilt Debug-stm32f413 |
| 20260430-133900 | 20260430-133900-phase45-zj-real-path-draft-bfs | Phase4.5 step5としてz/j入口に本経路ドラフト算出を追加 | n/a | yes | added gate-on draft path check in F413 main.c (`nvm_maze_load_map` + BFS shortest-step estimation to goal) and entry logs before safe fallback execution, updated development plan and trace-log spec notes, rebuilt Debug-stm32f413 |
| 20260430-134600 | 20260430-134600-phase45-zj-real-path-draft-metrics | z/j入口の本経路ドラフト指標を拡張 | n/a | yes | enhanced F413 gate-on BFS draft output to include turn count and straight segment count in addition to steps by reconstructing path from predecessor chain; updated development plan/spec notes; rebuilt Debug-stm32f413 |
| 20260501-000000 | 20260501-000000-phase45-step6-solver-integration | Phase4.5 step6: solver/path/maze_gridモジュールをF413ビルドに統合 | n/a | yes | created `params/f413_preorder/` (params.h + run params), `f413_solver_bridge.c` (MAIN_C_ globals + load_map_from_eeprom NVM adapter), added solver.c/solver_params.c/path.c/maze_grid.c to F413 CMake sources, replaced BFS draft in z/j entry wrappers with `solver_build_path()` (Dijkstra + simplify + L-turn conversion), rebuilt Debug-stm32f413 (RAM 55KB/320KB, FLASH 81KB/1536KB) and Debug-stm32f405 |
| 20260501-001500 | 20260501-001500-phase45-step7-solver-path-execution | Phase4.5 step7: solver-path オープンループ走行セッションを実装 | n/a | yes | added `nightfall_run_solver_path_session_once()` to F413 main.c: reads solver-generated `path[]` codes and executes open-loop motor commands (straight=forward×time, turn=rotate×time, coast between segments), with guard checks and trace flags (new bit12=SOLVER_PATH_FLAG); z/j entry wrappers call this when solver succeeds, fallback to safe session on failure; updated development plan Step7; rebuilt Debug-stm32f413 (RAM 55KB, FLASH 83KB) and Debug-stm32f405 |
| 20260501-084700 | 20260501-084700-phase45-step8-closed-loop-control | Phase4.5 step8: TIM5 1kHz PID速度制御とクローズドループ走行 | n/a | yes | created `f413_control.c/h` with TIM5 1kHz interrupt: TIM3/TIM4 encoder velocity/omega calculation + P+FF velocity/omega control + TIM2 motor PWM output; added `HAL_TIM_PeriodElapsedCallback` in main.c; rewrote solver path session to use `f413_ctrl_start/stop/set_velocity/set_omega` with distance(45mm×N)/angle(90°/180°) target completion; rebuilt Debug-stm32f413 (RAM 55KB/320KB, FLASH 84KB/1536KB) and Debug-stm32f405 |
| 20260501-090000 | 20260501-090000-phase45-step8-imu-and-encoder-direction | Step8補: IMU角速度制御追加とエンコーダ方向確定 | n/a | yes | added ISM330DHCX IMU init/read/offset/LPF to f413_control.c (角速度をエンコーダ差動→IMUジャイロに変更); confirmed encoder direction from y-command CSV (forward=counter decrease → *(-1.0f) for positive velocity); rebuilt Debug-stm32f413 |
| 20260501-105700 | 20260501-105700-phase45-test-mode | 調整用テストモードを追加 | n/a | yes | added UART commands 1-5 for direct test execution (S3/S6 straight, R90/L90 turn, S3+R90+S3 combo) with closed-loop control + trace log; added F command for button-armed untethered execution (disconnect UART → button press → test runs → FRAM save → reconnect → V dump); rebuilt Debug-stm32f413 (RAM 55KB, FLASH 86KB) |
| 20260506-054700 | 20260506-054700-f413-migration-backup-before-doc-sync | F413移植作業ツリーをバックアップ | n/a | yes | created and pushed `backup/20260506-1447-before-f413-doc-sync` with WIP commit `e3d925b` before documentation synchronization; excluded generated `site/` and captured CSV logs from commit |
| 20260506-055000 | 20260506-055000-f413-phase45-doc-sync | F413 Phase4.5進捗ドキュメント同期 | n/a | yes | updated development plan, trace-log format spec, bring-up checklist, and logging README for `NIGHTFALL_F413_REAL_RUN_PATH_ENABLED=1`, closed-loop solver path, trace flag bit12, and UART test commands `1`-`9`/`F` |
| 20260506-061540 | 20260506-061540-workspace-context-cleanup | コンテクスト消費削減のためワークスペースを軽量化 | n/a | yes | removed stale `.ai/worktrees` + generated `build*`/`site`/`tools/logging/logs` and added root `.codeiumignore` (`_sandbox`, `docs/blog`, `docs/datasheets`, hardware CAD assets, generated artifacts) |
| 20260506-061859 | 20260506-061859-delegation-ignore-policy-sync | 委譲関連資産を復活指示まで無視する運用へ整理 | n/a | yes | updated `AGENTS.md`, `docs/ai/STATE.md`, `docs/ai/README.md`, and root `.codeiumignore` to keep delegation assets as archive but out of normal reference/index scope |
| 20260506-062604 | 20260506-062604-delegation-legacy-archive-move | 委譲関連の不要資産をアーカイブへ移動して整理 | n/a | yes | consolidated delegation legacy assets under `docs/ai/archive/delegation-legacy/` and synchronized references/ignore paths |
| 20260506-063912 | 20260506-063912-f413-motor-encoder-control-prehw | F413モータ/エンコーダ/並進制御の実機前確認 | n/a | yes | fixed F413 open-loop motor helper and trace CSV `motor_out_l/r` mapping to match hardware (`TIM2_CH3=left`, `TIM2_CH1=right`); rebuilt Debug-stm32f413 and Debug-stm32f405 successfully; next step is device verification with UART commands `o/e/6/7/8/9/1` |
| 20260506-064543 | 20260506-064543-agents-archive-and-remove | AGENTS運用ルールをアーカイブ化して削除 | n/a | yes | moved root `AGENTS.md` to `docs/ai/archive/AGENTS.md`, updated related references, and prepared clean git state before resuming migration work |
| 20260506-065803 | 20260506-065803-f413-encoder-sign-and-zero-alias | F413実機エンコーダ確認結果を反映 | n/a | yes | added UART `0` alias for motor test (`o`) to avoid zero/O confusion; fixed encoder delta display to handle TIM3/TIM4 wraparound and invert right encoder sign; updated closed-loop velocity calculation with `L=+1`, `R=-1`; rebuilt Debug-stm32f413 successfully |
| 20260506-072341 | 20260506-072341-f413-hw-enc-open-loop-simplify | F413片側モータ+エンコーダ確認の実機結果を反映 | n/a | yes | user confirmed motor test `0/o` and encoder test `e` are OK; changed UART `6`-`9` HW encoder/motor checks to bypass run guard and trace auto logging, using direct `nightfall_motor_set()` + `HAL_Delay()` like `0/o`; removed unreachable legacy guarded `6`-`9` switch cases; rebuilt Debug-stm32f413 successfully |
| 20260506-073400 | 20260506-073400-f413-motor-channel-direction-fix | F413片側モータ回転方向の実機結果を反映 | n/a | yes | user observed UART `6`-`9` motor selection/direction mismatch; corrected F413 motor mapping to `TIM2_CH1=left`, `TIM2_CH3=right`, `L_DIR RESET=forward`, `R_DIR SET=forward` in `nightfall_motor_set()`, trace motor output sampling, and closed-loop `f413_ctrl_tick()` motor output; rebuilt Debug-stm32f413 successfully |
| 20260506-075903 | 20260506-075903-f413-mp6551-in1-in2-control | F413 MP6551モータドライバ入力制御を修正 | n/a | yes | checked `hardware/mini_r2_0/cad/HM_Nightfall-mini-2e_v1.sch`: `MOTOR_*_PWM` connects to MP6551 `IN1`, `MOTOR_*_DIR` connects to `IN2`, and `MOTOR_STBY` connects to `EN1/EN2`; fixed `nightfall_motor_set()` and closed-loop `f413_ctrl_tick()` to treat stop as `IN1=0, IN2=0` and use inverted PWM when driving the `IN2` direction, avoiding inactive motor full-drive; rebuilt Debug-stm32f413 successfully |
| 20260506-080359 | 20260506-080359-f413-motor-encoder-hw-pass | F413片側モータ+エンコーダ実機確認PASS | n/a | yes | user confirmed UART `6`-`9` now match expected motor selection/direction and encoder signs (`6` L fwd positive, `7` R fwd positive, `8` L rev negative, `9` R rev negative); fixed test log display for inactive motor `IN2=RESET`; next step is closed-loop checks with UART `1`/`2` |
| 20260506-081341 | 20260506-081341-f413-imu-manual-angle-test | F413手動旋回IMU角度テストを追加 | n/a | yes | added UART `I` command for manual IMU yaw verification without motor drive: configures ISM330DHCX gyro, samples stationary Z gyro offset, then logs `omega_z` and integrated `angle` for 8 seconds at 200ms print intervals; expected sign is CCW/left positive and CW/right negative |
| 20260506-082819 | 20260506-082819-f413-turn-angle-control-port | F413旋回角度制御を移植 | e25dc3e | yes | user confirmed IMU yaw direction/angle OK; committed and pushed user CAD updates separately as `e25dc3e Update mini CAD files`; ported F413 turn control toward F405 structure by adding angle-target API, angle outer loop, omega PID I/D state, signed angle wait for UART `3`/`4`/`5`, and using target angle for solver turns; rebuilt Debug-stm32f413 successfully |
| 20260506-083720 | 20260506-083720-f413-turn-button-start-delay | F413旋回テストをボタン始動+2秒静止待ちへ変更 | n/a | yes | changed UART `3`/`4`/`5` to arm the selected turn test instead of running immediately, so the UART cable can be disconnected before motion; button release now waits 2 seconds before `nightfall_test_run()`, allowing the IMU offset recalibration inside `f413_ctrl_start()` to happen after the robot settles |
| 20260506-084513 | 20260506-084513-f413-turn-gain-increase | F413旋回ゲインを実機結果に基づき増加 | n/a | yes | user confirmed turn direction/behavior look correct but gain was too low to complete 90deg; increased angle/omega gains and omega feed-forward, raised omega integral limit, and added minimum rotation PWM when commanded omega is above a small threshold to overcome static friction near the end of the turn; rebuilt Debug-stm32f413 successfully |
| 20260506-085323 | 20260506-085323-f413-straight-heading-hold-test | F413直進+方位保持テストを用意 | n/a | yes | user confirmed UART `3` can turn right 90deg though oscillatory and deferred detailed turn tuning; changed UART `1`/`2` straight tests to reset angle and enable angle target 0deg while running velocity control, changed `5` straight segments to the same heading-hold behavior, made motion tests `1`-`5` button-armed, and limited minimum rotation PWM to near-zero target velocity so straight heading correction does not force bang-bang minimum PWM; rebuilt Debug-stm32f413 successfully |
| 20260506-090136 | 20260506-090136-f413-last-test-csv-meta | F413直前テスト結果をCSVメタに追加 | n/a | yes | user confirmed UART `1` heading-hold straight test looked good and provided CSV; noted encoder columns stay near the 30000 center because the control loop recenters TIM3/TIM4 every tick, so added `#last_test_id`, `#last_test_status`, `#last_test_dist_mm`, and `#last_test_angle_deg` meta lines to `V` CSV output without changing the NVM trace record schema or CSV columns; rebuilt Debug-stm32f413 successfully |
| 20260506-091335 | 20260506-091335-f413-imu-accel-velocity-fusion | F413速度推定にIMU加速度補助を追加 | n/a | yes | user pointed out encoder distance works but 1kHz velocity is resolution-limited with 13.5mm wheel and 100ppr encoder; kept distance integration encoder-based, added forward accelerometer offset sampling and acceleration-integrated velocity corrected back toward encoder velocity with a short time constant for control feedback, and added UART `c/C` IMU accelerometer test that prints offset-compensated ax/ay/az and integrated vx/vy/vz for forward-axis/sign verification; rebuilt Debug-stm32f413 successfully |
| 20260506-092708 | 20260506-092708-f413-forward-accel-axis-y | F413前進加速度軸をY+へ修正 | n/a | yes | user confirmed IMU accelerometer Y+ is forward direction; UART `1` with previous X-axis velocity fusion still completed at `#last_test_dist_mm=300` and `#last_test_angle_deg=-2`; changed `F413_IMU_FORWARD_ACCEL_REG` from X to Y with positive sign and updated UART `c/C` guidance text; rebuilt Debug-stm32f413 successfully |
| 20260506-094701 | 20260506-094701-f413-path-code-step1 | F413 solver path実行をF405 path code体系へ近づけた | n/a | no | started F405 mini parity porting with a low-risk F413-side step: kept existing UART `z/j` solver path runner, added F405 shortest-run parameter lookup, capped requested speeds to the existing safe F413 path velocity, used diagonal half-cell distance for diagonal straight codes, and executed 45/V90/135/large-turn path codes with their parameterized angles instead of degenerating all non-90 codes to 90deg; rebuilt Debug-stm32f413 and Debug-stm32f405 successfully |
| 20260506-100000 | 20260506-100000-f413-path-heading-hold-step2 | F413 solver path実行の方位保持と符号付き旋回判定 | n/a | yes | continued F405 mini parity porting: enabled angle target 0deg during F413 solver path straight/diagonal segments and changed solver path turn wait from absolute-angle completion to signed-angle completion; `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check` succeeded |
| 20260507-193400 | 20260507-193400-f413-solver-host-sample-test | F413経路導出のホストサンプルテスト導線を追加 | n/a | no | added `tools/solver_host/run_solver_host.sh` and host shim sources to compile the shared F405/F413 `solver_build_path()` on macOS with F413 preorder params; supports internal open 16x16 sample and KeriLab-style C array files via `--maze-c-array`; verified internal mode2/case1, C-array mode2/case1, and internal case8 runs, then rebuilt Debug-stm32f413 and Debug-stm32f405 successfully |
| 20260507-194500 | 20260507-194500-f413-solver-host-kerilab-maze | KeriLab `.maze` 形式のホスト経路導出確認を追加 | n/a | no | extended `tools/solver_host/solver_host.c` to parse KeriLab text `.maze` files directly via `--maze`, added `tools/solver_host/run_kerilab_samples.sh` to fetch representative 32x32 maze files from `kerikun11/micromouse-maze-data` and run `solver_build_path()` against them, and added `tools/solver_host/README.md`; verified a 16x16 real sample directly plus the default 32x32 sample set, then ran `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405` successfully |
| 20260507-195400 | 20260507-195400-f413-turn-log-analysis-phase6 | F413 solver/path移植完了整理とターン評価解析を追加 | n/a | no | accepted small-turn and large-turn path support as the F413 solver/path migration completion point, explicitly deferred diagonal path conversion/running to a post-migration phase, and started Phase 6 by adding `tools/logging/analyze_turn_csv.py` for turn tuning metrics from trace CSV (`omega_z_mdps` integration, final angle error, overshoot, settling time, motor output summary); verified with a synthetic -90deg sample, then ran `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405` successfully |
| 20260507-200200 | 20260507-200200-f413-button-sensor-led-op-ui | F413にボタン・壁センサenter・LEDモード表示の操作UIを追加 | n/a | no | ported an F405-like operation flow to the F413 preorder main loop while keeping all UART commands: PUSH advances modes 0-7 on press edge, LED1-3 display the selected mode in binary, and a front-right wall response with front-left non-response is treated as enter after consecutive confirmations; current modes are idle, wall sensor test, S3, R90, L90, S3+R90+S3, search-entry, and shortest-entry; motion modes keep a 2s start delay and existing run guards; `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405` succeeded |
| 20260507-202400 | 20260507-202400-f413-op-ui-enter-threshold | F413操作UIのenter閾値を実測値に基づき調整 | n/a | yes | user confirmed mode1 works and measured front sensor delta as roughly 0-50 with no target and 1000+ with a wall; lowered the F413 operation UI front-right enter threshold from 300 to 150 and widened the front-left non-response threshold from 120 to 250 so a hand gesture can also trigger enter with margin, while keeping the 3-sample consecutive confirmation; `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405` succeeded |
| 20260507-203000 | 20260507-203000-f413-f405-compatible-op-mode-tree | F413操作UIをF405互換のモード/ケース体系へ拡張 | n/a | no | replaced the temporary F413 0-7 flat operation UI with an F405-compatible selection tree: top-level mode 0-9, mode1-7 case 0-9, and mode1-7 case0 sub 0-9; PUSH beep now uses the F405 `select_mode()` formula `(11-selected)*400`, and enter beep uses tone 900 as two short beeps; safe already-ported entries are wired to F413 wall/IMU tests, S3/R90/S6 motion tests, search entry, and shortest entry where applicable, while unported mode/case/sub selections print their intended F405 meaning and a no-op reason; `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405` succeeded |
| 20260507-205000 | 20260507-205000-f413-startup-sound-and-case0-tests | F413起動識別音とcase0テスト移植第一段を追加 | n/a | yes | user confirmed the F405-compatible operation UI works on hardware; added a distinct F413 startup buzzer pattern (3 rising tones, pause, 2 falling tones) instead of the F405 descending startup pattern; added a reusable low-speed path-code test runner and connected mode2-7 case0 sub0-9 to short closed-loop tests for normal 90, large 90, large 180, diagonal 45-in/out, V90, 135-in/out, slow straight, and faster straight with mode-dependent capped velocity; also wired mode9 case2 to encoder check and mode9 case4 to fan PWM check; preserved UART commands and run guards; `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405` succeeded |
| 20260507-213000 | 20260507-213000-f413-full-fram-log-v2 | F413 FRAMログを全テレメトリ保存形式へ拡張 | n/a | yes | changed trace log schema from v1 to v2 and expanded each FRAM record to include distance, angle, target/real velocity, accel-fused velocity, target/real omega, target angle, forward acceleration, encoder, signed motor outputs, wall/vbat ADC, operation mode/case/sub/test_id, flags, and reserved fields; run-start now formats FRAM and auto-captures the latest movement/test session without requiring a post-run hand-enter, and `mode9 case5` dumps the latest full CSV using `#log_format=nightfall_trace_csv_v2`; added F413 control getters for logging internals and updated host analyzers to accept both legacy `omega_z_mdps` and v2 `real_omega_mdps`; updated trace log spec; `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405` succeeded |
| 20260507-214800 | 20260507-214800-f413-trace-csv-long-line-fix | F413 v2ログCSVの長行切れとキャプチャ列数判定を修正 | n/a | yes | user reported that after `mode2 case0 sub8`, reset, and `mode9 case5`, the serial capture showed `#mm_columns` truncated and merged with the first data row, making the log look too short; root cause was `trace_printf()` using a 256-byte internal buffer while v2 `#mm_columns` and each 31-column CSV row exceeded it, plus `serial_capture_csv.py` still accepted only 8-column v1 rows; split F413 CSV header/data emission across multiple `trace_printf()` calls without intermediate newline, and changed the capture tool to derive expected CSV column count from `#mm_columns` while preserving `#log_format` and `#last_test_*` metadata; `git diff --check`, `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and Python py_compile succeeded |
| 20260509-051554 | 20260509-051554-f413-stlink-v3-minie-tools | ST-LINK V3 MINIEでF413を書き込み・UART通信できるホストツールを追加 | n/a | yes | added `tools/flashing/flash_stlink` using CubeProgrammer CLI over SWD and `tools/logging/serial_terminal.py` for ST-LINK VCP UART; updated monitor wrappers and README docs; verified connected `STLINK-V3MINIE` SN `003B00273234511537333934`, flashed `build/Debug/nightfall_stm32f413.elf` successfully with `mode=NORMAL freq=4000`, and confirmed UART response `WHO_AM_I=0x6B => PASS` via `/dev/cu.usbmodem112202` |
| 20260509-052433 | 20260509-052433-f413-autonomous-hardware-loop-policy | ST-LINK接続時のF413自律実機開発ループ運用を明文化 | n/a | yes | user authorized Cascade to repeat build, ST-LINK flash, software reset, UART command execution, log dump, and analysis when the F413 machine is connected; non-motor operations may run without user action, while motor-driving checks require notifying the user to lift and secure the machine first; updated `docs/NIGHTFALL_FW_DEV_POLICY.md` and persistent memory |
| 20260509-143300 | 20260509-143300-f413-stlink-straight-fram-log-check | ST-LINK経由でF413直進のみのFRAM v2ログを実機確認 | n/a | yes | user secured the machine lifted and explicitly prohibited turn-containing motion; added UART `n/N` as a direct straight-only S3 test entry so Cascade can start a straight run without physical PUSH, then committed `9c66c7d Add F413 direct straight UART test`, rebuilt clean, flashed via `python3 tools/flashing/flash_stlink --sn 003B00273234511537333934`, and captured `/dev/cu.usbmodem112202` with `python3 tools/logging/serial_capture_csv.py --show-noncsv --send n,V --send-interval-ms 8000`; only straight S3 was executed, no turn/solver/path turn motion; result: `#fw_git_sha=9c66c7d`, `#fw_git_dirty=0`, `#fw_log_schema=0x00020000`, `#last_test_status=OK`, `#last_test_dist_mm=274`, `#last_test_angle_deg=-0`, CSV saved as `tools/logging/logs/stm32_log_20260509_143309.csv`; v2 CSV had 31 columns, 88 rows, 0 bad-width rows, flags `0xB004` forward+angle-target+auto and `0x9008` coast+auto, and `analyze_trace_csv.py` reported 70 motor_forward + 18 motor_coast records with median period 11ms |
| 20260509-144600 | 20260509-144600-f413-uart-operation-ui-controls | F413通常操作系統をUARTから扱えるようにした | n/a | yes | user requested that captured logs be ignored and that Cascade should be able to operate the normal F405-compatible operation UI over commands instead of adding one-off test commands each time; added `tools/logging/logs/` to `.gitignore`; refactored the F413 operation UI so physical PUSH and UART share the same selection increment helper and busy guard; added UART `P` for PUSH increment, `E` for FR-wall enter, and `!` for software reset, while removing the temporary direct straight-only `n/N` command; `E` calls the same operation selection path used by the sensor enter, so mode/case/sub execution behavior stays aligned with the normal UI; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; committed `84df15a Add F413 UART operation UI controls`, flashed it via ST-LINK, confirmed `P,E,!` enters mode1 case selection then resets, and confirmed `PPPPPPPPPEPPPPPE` selects mode9 case5 and dumps the existing FRAM v2 CSV with `#fw_git_sha=84df15a`, `#fw_git_dirty=0`, 31 columns, and 88 stored rows without executing any motor-running mode |
| 20260509-145900 | 20260509-145900-f413-stlink-reset-between-ui-ops | ST-LINK software resetを挟んだF413 UART通常操作を確認 | n/a | yes | user pointed out that reset should preferably use ST-LINK software reset rather than a UART command so full initialization is exercised each time; verified `STM32_Programmer_CLI -c port=SWD freq=4000 mode=NORMAL reset=SWrst sn=003B00273234511537333934 -rst` performs software reset without reflashing; using the machine in lifted/fixed state, executed multiple normal UI command sequences separated by ST-LINK reset: `PPPPPPPPPEPE` selected mode9 case1 and confirmed `[HW-TEST][IMU] WHO_AM_I=0x6B expected=0x6B => PASS`; after another ST-LINK reset, `PPEEPPPPPPPPE` selected mode2 case0 sub8 straight slow and completed `[OP-UI][PATH-TEST] straight slow OK at code[1]=206 dist=271mm angle=0deg` without any turn selection; after another ST-LINK reset, `PPPPPPPPPEPPPPPE` selected mode9 case5 and dumped FRAM v2 CSV with `#fw_git_sha=84df15a`, `#fw_git_dirty=0`, `#fw_log_schema=0x00020000`, 31 columns, 82 data rows, and 0 bad-width-like rows; observed one `[TRACE-LOG] auto: FAIL(append NVM=3)` during the straight run (`NVM_STATUS_NOT_FOUND`), but the subsequent dump was readable and contained the straight run records, so this is noted for follow-up if it repeats |
| 20260509-150900 | 20260509-150900-f413-stlink-reset-only-workflow | F413 UART resetコマンドを廃止しST-LINK reset運用へ一本化 | n/a | yes | removed the UART `!` software reset command from F413 operation UI to avoid diverging from real reset behavior; updated help to show only `P=PUSH increment` and `E=FR enter`, with reset via ST-LINK software reset; added `tools/flashing/flash_stlink --reset-only` so Cascade can run a reset-only ST-LINK loop without reflashing; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and Python py_compile; committed `8b47a78 Use ST-LINK reset for F413 UART UI workflow`, pushed it, rebuilt clean, flashed it, and verified `python3 tools/flashing/flash_stlink --reset-only --sn 003B00273234511537333934` succeeds; after ST-LINK reset, `PPPPPPPPPEPPPPPE` still selects mode9 case5 and dumps FRAM v2 CSV using only `P/E`, with `#fw_git_sha=8b47a78`, `#fw_git_dirty=0`, `#fw_log_schema=0x00020000`, 31 columns, 82 rows, and no `!` help text |
| 20260509-152400 | 20260509-152400-f413-fram-v2-log-completion | F413 FRAM v2全種ログ拡張の残課題を潰して完了確認 | n/a | yes | audited the original FRAM v2 telemetry logging work and found the schema, firmware CSV output, mode9 case5 dump, docs, and host analyzers were implemented, but repeated real-hardware checks exposed one corrupted FRAM trace record around seq97 when logging during a 1kHz IMU-controlled straight run; root cause was a race on shared SPI2 between FRAM read/write and the TIM5 1kHz IMU control loop, because `nvm_wait_spi2_free()` only waited for an ongoing IMU read and did not prevent a new TIM5 IMU access from starting during a FRAM transaction; fixed F413 FRAM access by masking TIM5 IRQ only around the FRAM WREN/read/write SPI2 transaction, and hardened `serial_capture_csv.py` to drop suspicious v2 rows whose timestamp moves backward while `seq` is not reset to 0; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and Python py_compile; committed `c3a0421 Protect F413 FRAM trace log SPI transactions`, flashed it, ran lifted/fixed straight-only `mode2 case0 sub8` via `PPEEPPPPPPPPE`, observed `[TRACE-LOG] auto: STOP total=100 stored=100` and no append failure, then dumped via `mode9 case5` using `PPPPPPPPPEPPPPPE`; saved `tools/logging/logs/stm32_log_20260509_152329.csv` with `#fw_git_sha=c3a0421`, `#fw_git_dirty=0`, schema `0x00020000`, 31 columns, 100 rows, bad width 0, seq `0..99` with no missing values, suspicious rows 0, and `analyze_trace_csv.py` reported 95 motor_forward + 5 motor_coast records with median period 11ms |
| 20260509-162800 | 20260509-162800-f413-wall-sensor-snapshot-api | F413壁判断/壁切れ移植の前段として壁センサsnapshot APIを追加 | n/a | yes | user asked to proceed through the organized work toward F405-equivalent maze running; first added the ordered checklist to `docs/NIGHTFALL_FW_2026_DEVELOPMENT_PLAN.md` and committed `4098bf6 Plan F413 maze run migration steps`; then started Step1 by adding `nightfall_wall_sensor_snapshot_t` and `nightfall_wall_sensor_read_snapshot()` in F413 `main.c` to read all wall ADC channels with emitters off/on, compute deltas, detect front/right/left walls using `WALL_BASE_*`, and flag ADC saturation; refactored operation UI enter to use this shared snapshot instead of a separate front-only read; enhanced UART `w` wall test to print off/on/delta/detect fields; verified `git diff --check -- main.c`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; flashed F413 via ST-LINK and ran non-motor UART `w`, confirming wall-free output `delta: R=43 L=56 FR=21 FL=58` and `detect: front=0 right=0 left=0 sat=0 thr FR=160 FL=160 R=200 L=200`; this is only the observation/API foundation, not yet wall-end or wall-control integration |
| 20260509-164200 | 20260509-164200-f413-wall-end-monitor-api | F413壁切れ検出APIと非モータ確認用Wモニタを追加 | n/a | yes | added a F413 wall-end state machine on top of `nightfall_wall_sensor_read_snapshot()`: `nightfall_wall_end_reset_from_snapshot()` initializes hysteresis state from current side-wall deltas, and `nightfall_wall_end_update()` detects right/left wall falling edges using `WALL_END_THR_*` hysteresis plus derivative fall count, records `f413_ctrl_get_distance()` for future run integration, and supports an explicit gate flag; added non-motor UART `W` wall-end monitor that samples every 50ms for 4s and prints `R/L`, derivatives, wall states, and end flags; did not connect this to straight-run control or exploration yet; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; flashed F413 and ran UART `W`, confirming the monitor completed with wall-free readings around `R=13..22`, `L=18..26`, `wallR=0`, `wallL=0`, `endR=0`, `endL=0`, and final `done endR=0 distR=0 endL=0 distL=0`, so the monitor path works and does not false-trigger in open air |
| 20260509-165000 | 20260509-165000-f413-wall-end-hand-check | F413壁切れモニタで左壁あり→なしの実検出を確認 | n/a | yes | after committing the wall-end monitor API, ran a second non-motor UART `W` check after ST-LINK reset while the user placed and removed a hand/wall at the side sensor; left side delta stayed above the high threshold while covered (`L=646..750`, `wallL=1`), then dropped sharply at `t=1267ms` from `L=750` to `L=21` with `dL=-729`, causing `wallL=0` and `endL=1`; the final line was `done endR=0 distR=0 endL=1 distL=0`; this verifies the F413 wall-end state machine can detect an actual left wall falling edge in a non-motor observation test; right-side event and moving straight-run integration are still pending |
| 20260509-165200 | 20260509-165200-f413-wall-trace-observe | F413走行ログへ壁delta/壁切れ状態の観測接続を追加 | n/a | yes | connected the existing F413 wall snapshot and wall-end state machine to FRAM trace logging without changing schema v2: `reserved_i32_0..3` now carry wall deltas `FR/R/FL/L`, `reserved_u16_0` carries wall trace flags, and `reserved_u16_1` packs right/left wall-end distances as `distance_mm/4`; added `NIGHTFALL_F413_DISABLE_WALL_TRACE_OBSERVE` so the feature can be disabled at compile time if it interferes with later checks; CSV dump now emits `#wall_trace_observe=1` and field interpretation metadata, and `serial_capture_csv.py` preserves `#wall_trace_*` metadata in saved CSVs; updated `docs/NVM_TRACE_LOG_FORMAT_SPEC.md` and the development plan; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `python3 -m py_compile tools/logging/serial_capture_csv.py`; flashed F413 and ran non-motor `q,r,V`, saved `tools/logging/logs/stm32_log_20260509_165137.csv`, confirming the saved CSV includes the wall trace metadata and one sample row with wall-free deltas `reserved_i32_0..3=20,15,32,21`, `reserved_u16_0=32768` (`0x8000` observe enabled only), and analyzer compatibility still works |
| 20260509-170000 | 20260509-170000-f413-wall-control-angle-target | F413壁制御を弱い角度目標補正として直進制御へ接続 | n/a | yes | implemented F413 wall control as a conservative overlay on the existing angle-target heading hold instead of changing the 1kHz motor control loop: wall snapshot updates during trace auto-step compute a side-wall error from right/left deltas using F405-style both-wall/one-wall cases, LPF it, limit it to ±3deg, slew-limit it by 0.25deg per update, and `nightfall_wall_control_apply()` writes it through `f413_ctrl_set_angle_target()` only while straight motor-forward gate is active; added `NIGHTFALL_F413_DISABLE_WALL_CONTROL` so it can be disabled independently after migration; logged wall-control active as `reserved_u16_0` bit9; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; flashed F413 and ran non-motor `q,r,V`, confirming CSV metadata and sample logging still work, with wall control inactive as expected outside straight-run gate (`reserved_u16_0=32768`); moving straight-run wall-control behavior remains for later tuning and can be disabled with the compile-time flag if needed |
| 20260509-171000 | 20260509-171000-f413-search-map-probe | F413実探索前段の壁snapshot→map保存プローブを追加 | n/a | yes | added the first F413 real-search plumbing before enabling an autonomous exploration run: included F405-compatible `search.h` globals from the existing F413 solver bridge, added helpers to initialize an empty 32x32 `map[][]`, convert `nightfall_wall_sensor_read_snapshot()` wall booleans to F405-style relative `wall_info` bits, write the current cell and neighbor consistency bits into `map[][]`, and save/load-verify all 1024 cells through FRAM; changed F413 `store_map_in_eeprom()` from a stub to `nvm_maze_save_map(&map[0][0], MAZE_SIZE*MAZE_SIZE)`; added non-motor UART `O=search-map`; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; flashed F413 and ran UART `O`, confirming `[SEARCH-MAP] PASS start=(0,0,0) wall_info=0x0000 cell=0x0077 dFR=23 dR=77 dFL=86 dL=80`; this validates the wall observation to map persistence path without moving motors, but does not yet run the exploration loop |
| 20260509-171200 | 20260509-171200-f413-virtual-explore-sim | 仮想壁入力でF413探索移植を検証する導線を追加 | n/a | yes | user noted that running the real maze with the communication cable attached is difficult, so exploration migration should proceed using virtual wall inputs until the port is complete; extended `tools/solver_host/run_solver_host.sh` / `solver_host.c` with `--explore-sim`, which feeds front/right/left virtual wall observations from a loaded maze into the same F405-style `map[][]` representation, updates neighbor wall consistency, builds a goal step map, chooses the next relative move with forward/right/left/back priority, and virtually moves until the goal; verified internal open 16x16 sample reaches the goal in 30 steps, KeriLab `16MM2019CX.maze` in 82 steps, `32MM2019HX.maze` in 506 steps, `32MM2023HX.maze` in 242 steps, and `32_test_01.maze` in 144 steps; added F413 non-motor UART `G=search-preview`, which reads the real wall snapshot at the start cell, updates `map[][]`, computes the next direction to the goal, and saves the map without motor motion; flashed F413 and ran UART `G`, confirming `[SEARCH-PREVIEW] next=forward(0) smap=30 save=OK wall_info=0x0000 cell=0x0077 dFR=22 dR=18 dFL=31 dL=21`; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405` |
| 20260509-172300 | 20260509-172300-f413-search-forward-step | F413探索1ステップ直進入口を追加 | n/a | yes | added UART `N=search-step-fwd` as the first low-risk execution bridge from search decision to motion: it reads the current wall snapshot, initializes and updates the F405-compatible `map[][]`, builds the goal step map, chooses the next relative move, saves the map, and only if the next move is forward runs a 90mm straight segment at 150mm/s with angle target and FRAM trace logging; if the next move is right/left/back, it prints `preview-only` and does not move, keeping turn exploration deferred; kept `H` as the existing help command to avoid command conflict; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`; flashed F413 and ran non-motor UART `G` only, confirming `[SEARCH-PREVIEW] next=forward(0) smap=30 save=OK wall_info=0x0000 cell=0x0077 dFR=23 dR=17 dFL=42 dL=67`; did not execute UART `N` because it moves motors and real maze/running confirmation is deferred until the migration is complete or the machine is explicitly prepared for a straight-only check |
| 20260509-173000 | 20260509-173000-f413-search-step-turns | F413探索1ステップを直進/旋回対応の状態保持型へ拡張 | n/a | yes | advanced the exploration migration toward real-maze checking by changing UART `N` from a start-cell forward-only probe into a stateful one-action search step: the first `N` initializes `map[][]` and `mouse=(START_X,START_Y,0)`, later `N` commands keep `mouse.x/y/dir` and the explored map, read the wall snapshot at the current cell, update `map[][]`, rebuild the goal step map, choose the next relative move, save the map to FRAM, then execute exactly one action; forward runs 90mm at 150mm/s with heading hold, right/left/back run in-place angle-target turns of -90/+90/+180deg using the existing F413 angle control; on success the code updates `mouse` position or direction, and UART `B=search-reset` resets the step session; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and host `--explore-sim --max-steps 512`; flashed F413 and ran non-motor UART `G`, confirming `[SEARCH-PREVIEW] next=forward(0) smap=30 save=OK wall_info=0x0000 cell=0x0077 dFR=23 dR=21 dFL=122 dL=149`; did not run UART `N` yet because it can move/turn motors and should be tested only after the machine is prepared for controlled motion or in the real-maze confirmation flow |
| 20260509-175300 | 20260509-175300-f413-search-map-debug-uart | F413探索mapの非走行表示/clear/dumpを追加 | n/a | yes | user asked to continue migration tasks that do not require a real maze or running; added non-motor UART commands for search debug: `[` prints current search-step session state, `mouse.x/y/dir`, current cell, visited count, current known-cell count, FRAM map load status, FRAM known-cell count, and RAM/FRAM mismatch count; `]` initializes an empty F405-compatible search map, resets the search-step session, and saves the empty boundary/start map to FRAM; `@` dumps the FRAM map as 32 rows of compact 2-digit hex cells, falling back to RAM if FRAM load fails; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and host `--explore-sim --max-steps 512`; flashed F413 and sent `[][@`, confirming state before clear had FRAM load OK but RAM/FRAM mismatch, clear saved OK with `start_cell=0x00F7`, state after clear had `visited=1 known=124 fram=OK fram_known=124 mismatch=0`, and `@` dumped the expected empty 32x32 boundary map |
| 20260509-181000 | 20260509-181000-f413-op-mode9-nonmotor-maintenance | F413操作UI mode9の非走行メンテナンスcaseを接続 | n/a | yes | continued migration tasks that do not require real-maze running by wiring additional F405-compatible operation UI maintenance cases on F413: `mode9 case6` now runs the wall ADC/threshold check, `case7` prints a non-destructive NVM status summary instead of comparing fixed test patterns, `case8` prints identity block and MCU UID, and `case9` prints loaded/default sensor params but intentionally does not save or overwrite real sensor parameters; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and host `--explore-sim --max-steps 512`; flashed F413 and exercised the OP UI with UART `P/E`, confirming case6 wall readings and thresholds, case7 `[NVM-STATUS] distance=OK sensor=OK maze=OK ... trace=OK`, case8 identity output, and case9 sensor parameter output with the explicit no-save message |
| 20260509-190200 | 20260509-190200-f413-search-dump-render-tool | F413探索map dumpをASCII迷路へ変換するPCツールを追加 | n/a | yes | added `tools/logging/render_search_dump.py` to parse F413 UART `@` output lines like `[SEARCH-DUMP] y=..:<hex>` and render the FRAM/RAM search map as an ASCII maze; the tool interprets low bits as `W/S/E/N = 1/2/4/8`, prints summary counters for wall cells, missing boundary walls, and neighbor wall mismatches, marks start and goal cells by default, and offers `--summary-only`, `--no-mark`, and `--strict-consistency`; the normal consistency mode intentionally allows the F405-compatible start-cell one-way east forced wall, while strict mode reports it; documented usage in `tools/logging/README.md`; verified `python3 -m py_compile`, parsed an artificial dump, captured real F413 UART `@` output, and confirmed summary `source=FRAM size=32 wall_cells=124 boundary_errors=0 mismatches=0` with strict mode reporting the expected single start-cell mismatch |
| 20260509-194400 | 20260509-194400-solver-host-search-dump-input | F413 `@` dumpをsolver_hostの最短経路生成入力に追加 | n/a | yes | continued only non-motor migration before motor tuning or real-maze running by extending `tools/solver_host/solver_host.c` with `--search-dump <log>`; the parser reads F413 UART `[SEARCH-DUMP]` header and `y=..` rows, converts each dumped byte through `(cell >> 4) & 0x0F`, and feeds the existing F405-compatible `solver_build_path()` pipeline through `load_map_from_eeprom()`; documented the flow in `tools/solver_host/README.md`; verified an artificial fully-open 32x32 dump succeeds with `path_codes=229:S29,300:S-R90,260:S60`, while the current real F413 empty FRAM dump correctly fails because unexplored `0xF0` cells are treated as walls for shortest-run generation; also verified `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405` |
| 20260509-195400 | 20260509-195400-f413-control-tuning-precheck-doc | F413制御ゲイン調整前チェックリストを作成 | n/a | yes | user said the machine is lifted/fixed and asked to complete remaining assistant-side work before control gain and motion tuning; added `docs/F413_CONTROL_TUNING_PRECHECK.md` as a focused precheck for the exact transition into tuning: PC build/host checks, ST-LINK flash, UART connection, NVM/identity/sensor params via OP UART `P/E`, non-motor wall/IMU/encoder checks, lifted single-motor direction checks `6..9`, trace-log workflow `q,x,V` and lifted `q,y,V`, suggested tuning entry order, abort conditions, and a result template; linked it from the F413 hardware bring-up checklist and development plan Step28; verified `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, Python py_compile for flashing/logging tools, `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405` |
| 20260510-000000 | 20260510-000000-f413-f405-control-cascade | F413制御をF405従来機相当のカスケード構成へ移植 | n/a | yes | user requested that F413 control be made functionally identical to the F405 traditional control before any gain tuning; replaced the previous simplified F413 translational FF+P control with a F405-style distance outer PID producing target velocity and velocity inner PID producing translational motor output, retained IMU-based angle outer PID and omega inner PID, added the same target-distance/real-distance/error/integral state structure, and changed wall-control integration from direct angle-target overwrite to a F405-style heading omega correction path; existing F413 public APIs and FRAM v2 logging getters remain compatible, so current OP UI/test/path entry points continue to work; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512` |
| 20260510-003000 | 20260510-003000-f413-control-loop-tune-mode | F413へ制御ループ単独調整モードを移植 | n/a | yes | ported the F405 inner-loop tuning concept to the F413 cascade controller and extended it to four axes: velocity, omega, distance, and angle; added `f413_ctrl_tune_start/stop/is_done/get_*` APIs and a 1kHz bypass path that drives step/triangle/trapezoid references for the selected loop while preserving F413 motor wiring/sign conventions; wired OP UI `mode9 case0 sub0..9` to representative tuning presets, added UART shortcuts for the same presets, and reused FRAM v2 auto logging so tuning runs save target/real velocity, target/real omega, distance, angle, motor outputs, and tune reference metadata in reserved fields; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512` |
| 20260510-153500 | 20260510-153500-f413-fixed-velocity-distance-tune | F413浮かせ固定でvelocity/distance初期ゲインを調整 | n/a | yes | user fixed the machine lifted and asked Cascade to tune only velocity and distance, leaving omega/angle for later floor tuning by the user; flashed `fa0b375` and captured velocity step 500 via UART `q,!,V` to `tools/logging/logs/stm32_log_20260510_152957.csv`, showing target 500mm/s, real max 517mm/s, mean error about 5mm/s, mean absolute error about 10mm/s, motor output max 147/1000, and no saturation, so velocity gains were kept unchanged (`KP_VELOCITY_FAN_OFF=2.0`, `KI_VELOCITY_FAN_OFF=0.02`); distance step 90 with the old `KP_DISTANCE_FAN_OFF=0.07` reached only 4mm in 792ms, then `KP_DISTANCE_FAN_OFF=1.60` reached 64mm, and final `KP_DISTANCE_FAN_OFF=3.00` reached 82mm in 792ms with target velocity max 264mm/s, real velocity max 243mm/s, motor output max 96/1000, and no saturation (`tools/logging/logs/stm32_log_20260510_153411.csv`); velocity was rechecked after the distance change in `stm32_log_20260510_153440.csv`, still tracking 500mm/s with real max 518mm/s, mean absolute error about 10mm/s, no saturation; adopted `KP_DISTANCE_FAN_OFF=3.00` as a conservative floor-test starting point while noting fixed-wheel no-load behavior differs from real running |
| 20260517-064923 | 20260517-064923-f413-tune-encoder-delta-debug | F413調整ログの左右エンコーダ差分を追加して右モータ疑いを切り分け | n/a | yes | user reported lifted/fixed `mode9 case0 sub1` should go straight but the machine rotated and the previous log looked like the right motor was stopped; first analysis of `tools/logging/logs/stm32_log_20260517_152002.csv` showed `motor_out_l` and `motor_out_r` were identical and non-zero, and UART single-motor checks indicated the right motor/encoder can move; changed F413 trace sampling so `encoder_l/r` store signed per-tick encoder deltas while `f413_ctrl_is_running()` and raw timer counters otherwise, without changing the FRAM v2 schema; verified `git diff --check` and `cmake --build --preset Debug-stm32f413`, flashed via ST-LINK, ran the same velocity trapezoid tuning, dumped CSV to `tools/logging/logs/stm32_log_20260517_154752.csv`, and analyzed 73 rows: motor outputs were equal on all rows, active target>=200 rows had no zero right-encoder samples, encoder deltas averaged L=3.77 ticks/tick and R=5.11 ticks/tick, so the right motor is not stopped; the fixed-air rotation is more consistent with open-loop/equal-output left-right mechanical speed mismatch during velocity-only tuning, because this mode sets target omega to zero but does not close a left/right wheel-speed balance loop |
| 20260517-070700 | 20260517-070700-f413-tune-cross-axis-control | F413個別ループ調整で直交軸制御を有効化 | n/a | yes | user corrected the interpretation: the right wheel was stronger and the machine curved left, and requested that loop-specific tune modes still keep the orthogonal control axis closed so translational tuning can run straight and rotational tuning can hold position; changed `f413_ctrl_tick()` tune branch so velocity/distance tune modes now keep angle outer loop plus omega inner loop active around target angle 0, while omega/angle tune modes no longer force `s_out_translation=0` and instead keep the velocity inner loop active with target velocity 0; this preserves the selected tune reference in the tuned axis while damping unintended yaw or translation; verified `git diff --check -- f413_control.c`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405` |
| 20260517-071400 | 20260517-071400-f413-tune-velocity-300 | F413速度調整モードset0を300mm/sへ低速化 | n/a | yes | user reported `mode9 case0 sub1` still almost spins in place at high speed and said 500mm/s is too fast for half-size tuning; changed F413 velocity tune set0 peak from 500mm/s to 300mm/s while keeping omega tune set0 at 500deg/s by splitting `f413_ctrl_tune_get_peak()` handling for velocity and omega; updated OP UI labels from `velocity step/trapezoid 500` to `velocity step/trapezoid 300`; intended targets are now `mode9 case0 sub0` velocity step 300 and `sub1` velocity trapezoid 300, while `sub8` remains velocity trapezoid 1000 |
| 20260517-163000 | 20260517-163000-f413-motor-output-plot-visibility | F413調整ログの左右モータ出力表示を見やすく修正 | n/a | yes | user reported the 300mm/s velocity trapezoid still rotates and wanted to confirm individual motor outputs because the web plot appeared to show only `motor_out_avg` while `motor_out_l/r` looked zero; direct parsing of `tools/logging/logs/stm32_log_20260517_162038.csv` showed raw CSV `motor_out_l/r` were already non-zero and equal on 72/73 rows, so the issue was visualization overlap rather than missing firmware logging; changed the web fixed viewer Motor subplot to draw `motor_out_avg` and `motor_out_diff` first with thin dotted/dashed lines, then draw `motor_out_l/r` as thicker top-layer lines, and changed the PlotJuggler tune layout curve order similarly; flashed current F413 firmware via ST-LINK, captured lifted/fixed UART `q,\" ,V` tuning log to `tools/logging/logs/stm32_log_20260517_162935.csv`, and confirmed through the web loader that `motor_out_l` and `motor_out_r` were non-zero on 72 rows with max 119 duty, while `motor_out_diff` stayed 0 because the controller commanded equal left/right outputs |
| 20260517-164800 | 20260517-164800-f413-motor-output-overlap-marker | Web Visualizerで完全一致した左右モータ出力を識別可能に修正 | n/a | yes | user showed the Web Visualizer screenshot where `motor_out_l` and `motor_out_r` appeared absent even though their legend entries existed; confirmed on `tools/logging/logs/uploaded/stm32_log_20260517_164513_e0fa4c05854b.csv` that both columns were present and non-zero on 72/73 rows, with min=-32, max=810, and `motor_out_l == motor_out_r` for all rows; changed the Motor subplot so `motor_out_l` is drawn as a thick line with open-circle markers, `motor_out_r` as a dashed line with x markers, and an explicit `motor_out_l/r overlap` note is appended to the right-side legend when all samples match; verified `python3 -m py_compile tools/logging/visualizer/web_visualizer.py`, `git diff --check`, and Plotly trace metadata for the uploaded CSV |
| 20260517-165600 | 20260517-165600-f413-real-omega-shadow-fix | F413の実角速度更新が0固定になる制御バグを修正 | n/a | yes | user pointed out that identical left/right motor output means yaw control was not acting and `real_omega_dps` being exactly zero is impossible; inspected the F413 IMU/control/logging path and found `f413_ctrl_tick()` declared an outer `omega_raw` initialized from `s_real_omega`, but the IMU read branch redeclared `float omega_raw`, shadowing the outer variable, so `s_real_omega = omega_raw` kept the previous value and stayed zero from startup; removed the inner redeclaration so the filtered gyro Z value updates `s_real_omega`, `real_omega_mdps`, angle integration, and the omega PID; verified `git diff --check` and `cmake --build --preset Debug-stm32f413`, flashed via ST-LINK, captured lifted/fixed `mode9 case0 sub1` UART `q,\" ,V` to `tools/logging/logs/stm32_log_20260517_165547.csv`, and confirmed 73 rows with `target_velocity_mm_s` max 300, `real_omega_dps` non-zero on 71 rows with range -0.476..0.496dps, `target_omega_dps` non-zero from angle hold up to 0.857dps, and `motor_out_diff` non-zero on 68 rows with range -8..0, proving yaw feedback now reaches the controller and motor outputs |
| 20260517-170900 | 20260517-170900-f413-omega-control-sign-fix | F413のomega補正符号を実機応答に合わせて修正 | n/a | yes | user reran `mode9 case0 sub1` and reported the machine no longer spins but snakes, then noted `target_omega` is non-zero during a straight run; explained that non-zero `target_omega` is expected for angle-hold straight control because the outer angle loop commands small yaw-rate corrections to keep `target_angle=0`, but analyzed `tools/logging/logs/stm32_log_20260517_170045.csv` and found the correction sign was wrong on F413: `angle_deg` rose to +27.7deg, angle hold commanded negative `target_omega` down to -751dps, yet `motor_out_l > motor_out_r` drove `real_omega` further positive and saturated left output at 1000; changed both F413 omega PID sites from `s_real_omega - omega_ref` to `omega_ref - s_real_omega` so the existing F413 motor mixer applies yaw correction in the measured direction; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, flashed via ST-LINK, captured lifted/fixed `mode9 case0 sub1` UART `q,\" ,V` to `tools/logging/logs/stm32_log_20260517_170807.csv`, and confirmed 73 rows with `target_velocity_mm_s` max 300, `angle_deg` limited to -0.015..0.0deg, `real_omega_dps` -0.205..0.195dps, `target_omega_dps` 0..1.06dps, and `motor_out_diff` 0..8 without saturation; also verified `cmake --build --preset Debug-stm32f405` |
| 20260517-172000 | 20260517-172000-f413-sub1-log-recheck | F413速度trapezoid再発報告のログを再確認 | n/a | yes | user reported `mode9 case0 sub1` again spun at high speed and asked to check logs; found newest CSVs `stm32_log_20260517_171426.csv`, `171544.csv`, and `171743.csv` were not run logs but exactly the 16 deterministic records generated by `nightfall_fill_trace_log_selftest_record()` (`op_mode=9`, `op_case=5`, `op_sub=0..15`, `test_id='T'`, timestamp 1000..1150ms, motor_out_l/r ±200..230), likely from trace selftest `k/K` or a selftest dump, so they cannot diagnose the reported spin; rebuilt and flashed clean `cfc344a` (`fw_git_dirty=0`), then captured a fresh `q,\" ,V` run to `tools/logging/logs/stm32_log_20260517_171934.csv`; fresh log is valid `mode9 case0 sub1` with 73 rows and `op=(9,0,1)`, `target_velocity_mm_s` max 300, `angle_deg` -0.023..0.006deg, `real_omega_dps` -0.232..0.246dps, `motor_out_diff` -1..3, and no saturation, so the high-speed spin was not reproduced in the captured clean-FW log; previous true spin log remains `170045` from old `ae8a552 dirty=1`, while fixed logs `170807` and `171934` are stable |
| 20260517-173100 | 20260517-173100-f413-omega-damping-sign-and-angle-clamp | F413速度trapezoid高速回転ログからomegaダンピング符号と角度外側出力制限を修正 | n/a | yes | user provided `tools/logging/logs/stm32_log_20260517_172351.csv` as a clearly abnormal `mode9 case0 sub1` log; analysis confirmed valid run log with `op=(9,0,1)` and `fw_git_sha=e958a88`, but it showed `angle_deg` growing to 3002.75deg, `real_omega_dps` saturating near 4586.9dps, and `motor_out_l=-1000`, `motor_out_r=1000`, while `target_omega_dps` ran away to -159003dps; one early record also had impossible ADC/motor/encoder values, likely a single SPI/FRAM record corruption, but the subsequent runaway was consistent; changed F413 omega inner-loop error in both tune and normal paths back to `s_real_omega - omega_ref` so positive measured omega creates a damping differential with the current F413 motor mixer, and added `f413_ctrl_clamp_angle_omega()` using existing `F413_CTRL_ANGLE_OMEGA_MAX` to bound angle outer-loop `s_target_omega` in tune angle, tune velocity/distance angle-hold, and normal control; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, flashed via ST-LINK, and dumped the post-fix run as `tools/logging/logs/stm32_log_20260517_173016.csv`, which stayed stable with `angle_deg` 0..0.016deg, `real_omega_dps` -0.102..0.109dps, `target_omega_dps` -1.087..0dps, `motor_out_l/r` max 122/120, and `motor_out_diff` max 8; noted separately that build-info metadata can remain stale across dirty-tree rebuilds and should be improved later |
| 20260517-174500 | 20260517-174500-f413-straight-tune-damping-and-build-info | F413速度trapezoid蛇行とfw_git_sha staleを修正 | n/a | yes | user reported high-speed spin was fixed but `mode9 case0 sub1` still snaked and appeared to require an excessive motor output difference; newest log `tools/logging/logs/stm32_log_20260517_173556.csv` had correct `fw_git_sha=6448d12` but showed severe straight-tune oscillation: `angle_deg` -61.175..29.692deg, `target_omega_dps` clamped at ±300dps, `real_omega_dps` -403.543..376.513dps, `motor_out_l` up to 1000 while `motor_out_r` stayed near -20..272, and `motor_out_diff` max 1020; encoder deltas were not consistent with a totally dead side, so the immediate software issue was excessive angle-hold authority during velocity/distance tune rather than proof of a failed motor driver; changed velocity/distance tune angle-hold only to use conservative straight-tune gains (`F413_CTRL_TUNE_STRAIGHT_KP/KI/KD_ANGLE = 4/0/0`) and a lower `F413_CTRL_TUNE_STRAIGHT_ANGLE_OMEGA_MAX=60dps`, while leaving normal angle control and angle tune on the existing 300dps clamp; fixed stale firmware metadata by changing CMake build-info generation to an always-run `build_info_<target>` target with `BYPRODUCTS`, relying on `gen_build_info.cmake` to write only the current content; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `Debug-stm32f405`; after ST-LINK flash, captured `tools/logging/logs/stm32_log_20260517_174406.csv`, which reported `fw_git_sha=6448d12`, `fw_git_dirty=1` as expected for the uncommitted build, and stayed stable with `angle_deg` 0..0.033deg, `target_omega_dps` -0.133..-0.001dps, `real_omega_dps` -0.390..0.463dps, `motor_out_l/r` max 134/133, and `motor_out_diff` max 2 |
| 20260517-181500 | 20260517-181500-trace-op-meta-and-tune-targets | F413ログヘッダに実行モードを追加し、調整ログの上位目標値表示を修正 | n/a | yes | user found the remaining floor snaking root cause was hardware: the left pinion gear slipped under floor load, explaining why lifted behavior looked normal; updated logging after that issue was resolved: F413 CSV dump now emits `op_mode/op_case/op_sub/op_*_name/op_label` metadata so the executed mode is visible from tools; tune-mode reserved fields now keep `primary_ref_x1000` and `axis`, but store `target_distance_x1000` and `target_angle_x1000` so velocity trapezoid logs expose the integrated position target and omega logs expose the integrated angle target; added `f413_ctrl_get_target_distance()` for logging; updated PlotJuggler/Web conversion to derive `target_distance_mm` and `distance_error_mm`, with a fallback that integrates velocity `tune_ref` for older logs, and updated the Web Visualizer summary to show the operation label; verified `git diff --check`, Python py_compile for logging tools, conversion of `stm32_log_20260517_174406.csv` with integrated `target_distance_mm`, and `cmake --build --preset Debug-stm32f413` / `Debug-stm32f405`; no ST-LINK flash performed in this step because F413 flashing is left to the user |
| 20260517-184500 | 20260517-184500-f413-trace-log-1ms-period | F413自動トレースログ周期を1msへ変更 | n/a | yes | user asked why log plots looked stair-stepped and requested 1ms sampling if possible to see noise; confirmed F413 auto trace logging was controlled by `g_trace_log_auto_period_ms = 10U`, so automatic run logs were sampled every 10ms; changed it to `1U` so new run logs start with `period=1 ms`; capacity impact is acceptable for short tune runs because the 655360-byte FRAM trace area stores about 7446 packed 88-byte records, i.e. about 7.4s at 1ms, while the current velocity trapezoid tune is much shorter; noted that lowercase `v` still dumps only the latest 256 records, so full 1ms logs should be dumped with uppercase `V`; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; no ST-LINK flash performed in this step |
| 20260519-195500 | 20260519-195500-f413-trace-ram-buffer | F413 1msログを走行中FRAM直書きからRAMバッファ方式へ変更 | n/a | yes | user reported that the newest 1ms logs had all values constant; root cause was the previous 1ms change still called `nvm_trace_log_append()` on every sample, and F413 FRAM access locks SPI2 and disables TIM5 IRQ while each record and header are synchronously written, so 1kHz control/IMU update could be starved by 1kHz logging; changed automatic trace logging so run-time sampling only fills a 2048-record RAM buffer at 1ms, and `nightfall_trace_log_auto_stop()` flushes the buffered samples to FRAM after the run stops; STOP now reports `buffered` and `overflow` to indicate whether the RAM buffer was sufficient; this avoids FRAM/SPI writes during the active control loop and should make 1ms logs reflect real 1kHz control updates; F413 RAM usage increased to 248864B / 320KB (75.95%), acceptable for the current short tune logs; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; no ST-LINK flash performed in this step |
| 20260519-203000 | 20260519-203000-f413-trace-target-distance-column | F413 trace CSVへ目標距離と実測距離を正式列として常時出力 | n/a | yes | user confirmed the 1ms RAM-buffered log looked good, then requested that `mode9 case0 sub1` logs include both target distance (integrated velocity reference) and measured encoder distance on the machine-generated log side, not only as a visualizer-derived value; promoted target distance into the FRAM trace record as `target_distance_x1000`, bumped `NVM_TRACE_LOG_SCHEMA_VERSION` to `0x00030000`, and changed F413 CSV metadata to `nightfall_trace_csv_v3`; `#mm_columns` now explicitly contains `target_distance_mm,distance_mm` so the firmware log itself always carries both values for every mode, while tune `reserved_i32_2` remains as a compatibility copy during tune logs; updated the trace format spec and adjusted `export_plotjuggler_csv.py` to avoid adding duplicate derived columns when firmware already provides `target_distance_mm`; verified Python py_compile, old-log conversion produces one `target_distance_mm` column with `distance_mm` and `distance_error_mm`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; no ST-LINK flash performed in this step |
| 20260519-205500 | 20260519-205500-serial-capture-split-trace-runs | CSV保存ツールが複数trace dumpを1ファイルへ連結する問題を修正 | n/a | yes | user reported that the latest `mode9 case0 sub5` distance tune logs looked wrong: the newest capture had very little data and the previous capture contained unrelated turn data; analyzed `tools/logging/logs/stm32_log_20260519_204732.csv` and found it was not a run log but the deterministic trace selftest (`op_case=5`, `test_id='T'`, 16 records), likely dumped after `k/K` or an equivalent selftest; analyzed `stm32_log_20260519_204135.csv` and found 333 rows consisting of three independent runs concatenated by the host capture tool: `sub1`, `sub3`, and `sub5`, each with `seq=0..110`; root cause on the host side was `tools/logging/serial_capture_csv.py` only starting a new local CSV when `timestamp_ms` moved backward, so multiple dumps from the same boot with increasing HAL ticks but reset `seq` were appended into one file; fixed capture splitting so `#log_format=` resets capture state and a `seq` reset to zero starts a new file, added unique filename suffixing for multiple captures in the same second, and preserved `#op_*`/`#tune_*` metadata; extracted the valid `sub5` portion to `tools/logging/logs/stm32_log_20260519_204135_sub5_extracted.csv` with 111 rows; verified `python3 -m py_compile tools/logging/serial_capture_csv.py`, `git diff --check -- tools/logging/serial_capture_csv.py`, and that the extracted CSV contains only `op_sub=5` with `target_distance_mm` and `distance_mm` |
| 20260519-210500 | 20260519-210500-f413-tune-targets | F413調整モードのset0目標値をゲイン調整向けに変更 | n/a | no | user requested tune target changes: distance loop tune distance to 270mm, omega loop tune angular velocity to 1000deg/s, and angle loop tune angle to 360deg; updated `f413_ctrl_tune_get_peak()` in `platform/stm32f413/HM_Nightfall_f413_preorder/Core/Src/f413_control.c` so distance axis set0/default is 270, angle axis set0/default is 360, and omega axis set0/default is 1000 while preserving existing set1/set2 values; this affects `mode9 case0 sub4/sub5` distance tune, `sub2/sub3` omega tune, and `sub6/sub7` angle tune for set0; verified `git diff --check -- platform/stm32f413/HM_Nightfall_f413_preorder/Core/Src/f413_control.c`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; no ST-LINK flash performed, and the existing uncommitted `params/f413_preorder/params.h` gain-tuning edits were intentionally left separate |
| 20260519-213500 | 20260519-213500-f413-outer-tune-integrated-targets | F413外側ループ調整の目標を内側速度/角速度プロファイルの積分に修正 | n/a | yes | user reported that outer-loop tune target plots were inappropriate because distance/angle targets should match the integral of the corresponding inner-loop velocity/omega tune profile, not a direct distance/angle trapezoid; changed F413 tune profile generation so velocity/distance axes share the velocity profile peak and distance target, omega/angle axes share the omega profile peak and angle target, and outer-loop distance/angle targets are generated by integrating the virtual rate reference at 1kHz; fixed `mode255 unknown` CSV metadata by reconstructing OP metadata from stored trace records during dump, and added a Web Visualizer fallback so older CSVs with `mode255` headers use row-level `op_mode/op_case/op_sub`; enlarged trace printf buffer to 512 bytes and increased UART trace transmit timeout to avoid CSV/meta truncation during full dumps; flashed F413 via ST-LINK and verified floating fixed `mode9 case0 sub5` and `sub7` logs: `stm32_log_20260519_213102.csv` shows `op=(9,0,5)` and distance target from 0 to 270.002mm with derivative ramp-up/down; `stm32_log_20260519_213135.csv` shows `op=(9,0,7)` and angle target from 0 to 359.9deg with derivative ramp-up/down; `mode255` metadata no longer appears in new captures; verified `git diff --check`, Python py_compile, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; existing uncommitted `params/f413_preorder/params.h` gain-tuning edits were intentionally left separate |
| 20260519-215500 | 20260519-215500-f413-swo-and-binary-trace-dump | F413 trace通信高速化としてSWO試行とバイナリdumpを追加 | n/a | partial | user asked to prioritize communication setup because UART CSV dumps were becoming slow/unstable; added CMake option `NIGHTFALL_TRACE_F413_USE_UART` and preset `Debug-stm32f413-swo` so F413 trace output can be built for SWO/ITM while the normal `Debug-stm32f413` preset remains UART; strengthened the F413 SWO path in `platform/trace/trace.c` by explicitly configuring PB3 AF0 TRACE, enabling TPI async NRZ, ITM unlock, SWO enable, and a 1MHz SWO baud target; CubeProgrammer CLI `-swv freq=100 portnumber=0/all -RA` starts reception but captures no ITM data after UART `h`, so SWO is not yet usable from the current STLINK-V3MINIE setup and likely needs physical SWO wiring/probe-side confirmation; SWV attempts also sometimes leave ST-LINK USB in `DEV_USB_COMM_ERR`/`SN=-` until USB replug; independently implemented `</>` UART commands for binary trace dump (latest 256/all) using a compact `NFTB` frame containing binary trace header and records, plus `tools/logging/trace_bin_dump.py` to capture/decode raw binary dumps into normal CSV with existing 32 columns; verified binary transport on real F413 normal UART firmware: `python3 tools/logging/trace_bin_dump.py --send 'k>' --timeout 6` decoded 16 selftest records to `tools/logging/logs/trace_bin_20260519_215240.csv`, and `--send 'x>' --timeout 15` decoded a non-motor idle trace with 126 records to `trace_bin_20260519_215312.csv`; verified `cmake --build --preset Debug-stm32f413`, `Debug-stm32f413-swo`, `Debug-stm32f405`, and Python py_compile; final normal UART reflash is pending ST-LINK USB replug because the last SWV attempt left ST-LINK in USB communication error |
| 20260519-220800 | 20260519-220800-f413-uart-921600-trace | SWO不使用前提でF413 UART traceを921600bpsへ高速化 | n/a | yes | user inspected the board and found SWO is probably disconnected by damaged trace near an enlarged screw hole, so decided to avoid SWO until board repair and improve communication within UART only; changed F413 USART1 baud to a CMake-controlled `NIGHTFALL_F413_UART_BAUD_RATE`, default `921600`, with allowed fallbacks `115200/460800/921600/1000000`; host UART tools `serial_capture_csv.py`, `serial_terminal.py`, and `trace_bin_dump.py` now default to `NIGHTFALL_UART_BAUD` env var or `921600`, and support macOS custom baud via `IOSSIOSPEED` so 921600 works even though Python `termios` lacks `B921600`; flashed the 921600bps F413 firmware and verified real hardware UART binary dumps: `trace_bin_dump.py --send 'k>' --timeout 6` produced `trace_bin_20260519_220445.csv` with 16 selftest rows, and `--send 'x>' --timeout 8` produced `trace_bin_20260519_220458.csv` with 126 non-motor idle rows; also verified legacy CSV capture at 921600bps with `serial_capture_csv.py --send 'k,V'`, producing `stm32_log_20260519_220505.csv` with 16 rows and intact v3 metadata/columns; verified `git diff --check`, Python py_compile, `cmake --build --preset Debug-stm32f413`, `Debug-stm32f413-swo`, and `Debug-stm32f405`; practical default for tuning logs is now binary dump via `python3 tools/logging/trace_bin_dump.py --send 'q,<run>,>'` when a run command is safe for the fixture |
| 20260520-203500 | 20260520-203500-serial-capture-binary-autodecode | serial_capture_csv起動しっぱなし運用でbinary dumpを自動CSV化 | n/a | partial | user clarified that most log capture is done by starting `python3 tools/logging/serial_capture_csv.py` and then executing `mode9 case5` on the connected robot; changed `serial_capture_csv.py` so it scans the UART byte stream for `NFTB` binary trace frames, validates frame/header/record sizes and payload checksum using `trace_bin_dump.py`, then automatically writes both `trace_bin_YYYYMMDD_HHMMSS.raw` and decoded `.csv` into the normal log directory while keeping legacy CSV-line capture behavior; changed F413 OP UI `mode9 case5` from CSV all-record dump to binary all-record dump, and updated help/name text to `dump latest full log(bin)` / `dump latest full log bin`; this preserves the user's usual operation path while replacing the slow CSV transmission with compact binary; offline validation using an existing raw binary frame succeeded and produced raw+CSV with 16 decoded records; `python3 -m py_compile tools/logging/serial_capture_csv.py tools/logging/trace_bin_dump.py tools/logging/export_plotjuggler_csv.py`, `git diff --check`, `cmake --build --preset Debug-stm32f413`, `Debug-stm32f413-swo`, and `Debug-stm32f405` succeeded; real flash/test of the new `mode9 case5` binary OP path is pending because ST-LINK saw the probe but target voltage was `0.00V` and `Unable to get core ID`, so the robot needs power/contact restored before flashing |
| 20260520-212900 | 20260520-212900-f413-angle-omega-limit-1000 | F413角度外側ループの目標角速度上限を1000dpsへ変更 | n/a | yes | user observed in `mode9 case0 sub7` logs that `target_omega_mdps` was stuck at about 300000 even though omega tune could rotate at 500dps and the robot appeared to have margin; root cause was the angle outer loop clamp `F413_CTRL_ANGLE_OMEGA_MAX` limiting `s_target_omega` to 300deg/s after applying `ANGLE_FAN_OFF` gains; changed `F413_CTRL_ANGLE_OMEGA_MAX` in `platform/stm32f413/HM_Nightfall_f413_preorder/Core/Src/f413_control.c` from `300.0f` to `1000.0f`; this should allow angle tune sub6/sub7 and normal angle target control to command up to ±1000deg/s into the omega inner loop; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; MCU flashing was not performed because the user writes micromouse firmware, and the user's in-progress gain edits in `params/f413_preorder/params.h` were intentionally left uncommitted |
| 20260520-213500 | 20260520-213500-f413-angle-omega-limit-4000 | F413角度外側ループの目標角速度上限をIMU上限相当4000dpsへ変更 | n/a | yes | after raising the angle outer-loop clamp to 1000dps, user reported `target_omega_mdps` now saturated at about 1000000 while angle tracking mostly improved; per user request, changed `F413_CTRL_ANGLE_OMEGA_MAX` in `platform/stm32f413/HM_Nightfall_f413_preorder/Core/Src/f413_control.c` from `1000.0f` to `4000.0f` as a temporary IMU-limit-based ceiling, with the intent to later set a robot-performance-based value; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; MCU flashing was not performed because the user writes micromouse firmware, and the user's in-progress gain edits in `params/f413_preorder/params.h` were intentionally left uncommitted |
| 20260520-221200 | 20260520-221200-f413-fanoff-gain-tune-snapshot | F413 FAN_OFFゲイン調整値を保存 | n/a | yes | user reported angle tracking became reasonably good after increasing the angle outer-loop omega clamp, and requested saving the current tuning state to git/GitHub; saved the current `params/f413_preorder/params.h` FAN_OFF tuning snapshot: velocity `KP=2.5, KI=0.0, KD=0.0`, distance `KP=90.0, KI=0.3, KD=0.0`, angle `KP=20.0, KI=0.1, KD=0.0`, omega `KP=1.2, KI=0.03, KD=2.2`; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; MCU flashing was not performed because the user writes micromouse firmware |
| 20260523-124700 | 20260523-124700-f413-outer-loop-ff | F413外側ループに理論内側目標FFを追加 | n/a | yes | user reported that inner velocity/omega loops now track well, but outer distance/angle loops need high gains that create oscillatory inner-loop targets; added outer-loop feed-forward gains to `params/f413_preorder/params.h`: `FF_DISTANCE_FAN_ON/OFF` and `FF_ANGLE_FAN_ON/OFF`, defaulting to `1.0F`; in F413 control, distance outer-loop target velocity is now `FF_DISTANCE * theoretical_velocity + distance_FB`, and angle tune target omega is `FF_ANGLE * theoretical_omega + angle_FB`; in normal running, the existing `omega_ref = s_omega_interrupt + s_target_omega + heading_correction` composition is preserved by storing `(FF_ANGLE - 1) * s_omega_interrupt + angle_FB` in `s_target_omega`, so the final inner omega reference becomes `FF_ANGLE * theoretical_omega + angle_FB + heading_correction`; setting FF gains to `0.0F` restores complete FB-only outer-loop behavior, while `1.0F` makes the theoretical inner target the main command and leaves FB as error correction; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; MCU flashing was not performed because the user writes micromouse firmware |
| 20260523-131500 | 20260523-131500-f413-trace-log-1ms-tick-buffer | F413 trace logの制御値サンプリングをTIM5 1ms tickへ移動 | n/a | yes | user observed recent CSV `timestamp_ms` increments such as 15338, 15346, 15354, 15360, indicating the auto trace log effective sampling period was about 6-8ms instead of 1ms; root cause was that `nightfall_trace_log_auto_step()` was called from the main loop and each sample executed wall/ADC observation, including two `HAL_Delay(2U)` calls inside `nightfall_wall_sensor_read_snapshot()`, so the logger could not append at 1ms; changed F413 auto trace logging so TIM5 `HAL_TIM_PeriodElapsedCallback()` calls `nightfall_trace_log_auto_tick_sample()` immediately after `f413_ctrl_tick()`, appending lightweight control-state records to the existing RAM buffer at the control tick cadence; kept FRAM writes out of the interrupt path and still flush the RAM buffer on stop; moved heavy wall/ADC observation to a cached main-loop update used by the tick logger, so control columns should now have 1ms timestamps while wall/ADC columns reflect the latest cached observation; verified `git diff --check -- platform/stm32f413/HM_Nightfall_f413_preorder/Core/Src/main.c`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; MCU flashing was not performed because the user writes micromouse firmware |
| 20260607-045200 | 20260607-045200-f413-trace-log-stream-fram | F413 trace logを走行中にFRAMへ逐次転送 | n/a | yes | user pointed out that the previous auto trace log only used FRAM after a normal stop, so FRAM mainly preserved data after power-off but did not extend logging time or help analyze a crash/power loss during a run; changed F413 auto trace logging from a fixed 2048-record RAM-only staging buffer to a single-producer/single-consumer RAM ring queue streamed to FRAM from the main loop; TIM5 still only captures lightweight 1ms control records into RAM, while `nightfall_trace_log_auto_step()` first refreshes the wall/ADC observe cache on each run-loop call so wall-end and wall-control updates are not throttled, then transfers up to `NIGHTFALL_F413_TRACE_AUTO_FLUSH_RECORDS_PER_STEP=8` pending records per call to FRAM using cached NVM header state; added `nvm_trace_log_append_cached()` and `nvm_trace_log_commit_header()` so each append avoids rereading the header and the FRAM header is committed every `NIGHTFALL_F413_TRACE_AUTO_HEADER_COMMIT_RECORDS=8` records, balancing power-loss recoverability against SPI/TIM5 blocking; normal stop flushes all pending records and commits the header; wall/ADC observe cache refresh is intentionally not throttled because wall-end detection and wall-control tuning need high-rate sensor history; with the current 655360-byte FRAM trace area and 92-byte record size, stored capacity is about 7123 records, so 1ms logging retains about 7.1s of latest trace data instead of stopping at 2048ms; verified `git diff --check -- nvm/nvm_trace_log.c nvm/nvm_trace_log.h platform/stm32f413/HM_Nightfall_f413_preorder/Core/Src/main.c`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; MCU flashing was not performed because the user writes micromouse firmware |
| 20260607-052800 | 20260607-052800-f413-wall-sensor-async-dma | F413壁センサ取得をF405相当のTIM6+ADC DMA方式へ移植 | n/a | yes | user asked to make F413 wall sensor acquisition behave like F405 because wall/ADC values are needed at high rate for wall-end detection and tuning; added F413 async wall sensor state (`g_wall_sensor_adc_phase`, `g_wall_sensor_adc_inflight`, DMA off/on buffers, latest raw/off/corrected values, loaded wall offsets) and start it at boot before TIM5 control starts; TIM6 now runs the 8-phase IR/ADC scheduler (R/L off capture, R/L on capture, FR/FL off capture, FR/FL on capture) and starts ADC1 DMA scans instead of blocking in the main loop; `HAL_ADC_ConvCpltCallback()` computes F405-style corrected values `max(on - off - wall_offset, 0)` and updates latest `g_wall_adc_*`; `nightfall_wall_sensor_read_snapshot()` now returns the latest DMA snapshot without `HAL_Delay(2U)` or ADC reinitialization; run guard saturation checks, wall-end/wall-control observe update, manual wall tests, and 1ms TIM5 trace samples all read the latest DMA values; trace `adc_fr/r/fl/l` now contain corrected wall sensor intensity like F405 `ad_*`, while snapshot debug still exposes raw on/off and corrected delta; verified `git diff --check`, `cmake --build --preset Debug-stm32f413`, and `cmake --build --preset Debug-stm32f405`; MCU flashing was not performed because the user writes micromouse firmware |
| 20260607-053900 | 20260607-053900-web-visualizer-sensor-plots | Webログ可視化に壁センサ/電池ADCグラフを追加 | n/a | yes | user asked to add sensor value graphs to the log visualizer after F413 wall sensor acquisition was migrated to async DMA; updated `tools/logging/visualizer/web_visualizer.py` fixed FRAM trace plot groups to include `Wall Sensor [ADC]` for `adc_fr`, `adc_r`, `adc_fl`, `adc_l` and `Battery [ADC]` for `adc_vbat`; the existing available-column filtering keeps older CSVs compatible because groups are only shown when the relevant columns exist; verified `python3 -m py_compile tools/logging/visualizer/web_visualizer.py` and `git diff --check -- tools/logging/visualizer/web_visualizer.py` |
| 20260607-055800 | 20260607-055800-f413-wall-sensor-zero-fix | F413壁センサログが全0になる問題を修正 | yes | yes | user reported that the latest CSV had all wall sensor values at 0 and placed a wall so the right wall sensor should be high; investigated with ST-LINK memory reads because the UART port was held by the IDE plugin; confirmed the TIM6/ADC DMA sequencer itself was running (`ready_mask=0x03`) and raw/off/on buffers were updating, but runtime wall offsets had loaded the NVM self-test dummy blob (`base_l/r/f=1111/1222/1333`, `wall_offset_r/l/fr/fl=200/210/220/230`), so `max(on - off - offset, 0)` clipped all trace `adc_*` values to 0; changed F413 wall sensor startup to detect and ignore that self-test blob and fall back to default offsets, added `w` wall-test output for offsets/ready/phase/inflight, and changed F413 ADC scan settings toward the F405 acquisition setup (`ADC_EOC_SEQ_CONV`, 15-cycle sampling; `.ioc` sampling settings updated too); built and flashed F413 via ST-LINK, then verified via ST-LINK RAM read with the user's right-wall setup that offsets are 0, DMA ready is set, and right sensor reads nonzero/high (`r_off=9`, `r_on=513`, `r_delta=504`); verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check` |
| 20260607-061600 | 20260607-061600-f413-wall-sensor-module-split | F413壁センサ取得を専用モジュールへ分離 | yes | yes | started the F413 file-splitting/F405-equivalent migration roadmap requested by the user; saved the long-term roadmap to memory so later phases do not lose direction; extracted the F413 TIM6+ADC DMA wall sensor acquisition state and logic from the long `main.c` into new `Core/Inc/f413_wall_sensor.h` and `Core/Src/f413_wall_sensor.c`, including DMA buffers, latest off/on/corrected ADC values, NVM offset loading with self-test blob rejection, IR emitter sequencing, TIM6 tick handling, ADC conversion complete handling, raw ADC read API, snapshot API, and debug-state API; kept `main.c` behavior-compatible by leaving thin wrappers and delegating `HAL_ADC_ConvCpltCallback()` to `f413_wall_sensor_adc_complete()`; added the new source to `cmake/stm32cubemx/CMakeLists.txt`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; flashed F413 via ST-LINK without motor operation and confirmed RAM state after reset: `ready=0x03`, offsets all 0, sensor values updating (`r_delta=22`, `l_delta=91`, `fr_delta=40`, `fl_delta=80`, `vbat=2479`) |
| 20260607-062900 | 20260607-062900-f413-trace-auto-module-split | F413自動trace log管理を専用モジュールへ分離 | yes | yes | continued the F413 file-splitting roadmap; added `Core/Inc/f413_trace_log.h` and `Core/Src/f413_trace_log.c` and moved the auto trace log state machine out of `main.c`, including auto-enabled state, mode flags, RAM ring buffer, cached FRAM header, incremental FRAM flush, start/stop/step/tick sample handling, and abort API used before destructive trace format/selftest operations; kept machine-dependent sample generation and wall observation in `main.c` via `f413_trace_log_config()` callbacks so behavior remains equivalent while the storage/streaming responsibility is separated; `main.c` now delegates `nightfall_trace_log_auto_step()`, `nightfall_trace_log_auto_tick_sample()`, and mode flag updates to the new module; added the new source to `cmake/stm32cubemx/CMakeLists.txt`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; flashed F413 via ST-LINK without motor operation and verified RAM after boot without reset: trace auto stopped (`enabled=0`, `flags=0`, `head=0`, `tail=0`) and wall sensor scheduler still running (`ready=0x03`, `r/l/fr/fl=22/89/40/81`, `vbat=2478`, offsets all 0) |
| 20260607-064300 | 20260607-064300-f413-hw-helper-module-split | F413低レベルHW補助を専用モジュールへ分離 | yes | yes | continued Phase 1 file splitting by adding `Core/Inc/f413_hw.h` and `Core/Src/f413_hw.c`; moved low-level hardware helpers out of `main.c` without changing call-site behavior: all-LED set, mode LED display, buzzer beep, OP enter beep, boot buzzer pattern, stop switch raw/pressed read, encoder wrap-aware delta calculation, and open-loop motor driver set helper; kept the original `nightfall_*` helper names in `main.c` as thin wrappers so OP UI, test mode, run guard, and trace sampling behavior remain unchanged; trace switch flags now use `f413_hw_stop_switch_pressed()` instead of direct GPIO reads; added `f413_hw.c` to `cmake/stm32cubemx/CMakeLists.txt`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; flashed F413 via ST-LINK without motor operation and verified RAM/peripheral state after boot: trace auto stopped (`enabled=0`, `flags=0`, `head=0`, `tail=0`), wall sensor scheduler running (`ready=0x03`, `r/l/fr/fl=24/88/37/82`, `vbat=2479`, offsets all 0), and motor outputs disabled (`MOTOR_STBY_PB2=0`, `TIM2_CCER=0`, `CCR1=0`, `CCR3=0`) |
| 20260607-065700 | 20260607-065700-f413-diag-nvm-module-split | F413 NVM診断系を専用モジュールへ分離 | yes | yes | continued the post-HW helper split order by adding `Core/Inc/f413_diag.h` and `Core/Src/f413_diag.c`; moved the F413 NVM diagnostic/save-load verification helpers out of `main.c` without changing expected data or trace messages: distance params save/load, sensor params save/load and load-only compare, maze save/load and load-only compare, trace-log-area raw NVM save/load and load-only compare, and all-tests/load-only aggregate helpers; kept the original `nightfall_run_*_nvm_test()` and `nightfall_verify_*_nvm_load_only()` names in `main.c` as thin wrappers so UART command routing remains unchanged; removed now-local NVM diagnostic constants from `main.c`; added `f413_diag.c` to `cmake/stm32cubemx/CMakeLists.txt`; verified the moved code against the previous `main.c` implementation using `git show HEAD:.../main.c`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; flashed F413 via ST-LINK without motor operation and verified boot/RAM/peripheral state without running destructive NVM write diagnostics: trace auto stopped (`enabled=0`, `flags=0`, `head=0`, `tail=0`), wall scheduler running (`ready=0x03`, `r/l/fr/fl=22/88/40/80`, `vbat=2479`, offsets all 0), and motor disabled (`LDIR/RDIR/STBY=0/0/0`, `TIM2_CCER=0`, `CCR1=0`, `CCR3=0`) |
| 20260607-070900 | 20260607-070900-f413-hw-diag-module-split | F413低レベルHW診断を専用モジュールへ分離 | yes | yes | continued the requested sequence after NVM diagnostics by adding `Core/Inc/f413_hw_diag.h` and `Core/Src/f413_hw_diag.c`; moved low-level hardware diagnostic commands out of `main.c` while preserving trace messages and behavior: LED 30s test, switch raw test, buzzer PWM test, fan PWM duty sweep, short open-loop motor driver pulse test, and encoder measurement test; the new module uses `f413_hw` helpers for LEDs/switch/motor/encoder delta and directly uses TIM10/TIM11/TIM3/TIM4 handles for the same PWM/encoder behavior as before; kept `main.c` functions as thin wrappers so UART command routing and smoke-test aggregation remain unchanged; intentionally left wall sensor/IMU/smoke aggregation and test-run `6..9` single-motor encoder checks in `main.c` because they are more tightly coupled to wall/IMU/trace/test-run flow and are better handled in later trace/test/run split phases; added `f413_hw_diag.c` to `cmake/stm32cubemx/CMakeLists.txt`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; flashed F413 via ST-LINK, did not auto-run UART motor commands despite the robot being lifted, and verified safe boot state by RAM/peripheral read: trace auto stopped (`enabled=0`, `flags=0`, `head=0`, `tail=0`), wall scheduler running (`ready=0x03`, `r/l/fr/fl=9/25/19/28`, `vbat=2479`, offsets all 0), and motor disabled (`LDIR/RDIR/STBY=0/0/0`, `TIM2_CCER=0`, `CCR1=0`, `CCR3=0`) |
| 20260607-072400 | 20260607-072400-f413-trace-diag-module-split | F413 trace dump/診断を専用モジュールへ分離 | yes | yes | continued the sequence by adding `Core/Inc/f413_trace_diag.h` and `Core/Src/f413_trace_diag.c`; moved trace log diagnostic responsibilities out of `main.c`: trace format/header print, manual append sample, latest text dump, CSV dump latest/all, binary dump latest/all (`NFTB` frame), and FRAM trace selftest record generation/verification; kept machine/application-specific sample filling and CSV extra metadata in `main.c` through `f413_trace_diag_config()` callbacks (`fill_sample`, `get_context`, OP mode/case/sub names, and extra CSV metadata emitter), preserving existing CSV metadata including wall trace observe, tune metadata, identity metadata, and last-test metadata; removed binary frame/checksum and selftest helper implementations from `main.c`; added `f413_trace_diag.c` to `cmake/stm32cubemx/CMakeLists.txt`; verified no moved NVM trace calls remained in `main.c` via grep; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; flashed F413 via ST-LINK and verified safe boot state by RAM/peripheral read without running destructive trace format/selftest commands: trace auto stopped (`enabled=0`, `flags=0`, `head=0`, `tail=0`), wall scheduler running (`ready=0x03`, `phase=7`, `inflight=1`, `r/l/fr/fl=9/27/21/26`, `vbat=2478`, offsets all 0), and motor disabled (`LDIR/RDIR/STBY=0/0/0`, `TIM2_CCER=0`, `CCR1=0`, `CCR3=0`); note: `git status --short` timed out twice due I/O wait, so later commit step used explicit path staging |
| 20260607-073900 | 20260607-073900-f413-op-ui-name-module-split | F413 OP UI名解決/表示を専用モジュールへ分離 | yes | yes | started OP UI separation with the lowest-risk piece by adding `Core/Inc/f413_op_ui.h` and `Core/Src/f413_op_ui.c`; moved F405-compatible OP mode/case/sub/level name resolution and selection print formatting out of `main.c` without changing any strings or UART/FR/PUSH behavior; kept OP state, input polling, enter/push handling, and execution routing in `main.c` as thin wrappers/call sites because those remain coupled to wall sensor enter detection, run/test dispatch, trace context, and control sessions; `main.c` now delegates `nightfall_op_mode_name()`, `nightfall_op_level_name()`, `nightfall_op_case_name()`, `nightfall_op_sub_name()`, and `nightfall_op_print_selection()` to `f413_op_ui`; added `f413_op_ui.c` to `cmake/stm32cubemx/CMakeLists.txt`; fixed an intermediate mistaken replacement where prototype declarations were changed into definitions before the implementation block was replaced; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `git diff --check`, and grep that representative moved strings (`case 0U: return "idle"`, `velocity step 300`) no longer remain in `main.c`; flashed F413 via ST-LINK and verified safe boot state by RAM/peripheral read: trace auto stopped (`enabled=0`, `flags=0`, `head=0`, `tail=0`), wall scheduler running (`ready=0x03`, `phase=1`, `inflight=0`, `r/l/fr/fl=8/26/23/24`, `vbat=2481`, offsets all 0), and motor disabled (`LDIR/RDIR/STBY=0/0/0`, `TIM2_CCER=0`, `CCR1=0`, `CCR3=0`) |
| 20260607-075500 | 20260607-075500-f413-op-ui-state-module-split | F413 OP UI状態/入力処理を専用モジュールへ分離 | yes | yes | continued OP UI separation by extending `f413_op_ui.h/c` from pure name/print helpers into the OP UI state/input handler; moved OP state (`mode/case/sub/level`), selected value management, PUSH increment debounce/lockout, FR wall-enter streak/latch handling, UART `P/E` wrappers, selection beep/LED update, and top/case/sub enter transitions out of `main.c`; kept OP execution routing in `main.c` behind callbacks because run/test dispatch, trace context, and control sessions remain application-level responsibilities; added `f413_op_ui_config_t` callbacks for `can_accept_input`, `stop_switch_pressed`, `enter_sensor_active`, `execute_case`, and `execute_sub`; `main.c` now configures those callbacks at boot and delegates `nightfall_op_ui_step()`, `nightfall_op_uart_push_once()`, and `nightfall_op_uart_enter_once()` to `f413_op_ui`; removed obsolete OP UI state variables, OP UI timing/level/select constants, and now-unused LED/buzzer wrapper functions from `main.c`; fixed two intermediate edit issues: an `apply_patch` no-op when appending tail APIs to `f413_op_ui.c`, and a stale patch context while deleting `nightfall_buzzer_beep_ms`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `git diff --check`, and grep that `g_op_*` and old OP UI state/input helper names no longer remain in `main.c`; flashed F413 via ST-LINK and verified safe boot state without motor/NVM/trace destructive commands: trace auto stopped (`enabled=0`, `flags=0`, `head=0`, `tail=0`), wall scheduler running (`ready=0x03`, `phase=3`, `inflight=0`, `r/l/fr/fl=5/232/6/100`, `vbat=2481`, offsets all 0), and motor disabled (`LDIR/RDIR/STBY=0/0/0`, `TIM2_CCER=0`, `CCR1=0`, `CCR3=0`) |
| 20260607-081000 | 20260607-081000-f413-op-ui-routing-module-split | F413 OP UI実行分岐を専用モジュールへ分離 | yes | yes | continued OP UI separation by moving the OP `mode/case/sub` execution decision tree and OP no-op/execute trace messages into `f413_op_ui.c`; added `f413_op_ui_action_t` and changed `f413_op_ui_config_t` from direct `execute_case`/`execute_sub` callbacks to one `execute_action(action, mode, op_case, sub)` callback; `f413_op_ui` now decides actions such as search trace entry, shortest trace entry, test-run 1/2/3/5, IMU/encoder/wall/fan diagnostics, trace dump bin all, NVM/identity/sensor status, control tune sub, and path case0 sub; `main.c` now only maps those action IDs to application-level functions that still need trace context, HAL delays, run/test dispatch, and control/tune/session dependencies; direct UART shortcuts like `!` tune shortcuts, `z`, and `j` remain unchanged; fixed an intermediate F413 build failure caused by missing semicolons after generated `f413_op_ui_execute_case/sub` calls; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `git diff --check`, grep that old OP no-op strings and `nightfall_op_execute_case/sub` no longer remain in `main.c`, and grep that action dispatch lives in `f413_op_ui.c`; flashed F413 via ST-LINK and verified safe boot state without motor/NVM/trace destructive commands: trace auto stopped (`enabled=0`, `flags=0`, `head=0`, `tail=0`), wall scheduler running (`ready=0x03`, `phase=1`, `inflight=1`, `r/l/fr/fl=8/219/3/101`, `vbat=2480`, offsets all 0), and motor disabled (`LDIR/RDIR/STBY=0/0/0`, `TIM2_CCER=0`, `CCR1=0`, `CCR3=0`) |
| 20260607-082400 | 20260607-082400-f413-run-session-helper-split | F413 run guard/wait helperを専用モジュールへ分離 | yes | yes | started run/test/session separation from the lowest-risk common helper layer by adding `Core/Inc/f413_run_session.h` and `Core/Src/f413_run_session.c`; moved run abort reason enum, guard state, guard prepare/cleanup/check, guarded wait-with-trace-step loop, unguarded trace-step wait loop, abort reason text, and abort-to-trace-flag mapping out of `main.c`; kept machine-specific access in `main.c` via `f413_run_session_config()` callbacks for stop switch, left/right encoder counters, wall sensor OK (`read_snapshot` and saturation), IMU OK (`WHO_AM_I`), and trace auto step; `main.c` retains thin compatibility wrappers (`nightfall_run_guard_prepare`, `nightfall_trace_log_wait_with_auto_step_guarded`, etc.) so solver path, OP path tests, tune/test waits, and safe trace sessions remain behavior-compatible; removed old run guard and trace abort constants from `main.c`; added `f413_run_session.c` to `cmake/stm32cubemx/CMakeLists.txt`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `git diff --check`, grep that old `nightfall_run_guard_check`, `nightfall_abs_i32`, `NIGHTFALL_F413_RUN_GUARD_*`, and `NIGHTFALL_F413_TRACE_ABORT_*` no longer remain in `main.c`, and grep that `main.c` delegates to `f413_run_session`; flashed F413 via ST-LINK and verified safe boot state without motor/NVM/trace destructive commands: trace auto stopped (`enabled=0`, `flags=0`, `head=0`, `tail=0`), wall scheduler running (`ready=0x03`, `phase=3`, `inflight=0`, `r/l/fr/fl=2/14/1/74`, `vbat=2480`, offsets all 0), and motor disabled (`LDIR/RDIR/STBY=0/0/0`, `TIM2_CCER=0`, `CCR1=0`, `CCR3=0`) |
| 20260607-120800 | 20260607-120800-f413-idle-run-session-split | F413 idle trace sessionをrun_sessionへ分離 | yes | yes | continued run/test/session separation from the safest session body by moving only the non-motor idle trace session body into `f413_run_session.c`; extended `f413_run_session_config_t` with trace-session callbacks for `trace_auto_is_enabled`, `trace_on_run_start`, `trace_on_run_stop`, and `trace_set_mode_flags`; added `f413_run_session_run_idle_trace_once(duration_ms, idle_mode_flag)` which preserves the existing busy message, idle start/end messages, run-hook start/stop callbacks, idle mode flag setting, wait-with-auto-step, and flag clear behavior; kept `main.c` as a thin wrapper passing `NIGHTFALL_F413_RUN_SESSION_IDLE_MS` and `NIGHTFALL_F413_TRACE_MODE_IDLE_FLAG`, so UART `x/X` routing remains unchanged; intentionally did not move motor/search-safe/shortest-safe sessions yet because they still touch encoder start/stop and motor output; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `git diff --check`, grep that idle trace session strings no longer remain in `main.c`, and grep that the new callback API is declared in `f413_run_session.h`; flashed F413 via ST-LINK and verified safe boot state without motor/NVM/trace command execution: trace auto stopped (`enabled=0`, `flags=0`, `head=0`, `tail=0`), wall scheduler running (`ready=0x03`, `phase=7`, `inflight=0`, `r/l/fr/fl=8/39/4/77`, `vbat=2482`, offsets all 0), and motor disabled (`LDIR/RDIR/STBY=0/0/0`, `TIM2_CCER=0`, `CCR1=0`, `CCR3=0`) |
| 20260607-121600 | 20260607-121600-f413-motor-run-session-split | F413 motor trace sessionをrun_sessionへ分離 | yes | yes | continued run session body separation by adding explicit encoder/motor callback boundaries to `f413_run_session_config_t`: `encoder_stop_all`, `encoder_reset_all`, `encoder_start_l`, `encoder_start_r`, `encoder_stop_l`, `encoder_stop_r`, and `motor_set`; kept HAL TIM handles and GPIO/PWM details in `main.c` callback implementations so `f413_run_session` remains hardware-handle agnostic; moved the motor trace session sequence into `f413_run_session_run_motor_trace_once(motor_fwd_flag, motor_coast_flag, motor_rev_flag, motor_duty, pulse_ms, coast_ms)`, preserving the existing busy guard, start/end messages, encoder stop/reset/start/conditional stop flow, trace run-hook start/stop, mode flag sequence, forward/coast/reverse/coast motor commands, and wait-with-auto-step timing; `main.c` now only passes the existing F413 motor trace flags and `NIGHTFALL_F413_RUN_SESSION_MOTOR_*` constants; removed now-unused `nightfall_trace_log_wait_with_auto_step()` wrapper after F413 build warned it was unused; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `git diff --check`, grep that motor trace session strings no longer remain in `main.c`, and grep that the new implementation lives in `f413_run_session.c`; flashed F413 via ST-LINK and verified safe boot state without running the motor trace command: trace auto stopped (`enabled=0`, `flags=0`, `head=0`, `tail=0`), wall scheduler running (`ready=0x03`, `phase=5`, `inflight=0`, `r/l/fr/fl=8/35/6/78`, `vbat=2478`, offsets all 0), and motor disabled (`LDIR/RDIR/STBY=0/0/0`, `TIM2_CCER=0`, `CCR1=0`, `CCR3=0`) |
| 20260607-122200 | 20260607-122200-f413-search-safe-run-session-split | F413 search-safe trace sessionをrun_sessionへ分離 | yes | yes | continued run session body separation by moving the search-safe trace session sequence into `f413_run_session_run_search_safe_trace_once(search_safe_flag, motor_fwd_flag, motor_coast_flag, safe_duty, forward_ms, coast_ms, explore_steps)`; reused the previously added encoder/motor/trace callbacks and guard/wait helpers, preserving the existing busy guard, start-switch cancel, start/cancel/end/abort messages, encoder stop/reset/start/conditional stop flow, guard prepare/cleanup, run-hook start/stop, repeated low-speed forward/coast sequence, abort trace-flag sample, and final motor stop; `main.c` now only passes `NIGHTFALL_F413_TRACE_MODE_SEARCH_SAFE_FLAG`, motor forward/coast flags, and `NIGHTFALL_F413_RUN_SESSION_SAFE_*` constants; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `git diff --check`, grep that search-safe trace session strings no longer remain in `main.c`, and grep that the implementation lives in `f413_run_session.c`; flashed F413 via ST-LINK and verified safe boot state: trace auto stopped (`enabled=0`, `flags=0`, `head=0`, `tail=0`), wall scheduler running (`ready=0x03`, `phase=3`, `inflight=0`, `r/l/fr/fl=9/30/6/82`, `vbat=2483`, offsets all 0), and motor disabled (`LDIR/RDIR/STBY=0/0/0`, `TIM2_CCER=0`, `CCR1=0`, `CCR3=0`); did not execute UART `z/Z` because it normally enters solver path and may include turns rather than direct search-safe fallback |
| 20260607-122800 | 20260607-122800-f413-shortest-safe-run-session-split | F413 shortest-safe trace sessionをrun_sessionへ分離 | yes | yes | completed the current safe trace session body separation by moving the shortest-safe trace session sequence into `f413_run_session_run_shortest_safe_trace_once(shortest_safe_flag, motor_fwd_flag, motor_coast_flag, motor_rev_flag, safe_duty, forward_ms, turn_ms, coast_ms)`; reused existing encoder/motor/trace callbacks and guard/wait helpers, preserving busy guard, start-switch cancel, start/cancel/end/abort messages, encoder stop/reset/start/conditional stop flow, guard prepare/cleanup, run-hook start/stop, open-loop forward/coast/differential-turn/coast/forward sequence, abort trace-flag sample, and final motor stop; `main.c` now only passes `NIGHTFALL_F413_TRACE_MODE_SHORTEST_SAFE_FLAG`, motor forward/coast/rev flags, and `NIGHTFALL_F413_RUN_SESSION_SAFE_*` constants; noted that shortest-safe includes a left/right opposite motor command (`motor_set(true, true, false, ...)`) but does not use IMU angle feedback control; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `git diff --check`, grep that shortest-safe trace session strings no longer remain in `main.c`, and grep that the implementation lives in `f413_run_session.c`; flashed F413 via ST-LINK and verified safe boot state without executing shortest-safe/`j/J` because fixed-machine turn-equivalent motor commands should be treated carefully: trace auto stopped (`enabled=0`, `flags=0`, `head=0`, `tail=0`), wall scheduler running (`ready=0x03`, `phase=3`, `inflight=0`, `r/l/fr/fl=6/33/8/76`, `vbat=2483`, offsets all 0), and motor disabled (`LDIR/RDIR/STBY=0/0/0`, `TIM2_CCER=0`, `CCR1=0`, `CCR3=0`) |
| 20260607-144800 | 20260607-144800-codex-f413-hil-tune-readiness | Codex移行後のF413実機HILログ取得を確認 | yes | yes | on branch `codex/codex-primary-migration-docs` at firmware `fw_git_sha=cc146a1` / `fw_git_dirty=0`, verified the Codex HIL entry path with the connected F413 `mini_r2_0` over ST-LINK `003B00273234511537333934` and UART `/dev/cu.usbmodem112202` at 921600bps; built `Debug-stm32f413` and `Debug-stm32f405`, flashed `build/Debug/nightfall_stm32f413.elf`, captured boot/non-motor smoke logs, and then, with the machine lifted/fixed and straight translation explicitly allowed, ran only the straight-capable `mode9 case0` tune shortcuts `sub0/sub1/sub4/sub5/sub8`; skipped rotation/angle/omega subtests `sub2/sub3/sub6/sub7/sub9` because fixed-state rotation was outside the allowed scope; first `sub0` attempt aborted with IMU fault, then after ST-LINK reset reran `sub0` successfully and dumped complete CSV `tools/logging/logs/stm32_log_20260607_144654.csv`; previous successful full dumps were `stm32_log_20260607_143748.csv` (`sub1`), `stm32_log_20260607_143810.csv` (`sub4`), `stm32_log_20260607_143832.csv` (`sub5`), and `stm32_log_20260607_143854.csv` (`sub8`); all five final straight-capable tests reported `last_test_status=OK`, had intact v3 metadata, auto/tune/motor-forward flags, target/reference, real velocity, encoder delta, and motor output columns; noted that velocity trapezoid logs can include a few post-profile samples where `reserved_i32_0=0` but tune mode flags remain set, so tune-command analysis should prefer the command-active rows; added `AGENTS.md` Review guidelines and documented that Codex PR automatic review must be enabled in Codex settings while repository-specific review guidance lives in `AGENTS.md` |
| 20260607-185900 | 20260607-185900-codex-review-reset-capture-rc | Codex review指摘に基づきreset-capture失敗伝播を修正 | yes | yes | addressed automated Codex review feedback on PR #11: `tools/hil/f413_safe_hil.py reset-capture` now checks whether `serial_capture_csv.py` exits before reset or during the capture window and returns that nonzero capture status instead of reporting only the ST-LINK reset status; verified `python3 -m py_compile tools/hil/f413_safe_hil.py`; verified the failure path with missing UART `/dev/nightfall_missing_uart`, which returned `rc=2` before issuing ST-LINK reset; verified the normal path on F413 via `/dev/cu.usbmodem112202` and ST-LINK `003B00273234511537333934` with a short software reset capture, which returned `rc=0` and saved `tools/logging/logs/f413_boot_20260607_185842.log`; no motor/fan/NVM commands were run |
| 20260607-192400 | 20260607-192400-codex-review-timed-capture-rc | Codex review指摘に基づきtimed_capture早期終了を失敗扱いへ変更 | n/a | yes | addressed the second automated Codex review feedback on PR #11: `tools/hil/f413_safe_hil.py timed_capture()` now returns success only when the capture subprocess survives until the requested timeout window and is intentionally terminated by the wrapper; if the subprocess exits early, even with status 0, the helper now prints `ERROR: capture exited before duration` and returns nonzero so nonmotor-smoke/dump-trace cannot be recorded as a successful HIL capture after a premature UART/serial failure; verified `python3 -m py_compile tools/hil/f413_safe_hil.py`; verified host-only behavior by directly calling `timed_capture()` with early `exit(0)` expecting rc=1, early `exit(7)` expecting rc=7, and a long-running child expecting wrapper timeout rc=0; no live hardware command was run for this review fix |
| 20260607-193300 | 20260607-193300-codex-review-safe-capture-hardening | Codex再レビュー指摘に基づきsafe captureを強化 | n/a | yes | addressed three additional automated Codex review findings on PR #11: detached stdin for all `serial_capture_csv.py` child processes launched by `tools/hil/f413_safe_hil.py` so terminal input cannot be forwarded as accidental UART commands; changed safe helper smoke/dump commands from full `V` to bounded `v` to avoid truncating large full-FRAM trace dumps under the fixed capture window; made `reset-capture` treat early clean capture exits during the reset window as failures instead of returning only ST-LINK reset status; updated `tools/hil/README.md` and `docs/ai/HIL_SAFETY.md` to document bounded `v` for safe helper flows and reserve uppercase `V` for explicit full dumps with completion-marker-aware capture; verified `python3 -m py_compile tools/hil/f413_safe_hil.py`, `git diff --check`, and host-only mock coverage for `timed_capture()` early clean exit, early nonzero exit, timeout-driven termination, `reset-capture` pre-reset clean exit, reset-window clean exit, and reset-window survival; no live hardware command was run for this review fix |
| 20260607-204940 | 20260607-204940-f413-wall-runtime-module-split | F413壁終端/壁制御/壁観測trace runtimeを専用モジュールへ分離 | yes | yes | continued F413 `main.c` responsibility split by adding `Core/Inc/f413_wall_runtime.h` and `Core/Src/f413_wall_runtime.c`; moved wall-end state, wall trace flag construction, wall-control overlay state/update/apply, wall-observe trace field filling, and the non-motor `W` wall-end monitor body out of `main.c`; kept raw wall sensor acquisition in `f413_wall_sensor` and left `main.c` with only thin callbacks/configuration and application-level trace sample composition; added `f413_wall_runtime.c` to `cmake/stm32cubemx/CMakeLists.txt`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405` (existing F405 warnings only), `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, `python3 -m py_compile tools/flashing/flash_stlink tools/flashing/flash_uart tools/flashing/make_identity_block.py tools/hil/f413_safe_hil.py tools/logging/*.py`, and `git diff --check`; flashed F413 `build/Debug/nightfall_stm32f413.elf` via ST-LINK SN `003B00273234511537333934`, UART `/dev/cu.usbmodem111202`, and ran safe non-motor smoke `i,w,v`: IMU WHO_AM_I `0x6B` PASS, wall read PASS, bounded CSV saved as `tools/logging/logs/stm32_log_20260607_204859.csv` with `#wall_trace_observe=1`; then ran non-motor `W` monitor for 4s, confirming read samples completed with wall-free `wallR=0 wallL=0 endR=0 endL=0` and final `done endR=0 distR=0 endL=0 distL=0`; no motor/fan/NVM-destructive command was run for this split |
| 20260607-205654 | 20260607-205654-f413-nvm-diag-module-split | F413 NVM/identity診断表示を専用モジュールへ分離 | yes | yes | added `Core/Inc/f413_nvm_diag.h` and `Core/Src/f413_nvm_diag.c`; moved identity family naming, identity CSV metadata emission, identity status print, sensor parameter status print, NVM status print, and the NVM diagnostic/load-only wrapper calls out of `main.c`; removed now-unused maze/NVM status constants and direct `f413_diag`/`nvm_params` includes from `main.c`; added `f413_nvm_diag.c` to `cmake/stm32cubemx/CMakeLists.txt`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405` (existing F405 warnings only), `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, and `git diff --check`; flashed F413 via ST-LINK SN `003B00273234511537333934`, UART `/dev/cu.usbmodem111202`, and ran safe non-motor smoke `i,w,v`: IMU WHO_AM_I `0x6B` PASS, wall read PASS, bounded CSV saved as `tools/logging/logs/stm32_log_20260607_205624.csv` with identity metadata still emitted; then ran non-destructive UART `A` load-only verify to exercise the moved NVM diagnostic dispatch: distance/sensor load-only PASS, maze load and trace compare reported FAIL from current FRAM contents, so no NVM formatting/save/erase was run; no motor/fan/NVM-destructive command was run for this split |
| 20260607-210314 | 20260607-210314-f413-control-tune-run-module-split | F413制御チューニング実行部を専用モジュールへ分離 | yes | yes | continued F413 `main.c` responsibility split by adding `Core/Inc/f413_control_tune_run.h` and `Core/Src/f413_control_tune_run.c`; moved the control tune run body, tune axis/pattern names, last tune metadata, tune CSV metadata emission, timeout/abort handling, run guard use, trace mode flag updates, and test-result recording bridge out of `main.c`; kept OP shortcut/sub selection and trace context setting in `main.c`, which now configures the tune runner with callbacks for stop-switch, tick, trace auto start/stop/step/flags, and last-test result recording; added `f413_control_tune_run.c` to `cmake/stm32cubemx/CMakeLists.txt`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405` (existing F405 warnings only), `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, and `git diff --check`; flashed F413 via ST-LINK SN `003B00273234511537333934`, UART `/dev/cu.usbmodem111202`, and ran safe non-motor smoke `i,w,v` with IMU/wall PASS and bounded CSV `tools/logging/logs/stm32_log_20260607_210216.csv`; then, with the machine already confirmed lifted/fixed and motors allowed, ran the straight-capable tune shortcut `!` (`mode9 case0 sub0`, velocity step 300) followed by bounded `v`, confirming `[TUNE] OK`, `#op_mode=9`, `#op_sub=0`, `#tune_axis=velocity`, `#tune_pattern=step`, `#last_test_status=OK`, and CSV `tools/logging/logs/stm32_log_20260607_210254.csv`; no fan/NVM-destructive command was run for this split |
| 20260607-211122 | 20260607-211122-f413-trace-sample-module-split | F413 traceサンプル合成を専用モジュールへ分離 | yes | yes | continued F413 `main.c` responsibility split by adding `Core/Inc/f413_trace_flags.h`, `Core/Inc/f413_trace_sample.h`, and `Core/Src/f413_trace_sample.c`; moved trace flag definitions shared by run/test/tune paths, trace context state, wall/ADC observe cache, manual/control trace record filling, tune reserved-field filling, identity/last-test extra CSV metadata emission, and last-test result recording out of `main.c`; `main.c` now only configures the sample module with HAL tick, encoder counter, and wall raw-ADC callbacks, sets boot identity once after `nvm_identity_read()`, and passes `f413_trace_sample_*` callbacks to trace log, trace diag, search-step, test-run, and tune-run modules; main line count fell from 3169 to 2869; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405` (existing F405 warnings only), `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, and `git diff --check`; flashed F413 via ST-LINK SN `003B00273234511537333934`, UART `/dev/cu.usbmodem111202`, and ran safe non-motor smoke `i,w,v`: boot `fw_git_sha=c618e7d dirty=1`, IMU WHO_AM_I `0x6B` PASS, wall read PASS, and bounded CSV `tools/logging/logs/stm32_log_20260607_211049.csv` preserved identity and wall trace metadata; then ran non-motor idle trace `x,v`, confirming `[RUN-TEST] idle trace session end`, `auto: STOP total=1011 stored=1011 overflow=0 nvm_error=0`, CSV `tools/logging/logs/stm32_log_20260607_211107.csv`, `#op_mode=0`, `#op_test_id=120`, and wall/identity metadata emitted from the new module; no motor/fan/NVM-destructive command was run for this split |
| 20260607-212155 | 20260607-212155-f413-path-run-module-split | F413 solver/path実行部を専用モジュールへ分離 | yes | yes | continued F413 `main.c` responsibility split by adding `Core/Inc/f413_path_run.h` and `Core/Src/f413_path_run.c`; moved solver path preview, shortest-run mode/case parameter lookup, path velocity limiting, path-code turn decoding, solver path closed-loop execution, OP path-code sequence execution, and path test velocity helper out of `main.c`; kept `solver_build_path()` entry routing, OP sub/case selection, and module configuration in `main.c`; added `f413_path_run.c` to `cmake/stm32cubemx/CMakeLists.txt`; `main.c` line count fell from 2869 to 2267; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405` (existing F405 warnings only), `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, and `git diff --check`; flashed F413 via ST-LINK SN `003B00273234511537333934`, UART `/dev/cu.usbmodem111202`, and ran safe non-motor smoke `i,w,v`: IMU WHO_AM_I `0x6B` PASS, wall read PASS, bounded CSV `tools/logging/logs/stm32_log_20260607_212129.csv`, identity and wall trace metadata still emitted; did not execute `z/j` solver path or OP path-code motion for this split because those commands may drive motors and include turns, and the extraction was validated with build/flash/non-motor diagnostics only; no fan/NVM-destructive command was run |
| 20260607-212922 | 20260607-212922-f413-uart-cli-module-split | F413 UART CLI配送を専用モジュールへ分離 | yes | yes | continued F413 `main.c` responsibility split by adding `Core/Inc/f413_uart_cli.h` and `Core/Src/f413_uart_cli.c`; moved UART help text and command-character dispatch for NVM diagnostics, trace dumps, hardware diagnostics, OP UI UART controls, search-step diagnostics, test-run shortcuts, run-session entries, and tune shortcuts out of `main.c`; kept only the callbacks that require app-level sequencing or HAL-handle wiring in `main.c`; added `f413_uart_cli.c` to `cmake/stm32cubemx/CMakeLists.txt`; `main.c` line count fell from 2267 to 1854; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405` (existing F405 warnings only), `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, and `git diff --check`; flashed F413 via ST-LINK SN `003B00273234511537333934`, UART `/dev/cu.usbmodem111202`, and ran safe non-motor smoke through the split CLI path `i,w,v`: boot `fw_git_sha=282e90e dirty=1`, IMU WHO_AM_I `0x6B` PASS, wall read PASS, bounded CSV `tools/logging/logs/stm32_log_20260607_212908.csv`, identity and wall trace metadata still emitted; no motor/fan/NVM-destructive command was run |
| 20260615-124608 | 20260615-124608-f413-encoder-diag-window-flash | F413エンコーダ診断の測定時間を10秒へ変更して実機へ書き込み | yes | yes | changed `F413_HW_DIAG_ENCODER_WINDOW_MS` in `f413_hw_diag.c` from 3000ms to 10000ms so UART `e` gives enough time to rotate a wheel by hand for CPR confirmation; built and flashed dirty firmware from commit `5af17aa` with local modification via `python3 tools/flashing/flash_stlink --build --sn 003B00273234511537333934`; CubeProgrammer connected to ST-LINK V3 MINIE SN `003B00273234511537333934`, detected STM32F413/F423 at 3.34V, erased/programmed app sectors 0-5, verified download successfully, and performed software reset; no motor/fan/search/shortest/NVM-destructive UART command was run |
| 20260615-130506 | 20260615-130506-f413-encoder-cpr-200-flash | F413エンコーダ距離換算を実測200count/revへ修正して実機へ書き込み | yes | yes | after the user measured about 200 encoder counts per wheel revolution on both sides with UART `e`, and after checking the `satoshihamasuna/encoder_disk` `100pr_encoder_disk.kicad_mod` geometry as 100 alternating boundaries / 50 reflective cycles, changed `F413_CTRL_CPR_WHEEL` in `f413_control.c` from 400.0f to 200.0f and removed the stale CPR=400 feed-forward comment; this changes encoder conversion from about 0.10485mm/count to about 0.20970mm/count for the 13.35mm tire diameter; verified `cmake --build --preset Debug-stm32f413` and `git diff --check`; flashed dirty firmware from commit `5af17aa` with local modifications via `python3 tools/flashing/flash_stlink --build --sn 003B00273234511537333934`; CubeProgrammer verified download successfully and performed software reset; no motor/fan/search/shortest/NVM-destructive UART command was run |
| 20260615-134901 | 20260615-134901-f413-velocity-accel-comp-log-prep | F413速度遅れ補償の実測比較用ログを準備 | yes | yes | after PR #13 was merged to `main`, fast-forwarded the Codex worktree to `origin/main` merge commit `8a8e927` and prepared the first measurement-only implementation of encoder-delay compensation based on the Matsui mouse velocity measurement method; added `VELOCITY_ACCEL_COMP_WINDOW_MS` default 30ms, `VELOCITY_ACCEL_COMP_GAIN` default 1.0, and `VELOCITY_ACCEL_COMP_ENABLE_CONTROL` default 0 in `params/f413_preorder/params.h`; added a fixed ring-buffer moving average in `f413_control.c` that logs `accel_velocity_mm_s = encoder_velocity_avg + accel_avg * window / 2`, while keeping `real_velocity_mm_s` and the actual control feedback on the existing encoder LPF unless the compile-time control flag is explicitly enabled; CSV metadata now records the compensated velocity meaning, window, and control flag; PlotJuggler export and the web visualizer now include compensated velocity and compensated velocity error in the Velocity plot; verified `cmake --build --preset Debug-stm32f413`, `python3 -m py_compile tools/logging/export_plotjuggler_csv.py tools/logging/visualizer/web_visualizer.py tools/logging/trace_bin_dump.py`, and `git diff --check`; then flashed the app image built from commit `3129131` with `fw_git_dirty=0` via `python3 tools/flashing/flash_stlink --build --sn 003B00273234511537333934`; CubeProgrammer connected to ST-LINK V3 MINIE SN `003B00273234511537333934`, detected STM32F413/F423 at 3.24V, erased/programmed app sectors 0-5, verified download successfully, and performed software reset; no motor, fan, run, search, shortest, or NVM-destructive command was run, so the next step is lifted/fixed HIL log capture to compare `real_velocity_mm_s` against `accel_velocity_mm_s` |
| 20260615-140119 | 20260615-140119-f413-velocity-accel-comp-control-enable | F413速度遅れ補償を速度フィードバックへ有効化 | yes | yes | after reviewing the user's latest floor-run logs and seeing `accel_velocity_mm_s` reduce velocity error versus the encoder LPF estimate, changed `VELOCITY_ACCEL_COMP_ENABLE_CONTROL` in `params/f413_preorder/params.h` from 0 to 1 while keeping the 30ms window and gain 1.0; this makes `s_real_velocity` use the compensated velocity estimate for control while still logging the same compensated estimate in `accel_velocity_mm_s`; verified `cmake --build --preset Debug-stm32f413` and `git diff --check`; committed the control-enable change as `Enable F413 accel compensated velocity control`; flashed the app image via `python3 tools/flashing/flash_stlink --build --sn 003B00273234511537333934`; CubeProgrammer connected to ST-LINK V3 MINIE SN `003B00273234511537333934`, detected STM32F413/F423 at 3.25V, erased/programmed app sectors 0-5, verified download successfully, and performed software reset; no motor, fan, run, search, shortest, or NVM-destructive command was run by Codex; next HIL step should be an on-floor short straight tune run, preferably OP `mode9 case0 sub5` / UART `%`, because fixed-lifted testing cannot exercise the accelerometer-based translational estimate |
| 20260615-140825 | 20260615-140825-web-visualizer-all-trace-fields | Webログ表示でF413 trace全列を表示対象化 | n/a | yes | extended `tools/logging/visualizer/web_visualizer.py` so Nightfall trace plots include trace context, IMU acceleration, encoder deltas, raw angle/omega fields, raw flags, reserved fields, tune derived fields, and an automatic `Other` group for any future columns not covered by the named groups; verified with `trace_bin_20260615_135328.csv` that every loaded column except the time axes is represented by a Plotly trace, and ran `python3 -m py_compile tools/logging/visualizer/web_visualizer.py` plus `git diff --check`; no live hardware command was run |
| 20260620-150700 | 20260620-150700-f413-path-normal-turn-unify | F413最短/path走行をF405 mini相当ロジックと新旋回制御へ統一 | yes | yes | applied the new smooth omega-profile turn control to the F413 path runner instead of keeping it only in test-specific flows; changed OP mode2..7 case0 subtests to build a normal shortest-path code list and execute it through the same `f413_path_run_session_once()` path as ordinary shortest runs; ported F405 mini-style straight/diagonal speed generation with turn in/out offsets and per-turn velocities from `ShortestRunModeParams_t`, removed F413-specific test velocity caps, and made shortest OP cases 1..9 route through selected mode/case parameters; while HIL testing lifted/connected machine with user permission, found the F413-only post-profile angle-target wait could drive `target_omega` to the 4000dps angle clamp and was not present in F405 `driveSR/driveSL`, so removed that wait and kept turn execution to the timed smooth omega profile followed by the normal out-offset segment; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; flashed dirty `fw_git_sha=769693d` to F413 via ST-LINK SN `003B00273234511537333934`, UART `/dev/cu.usbmodem111202`, ran lifted OP `mode2 case0 sub1` (`codes=203,501`) and dumped `tools/logging/logs/trace_bin_20260620_150602.csv`, confirming `op=(2,0,1)`, 2352 records, target omega profile peak about -528dps with max adjacent step about 6.15dps, no -4000dps clamp plateau, and run end `dist=42mm angle=-0deg`; no fan or NVM-destructive command was run |
| 20260620-221500 | 20260620-221500-f413-search-smooth-motion-port | F413探索走行をF405相当の滑らかな初期前進/区画前進/90度スラロームへ更新 | yes | yes | latest user maze-search log `trace_bin_20260620_214750.csv` showed mode1 case4 high-level exploration was choosing forward actions and updating position context `(0,0)->(0,1)->(0,2)`, but each forward action jumped target velocity from 0 to about 300mm/s and overshot 90mm to about 96-98mm, making wall sampling position unreliable; changed F413 mode1 case1..8 auto-search motion from reset-per-action stop/go primitives to a continuous F405-style search phase: force-open start front wall, run `DIST_FIRST_SEC + DIST_HALF_SEC` entry acceleration, keep cumulative distance, run one-cell forward at the current speed, run right/left turns as `dist_offset_in + smooth omega profile + dist_offset_out`, handle 180deg as half-section decel/rotate/half-section accel, and decelerate with a final half-section stop; preserved current user edits in `params/f413_preorder/params.h`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, and `git diff --check`; flashed F413 via ST-LINK SN `003B00273234511537333934`, UART `/dev/cu.usbmodem111202`, formatted trace with `q`, ran lifted/connected OP mode1 case4 using UART `P/E`, and observed run completion `[SEARCH-RUN] OK case4 actions=3 pos=(0,3,0) save=OK`; attempted `V` full CSV dump but it is a long text dump and overran/garbled the capture, then reset via ST-LINK to stop output; no fan command or protected NVM/identity/calibration write was run, but trace format and maze save areas were intentionally used by the search run |
| 20260620-222700 | 20260620-222700-f413-wall-sensor-calibration-op | F413でF405相当の壁センサoffset/壁制御基準値取得OPを追加 | yes | yes | implemented F405-style wall sensor calibration on F413: NVM `wall_offset_*` remains the LED on/off no-wall offset applied inside `f413_wall_sensor`, NVM `base_l/base_r/base_f` is now loaded into runtime control bases with fallback to `WALL_CTRL_BASE_L/R`, and wall-control error calculation now uses the loaded side control bases instead of compile-time constants; added `f413_wall_sensor_calibrate_offsets_and_save()` and `f413_wall_sensor_calibrate_side_base_and_save()`; mapped OP `mode9 case8` to side wall baseline sampling/save and `mode9 case9` to wall sensor offset sampling/save, matching F405 test-mode usage, while `mode9 case3/6` wall sensor test now prints current offsets and control bases; updated UART help and `docs/ai/HIL_SAFETY.md` because case8/case9 now write sensor calibration NVM and are no longer green diagnostics; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; flashed dirty firmware from `fw_git_sha=3041d55` via ST-LINK SN `003B00273234511537333934`, then ran safe non-motor smoke on UART `/dev/cu.usbmodem111202` with `i,w,v`: IMU WHO_AM_I `0x6B` PASS, wall snapshot PASS with offsets `0/0/0/0` and runtime control base fallback `L=1941 R=1989 F=0`, bounded CSV saved as `tools/logging/logs/stm32_log_20260620_222602.csv`; did not execute OP `mode9 case8` or `case9`, so no sensor calibration NVM write was performed by Codex |
| 20260620-224600 | 20260620-224600-f413-d-tire-param-distance-scale | F413エンコーダ距離換算をF405同様params.hのD_TIRE参照へ修正 | yes | yes | user found changing `params/f413_preorder/params.h` `D_TIRE` from 14mm to 16mm did not affect travel distance; audited F405 `read_encoder()` and F413 `f413_control.c`, confirming F405 computes encoder mm/s from `D_TIRE` in `params.h` while F413 still used the fixed local `F413_CTRL_D_TIRE (13.35f)` for `s_enc_to_mm`; changed `F413_CTRL_D_TIRE` to `D_TIRE`, so current `params/f413_preorder/params.h` `D_TIRE=16.0F` now directly scales `real_distance`, `real_velocity`, and distance control; checked other fixed constants and found `F413_CTRL_CPR_WHEEL=200` is a measured hardware encoder count equivalent to F405's code-local `Cpr_wheel`, while `F413_CTRL_TREAD=33.5` has no F405 `params.h` equivalent and is only used for encoder-derived omega logging, so neither was changed; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; flashed dirty firmware from `fw_git_sha=3041d55` via ST-LINK SN `003B00273234511537333934`, then ran safe non-motor smoke on UART `/dev/cu.usbmodem111202` with `i,w,v`: boot showed `DIRTY=1`, IMU WHO_AM_I `0x6B` PASS, wall snapshot PASS, and bounded CSV saved as `tools/logging/logs/stm32_log_20260620_224536.csv`; no motor/fan/search/shortest/NVM-destructive command was run |
| 20260621-000000 | 20260621-000000-f413-f405-run-feature-port | F413へF405同等の走行補正フラグ/壁切れ補正/前壁入口補正/既知区間加速の入口を追加 | n/a | yes | user requested completing the F413 maze-running migration to match F405 operation semantics; added F413 run feature flags (`wall_control`, `wall_end_correction`, `front_wall_correction`, `angle_accum`, `test_mode`) with mode/case defaults mirroring the actively assigned F405 flags; split F413 operation entry points into F405-like `f413_mode1.c` and `f413_mode2.c`..`f413_mode7.c` wrappers, with shared shortest logic in `f413_mode_shortest.c`; connected shortest/path straight segments to F405-style turn-before wall-end correction including the large-turn/S1/small-turn skip cases and `dist_wall_end` follow distance; added front-wall threshold early-exit/extension behavior for small-turn entry segments; made wall-control gating obey the new runtime flags; added F405-style search forward wall-end detection/follow distance and a `known_straight/acceled` state so F413 mode1 exploration can accelerate through known straight cells and decelerate before unknown/turn sections; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, and `git diff --check`; no live hardware, motor, fan, flash, calibration, maze reset, or trace destructive command was run for this change |
| 20260621-121239 | 20260621-121239-f413-mode-case-local-run-config | F413走行mode/caseごとの参照パラメータ/補正フラグを局所的に読める構成へ整理 | n/a | yes | user clarified that mode-split files are for tuning/debug readability: each mode/case should show which parameters and correction flags are used without chasing deep common helpers; moved F413 search mode1 case configuration into `f413_mode1.c`, including search param index, phase plan, and feature flags; moved mode2..7 shortest/test case tables into each `f413_modeX.c`, including shortest params reference labels, feature flag presets, and case0 sub path-code definitions; removed centralized `f413_run_features_apply_*` mode/case default helpers so callers set features explicitly from the local mode file; changed `main.c` case0 dispatch to delegate directly to `f413_mode2_run_case0_sub()`..`f413_mode7_run_case0_sub()` instead of rebuilding test path codes in `main.c`; kept common execution mechanics in `f413_mode_shortest.c`, `f413_path_run.c`, and `f413_search_step.c`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, and `git diff --check`; no live hardware, motor, fan, flash, calibration, maze reset, or trace destructive command was run |
| 20260621-124536 | 20260621-124536-f413-mode1-case0-search-turn-test | F413 mode1 case0へF405相当の探索ターン単発テストを移植 | n/a | yes | user requested the F405 `mode1 case0` exploration turn one-shot test on F413 for search-turn tuning; added local `mode1 case0 sub` configuration in `f413_mode1.c`: sub0 reserved, sub1 standard `searchRunParams[0]` small R90, sub2 low-speed `searchRunParams[1]` small R90, and sub3 standard 3-section straight; added `f413_search_step_run_case0_test_once()` so these tests reuse the same F413 search motion helpers as normal exploration (`drive_accel_distance`, `run_smooth_turn`, and final stop) while disabling wall control, wall-end correction, and front-wall correction like the F405 test mode; routed OP UI `mode1 case0 sub*` through the new mode1 table instead of the old no-op / generic `TEST_RUN_1` fallback; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, and `git diff --check`; no live hardware, motor, fan, flash, calibration, maze reset, or trace destructive command was run |
| 20260621-134646 | 20260621-134646-f413-turn-tune-host-tool | F413ターン軌道シミュレーション/ログ比較/fitツールを追加 | n/a | yes | added `tools/tuning/turn_tune.py` and `tools/tuning/README.md` for host-side F413 turn tuning; the tool reads current F413 preorder shortest/search params, reproduces the smooth omega-profile turn plan with 1ms integration, replays trace CSV target/real velocity and omega streams into the same local pose frame, compares replayed poses against simulation, exports optional simulated samples, and fits velocity/alpha/in-offset/out-offset/angle to a requested final pose while only printing suggested C assignments; verified `python3 -m py_compile tools/tuning/turn_tune.py`, `simulate --runner shortest --mode 2 --code 501`, `simulate --runner search --search-index 0 --side right`, `replay tools/logging/logs/trace_bin_20260620_210552.csv --runner shortest --mode 2 --code 502 --compare-sim`, and `fit --runner shortest --mode 2 --code 501 --target-x 104 --target-y 96 --target-theta -90`; no live hardware, motor, fan, flash, calibration, maze reset, or NVM/trace destructive command was run |
| 20260621-135656 | 20260621-135656-f413-search-small-turn-sim-tune | F413探索通常小回りターンの初期シミュレーション値を反映 | n/a | yes | tuned `searchRunParams[0]` for the normal F413 search small right turn using the new host simulator with velocity fixed at 300mm/s, in-offset fixed at 10mm for front-wall correction, and target local pose `(x=45mm, y=45mm, theta=-90deg)`; changed `.alpha_turn90` from `8920.0f` to `11282.0f` and `.dist_offset_out` from `14.2f` to `10.5f`, leaving `.velocity_turn90`, `.dist_offset_in`, and `.angle_turn_90` unchanged; simulator result is `x=45.097mm y=45.067mm theta=-90.000deg`, with omega peak about `822.8deg/s` and profile duration about `196.9ms`; verified `python3 tools/tuning/turn_tune.py simulate --runner search --search-index 0 --side right`, `cmake --build --preset Debug-stm32f413`, and `git diff --check`; no live hardware, motor, fan, flash, calibration, maze reset, or NVM/trace destructive command was run |
| 20260621-141742 | 20260621-141742-f413-constant-velocity-turn-control-fix | F413速度指定中に距離外側補正が残ってターン速度が上振れする問題を修正 | n/a | yes | analyzed user-provided `mode1 case0 sub1` logs `trace_bin_20260621_140056.csv` and `trace_bin_20260621_140157.csv`; the smooth-turn core angle tracking was close, but `target_velocity_mm_s` entered the turn around `323..327mm/s` and real velocity averaged about `311mm/s` because `f413_ctrl_set_velocity(300)` was overwritten on the next 1kHz tick by residual distance outer-loop feedback from the preceding distance segment; split F413 translation control semantics so `f413_ctrl_set_velocity_profile()` syncs/enables distance feedback for distance-profile segments while `f413_ctrl_set_velocity()` switches to velocity-only tracking and clears distance feedback, and clamped distance-profile target velocity so accel/constant profiles cannot exceed their configured target speed; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; no live hardware, motor, fan, flash, calibration, maze reset, or NVM/trace destructive command was run, so the next HIL step is to flash and rerun `mode1 case0 sub1` to confirm turn `target_velocity_mm_s` stays at `300mm/s` |
| 20260621-142420 | 20260621-142420-f413-turn-tune-branch-flash | F413ターン調整テスト用ブランチを実機へ書き込み | yes | no | user connected the F413 machine and requested flashing the current test branch; confirmed clean branch `codex/f413-turn-tune-tool` at `7da3aea`, built with `cmake --build --preset Debug-stm32f413`, listed ST-LINK probe SN `003B00273234511537333934`, then flashed `build/Debug/nightfall_stm32f413.elf` via `python3 tools/flashing/flash_stlink --sn 003B00273234511537333934`; CubeProgrammer detected STM32F413/F423 at 3.26V, erased/programmed application sectors 0-5, verified download successfully, and performed software reset; no UART command, motor, fan, run/search/shortest execution, calibration, maze reset, or protected NVM/identity write was run |
| 20260621-143035 | 20260621-143035-f413-search-small-turn-log-retune | F413探索通常小回りターンを速度修正後ログから再補正 | n/a | yes | analyzed new user-provided `mode1 case0 sub1` logs `trace_bin_20260621_142557.csv` and `trace_bin_20260621_142703.csv` after the constant-velocity control fix; turn-core `target_velocity_mm_s` is now exactly `300.0mm/s` min/mean/max in both logs, confirming the speed-command issue is fixed; using a target-angle-based core marker plus in/out distance window, measured turn-window real poses were about `x=48.971 y=45.840 theta=-89.772deg` and `x=47.433 y=45.621 theta=-90.109deg`, so the real path was still about `+3.2mm` right and `+0.7mm` forward on average versus the desired `(45,45,-90)`; applied a first empirical geometry compensation to `searchRunParams[0]`, changing `.alpha_turn90` from `11282.0f` to `11743.0f` and `.dist_offset_out` from `10.5f` to `8.0f`; simulator result for the compensated target is `x=41.980mm y=44.379mm theta=-90.000deg`, intentionally inside the ideal pose to offset the observed real-vs-sim bias; verified `python3 tools/tuning/turn_tune.py simulate --runner search --search-index 0 --side right`, `cmake --build --preset Debug-stm32f413`, and `git diff --check`; no live hardware command was run for the analysis/edit step |
| 20260621-143131 | 20260621-143131-f413-search-small-turn-retune-flash | F413探索小回り再補正ファームを実機へ書き込み | yes | no | after committing the log-based search small-turn retune at `7882ead`, rebuilt clean with `cmake --build --preset Debug-stm32f413` and flashed `build/Debug/nightfall_stm32f413.elf` via ST-LINK SN `003B00273234511537333934` using `python3 tools/flashing/flash_stlink --sn 003B00273234511537333934`; CubeProgrammer detected STM32F413/F423 at 3.25V, erased/programmed application sectors 0-5, verified download successfully, and performed software reset; no UART command, motor, fan, run/search/shortest execution, calibration, maze reset, or protected NVM/identity write was run |
| 20260621-144037 | 20260621-144037-f413-search-small-turn-second-log-retune | F413探索小回りを追加ログ2本から角度込みで再補正 | n/a | yes | analyzed the next two user-provided `mode1 case0 sub1` logs `trace_bin_20260621_143503.csv` and `trace_bin_20260621_143616.csv` from firmware commit `7882ead`; the turn-core `target_velocity_mm_s` stayed exactly `300.0mm/s` min/mean/max in both logs, so the constant velocity fix remains valid; measured turn-window real poses were about `x=45.812 y=44.514 theta=-91.582deg` and `x=44.772 y=45.699 theta=-89.815deg`, averaging `x=45.292 y=45.106 theta=-90.699deg`, which is close in position but about `0.7deg` too deep; compensated the model by fitting toward `x=41.688 y=44.273 theta=-89.301deg`, changing `searchRunParams[0]` from `.alpha_turn90=11743.0f`, `.dist_offset_out=8.0f`, `.angle_turn_90=90.0f` to `.alpha_turn90=12027.0f`, `.dist_offset_out=8.1f`, `.angle_turn_90=89.3f`; simulator result is `x=41.650mm y=44.250mm theta=-89.300deg`, intentionally leaving about `+3.35mm`, `+0.75mm`, and `-0.7deg` empirical real-vs-sim bias to land near `(45,45,-90)`; verified `python3 tools/tuning/turn_tune.py simulate --runner search --search-index 0 --side right`, `cmake --build --preset Debug-stm32f413`, and `git diff --check`; no live hardware command was run for the analysis/edit step |
| 20260621-144129 | 20260621-144129-f413-search-small-turn-second-retune-flash | F413探索小回り角度込み再補正ファームを実機へ書き込み | yes | no | after committing the additional-log search small-turn retune at `3102023`, rebuilt clean with `cmake --build --preset Debug-stm32f413` and flashed `build/Debug/nightfall_stm32f413.elf` via ST-LINK SN `003B00273234511537333934` using `python3 tools/flashing/flash_stlink --sn 003B00273234511537333934`; CubeProgrammer detected STM32F413/F423 at 3.24V, erased/programmed application sectors 0-5, verified download successfully, and performed software reset; no UART command, motor, fan, run/search/shortest execution, calibration, maze reset, or protected NVM/identity write was run |
| 20260621-145245 | 20260621-145245-f413-search-small-turn-forward-trim | F413探索小回りの前方向進みすぎをalpha/outの組で補正 | n/a | yes | analyzed the latest user-provided `mode1 case0 sub1` logs `trace_bin_20260621_144427.csv` and `trace_bin_20260621_144545.csv` from firmware commit `3102023`; the velocity issue remains fixed, and the `55..133mm` real-distance turn-window replay averaged about `x=45.92mm y=44.92mm theta=-89.55deg`, while the user observed the actual machine pose as about `+5mm` too far forward with right distance and angle nearly correct; to trim forward travel without changing the fixed `dist_offset_in=10mm` or the angle, increased `.alpha_turn90` from `12027.0f` to `16589.0f` to shorten the turn body and increased `.dist_offset_out` from `8.1f` to `13.2f` to restore the rightward displacement; kept `.velocity_turn90=300.0f` and `.angle_turn_90=89.3f`; simulator result is `x=41.555mm y=39.290mm theta=-89.300deg`, about `5mm` less forward than the previous compensated target while preserving right displacement within about `0.1mm`; verified `python3 tools/tuning/turn_tune.py simulate --runner search --search-index 0 --side right`, `cmake --build --preset Debug-stm32f413`, and `git diff --check`; no live hardware command was run for the analysis/edit step |
| 20260621-145342 | 20260621-145342-f413-search-small-turn-forward-trim-flash | F413探索小回り前方向トリム版を実機へ書き込み | yes | no | after committing the forward-trim search small-turn retune at `ce6b92f`, rebuilt clean with `cmake --build --preset Debug-stm32f413` and flashed `build/Debug/nightfall_stm32f413.elf` via ST-LINK SN `003B00273234511537333934` using `python3 tools/flashing/flash_stlink --sn 003B00273234511537333934`; CubeProgrammer detected STM32F413/F423 at 3.27V, erased/programmed application sectors 0-5, verified download successfully, and performed software reset; no UART command, motor, fan, run/search/shortest execution, calibration, maze reset, or protected NVM/identity write was run |
| 20260621-151809 | 20260621-151809-f413-turn-contact-analysis | F413探索小回りの左右エンコーダ/IMU比較ツールを追加 | n/a | yes | analyzed the newest `mode1 case0 sub1` logs `trace_bin_20260621_150116.csv` and `trace_bin_20260621_150234.csv` to separate wheel-side tracking from tire/floor contact behavior; `target_omega` peak was `993.78deg/s`, matching the `ce6b92f` forward-trim params (`alpha_turn90=16589`) rather than the previous `846.17deg/s` profile, so the logs are consistent with the current tuning branch despite missing `fw_git_sha` metadata; using the measured tread `34.5mm` and current `D_TIRE=14.5mm`, target-angle windows showed wheel-encoder yaw `-93.43deg` vs IMU yaw `-89.77deg` and wheel-encoder yaw `-92.67deg` vs IMU yaw `-89.28deg`, while IMU yaw stayed within about `0.7deg` and `0.2deg` of target; this suggests the wheel differential rotation is deeper than actual body yaw by roughly `3.4..3.7deg`, pointing more toward tire/floor compliance or slip in yaw than a motor-not-spinning or wheel-command tracking failure; added `turn_tune.py contact` to compare signed left/right encoder deltas against IMU/target yaw over selectable windows, documented it in `tools/tuning/README.md`, and updated `F413_CTRL_TREAD` from `33.5f` to the measured `34.5f` for encoder-derived omega/fallback consistency; verified `python3 -m py_compile tools/tuning/turn_tune.py`, `python3 tools/tuning/turn_tune.py contact tools/logging/logs/trace_bin_20260621_150116.csv --window target-angle --tread 34.5`, `python3 tools/tuning/turn_tune.py contact tools/logging/logs/trace_bin_20260621_150234.csv --window target-angle --tread 34.5`, `python3 tools/tuning/turn_tune.py replay tools/logging/logs/trace_bin_20260621_150116.csv --runner search --search-index 0 --side right --phase omega --compare-sim`, `cmake --build --preset Debug-stm32f413`, and `git diff --check`; no live hardware, flash, UART command, motor, fan, run/search/shortest execution, calibration, maze reset, or protected NVM/identity write was run |
| 20260621-184822 | 20260621-184822-f413-search-turn-position-update | F413 mode1探索で小回り旋回後の内部座標が1区画遅れる問題を修正 | n/a | yes | committed and pushed the user's completed F413 distance/search tuning checkpoint first as `ff3fdd1`; analyzed the latest `mode1 case4` logs `tools/logging/logs/trace_bin_20260621_183250.csv` and `tools/logging/logs/trace_bin_20260621_183529.csv`, decoding `op_sub=(y<<4)|x` and `test_id=0x80|(next_rel<<2)|dir`; both logs showed the planner choosing the expected route toward `(2,3)`, but after the smooth right turn the F413 auto-search loop updated only `mouse.dir` and did not advance `mouse.x/y`, unlike F405 `adachi()` which calls `adv_pos()` after every route action; this explains the user-observed no-wall run stopping one cell past the goal and the `(2,3)` east-wall run reading that wall as a front wall one firmware cell early; changed `f413_search_step_run_config_once()` so successful mode1 auto-search actions always apply `mouse.dir = (mouse.dir + next_rel) & 3` and then advance one cell, leaving UART `N` one-step in-place turn behavior unchanged; verified `cmake --build --preset Debug-stm32f413`, `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, a temporary host C-array maze with `(2,3)` east wall where explore sim ends at `(2,3,E)`, and `git diff --check`; no live hardware, flash, UART command, motor, fan, search/shortest execution, calibration, maze reset, or protected NVM/identity write was run by Codex for this fix |
| 20260627-164009 | 20260627-164009-f413-search-front-wall-entry-toggle | F413 mode1探索小回り前オフセット直進へ前壁補正ON/OFFを接続 | n/a | yes | user clarified that the requested front-wall correction is the F405-style small-turn entry behavior where the `dist_offset_in` straight segment is shortened/extended based on front sensor threshold `val_offset_in`; added `f413_search_step_drive_front_wall_entry_segment()` and routed `f413_search_step_run_smooth_turn()` through it so mode1 search turns now honor `front_wall_correction_enabled`; when disabled, in test mode, or when no front wall is initially detected, the code falls back to the normal fixed `dist_offset_in` segment for clean turn-parameter tuning; when enabled with a front wall, it stops at `params->val_offset_in` or the planned distance and can extend up to `WALL_END_EXTEND_MAX_MM` like the existing path-run front-wall entry correction; added `F413_MODE1_FEATURES_FRONTWALL_OFF_INIT` and `F413_MODE1_FEATURES_WALLEND_FRONTWALL_OFF_INIT` macros so `f413_mode1.c` cases can switch front-wall correction independently from wall-end correction; verified `cmake --build --preset Debug-stm32f413`, `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; no live hardware, flash, UART command, motor, fan, search/shortest execution, calibration, maze reset, or protected NVM/identity write was run |
| 20260627-170256 | 20260627-170256-f413-mode9-case3-wall-sensor-monitor | F413 mode9 case3の壁センサ表示を500ms周期の連続モニタに変更 | n/a | yes | user requested that OP `mode9 case3` continuously acquire and display wall sensor values instead of printing a single snapshot, with a period around 500ms to avoid printf congestion; added `nightfall_run_wall_sensor_monitor_once()` in F413 `main.c`, printing offset/control-base metadata once and then compact raw off/on, delta, detection, saturation, and VBAT values every `NIGHTFALL_F413_WALL_SENSOR_MONITOR_PERIOD_MS=500`; the monitor exits when the PUSH stop switch is pressed; kept `nightfall_run_wall_sensor_test_once()` unchanged for UART `w`, smoke tests, and `mode9 case6`; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, and `git diff --check`; no live hardware, flash, UART command, motor, fan, search/shortest execution, calibration, maze reset, or protected NVM/identity write was run |
| 20260627-190629 | 20260627-190629-f413-f405-turn-control-port | F405相当の角度積算・旋回補正挙動をF413探索/最短へ移植 | n/a | yes | user requested porting all F405-equivalent turn angle command and correction behavior to F413 at once; changed F413 normal control so omega-profile turns keep the angle outer loop active and integrate heading correction into `target_angle`, preserved accumulated angle across straight/turn segments when `angle_accum_mode` is enabled, and cleared explicit angle targets instead of forcing each straight to `target_angle=0`; added F405-style search angle reset streaks after enough straight cells with side walls; changed search and shortest smooth turns to use ideal 90/180/45/135deg command angles in angle-accum mode while leaving test/manual tuning modes on parameter angles; added runtime wall-control gains, side-wall control gating, diagonal wall control, and explicit correction clearing in `f413_wall_runtime`; routed shortest straight/diagonal/turn offset segments through F405-like wall-control gates and removed trace observe side effects on wall-control/wall-end state; verified `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, and `git diff --check`; no live hardware, flash, UART command, motor, fan, search/shortest execution, calibration, maze reset, or protected NVM/identity write was run |
| 20260704-103500 | 20260704-103500-f413-imu-angle-scale-cal | F413 720deg超信地旋回ログからIMU角度スケール補正を追加 | user-run | yes | user executed the new `mode1 case0 sub0` spot-spin test and provided physical observation that the machine over-rotated beyond 720deg by about `asin(4/70)=3.276deg`; analyzed `tools/logging/logs/trace_bin_20260704_102635.csv`, where the firmware ended at `angle=-720.313deg`, `target_angle=-720.000deg`, and `target_omega=0`; computed empirical sensor/control angle scale as `(720+asin(4/70 deg))/720.313 = 1.0041`; added `F413_IMU_GYRO_Z_SCALE=1.0041F` in `params/f413_preorder/params.h` and applied it to both normal F413 control gyro conversion and manual IMU angle diagnostic; verified `cmake --build --preset Debug-stm32f413` and `git diff --check`; Codex did not run live hardware, flash, UART motor/fan/run commands, calibration writes, maze reset, or protected NVM/identity writes |
| 20260704-111500 | 20260704-111500-f413-defer-fram-trace-during-motion | F413走行中trace FRAM書き込みによるTIM5停止を回避 | user-run | yes | user clarified the physical 720deg spin angle measurement uses a right-side reference line and ruler intersection and is not primarily affected by chassis translation; analyzed five user-run spot-spin logs (`trace_bin_20260704_103844.csv`, `104228.csv`, `105124.csv`, `105421.csv`, `105802.csv`) against physical observations from `65sinθ=5` over-rotation through `100sinθ=2` under-rotation; firmware IMU final angle stayed tightly around `720.03..720.47deg` while physical angle spread about `718.85..724.41deg`, invalidating the one-run gyro scale correction; found that F413 trace streaming writes records to FRAM during motion and each FRAM SPI2 access disables TIM5, pausing the 1kHz control/IMU integration while PWM continues; changed `f413_trace_log_auto_step()` to defer NVM/FRAM flushing whenever motor forward/coast/reverse trace flags are active, so samples are buffered in RAM and flushed after motion/stop; increased auto trace RAM buffer from 1536 to 2048 records so the 720deg spot-spin test fits without overflow; reset `F413_IMU_GYRO_Z_SCALE` to `1.0F` while retaining the parameter hook; verified `cmake --build --preset Debug-stm32f413` and `git diff --check`; no live hardware, flash, UART motor/fan/run command, calibration write, maze reset, or protected NVM/identity write was run by Codex |
| 20260704-232513 | 20260704-232513-f413-front-wall-match-continuous-test | F413前壁センサ距離合わせの連続調整モードを追加 | n/a | yes | added `mode1 case0 sub4` for continuous front-wall matching using the same `params/f413_preorder/params.h` targets and gains as search front-wall position alignment (`F_ALIGN_TARGET_FR/FL`, `F_ALIGN_DETECT_THR`, `MATCH_POS_*`); the mode keeps following until stop switch/reset, disables side wall/wall-end/front-wall run corrections via the existing case0 test feature set, and safely commands zero velocity/omega while waiting for recovery if the front wall is lost or either front sensor is far above the target plus a derived margin; updated OP UI text and case0 test dispatch; verified `cmake --build --preset Debug-stm32f413` and `git diff --check`; no live hardware, flash, UART motor/fan/run command, calibration write, maze reset, or protected NVM/identity write was run by Codex |
| 20260705-000015 | 20260705-000015-f413-front-wall-match-filter | F413前壁合わせにセンサ平滑化と保持ヒステリシスを追加 | n/a | yes | user reported that after tuning `mode1 case0 sub4`, the front-wall match settled and then oscillated about 1mm at roughly 0.5s intervals; preserved the user's adjusted F413 front-wall targets in `params/f413_preorder/params.h` (`F_ALIGN_TARGET_FR=1663`, `F_ALIGN_TARGET_FL=2050`) and added `MATCH_POS_SENSOR_LPF_ALPHA=0.04`, `MATCH_POS_HOLD_RELEASE_TOL=180`, and `MATCH_POS_HOLD_RELEASE_TOL_ANGLE=80`; changed F413 front-wall match control to run proportional position/angle commands from a filtered FR/FL value while keeping raw sensor values for wall-lost/too-close safety checks, command zero while inside the tight tolerance during finite search alignment, and in the continuous tuning mode hold zero after stable convergence until the wider release tolerance is exceeded; verified `cmake --build --preset Debug-stm32f413` and `git diff --check`; no live hardware, flash, UART motor/fan/run command, calibration write, maze reset, or protected NVM/identity write was run by Codex |
| 20260719-225627 | 20260719-225627-f413-front-distance-match | F413前壁距離換算を校正し位置合わせへ適用 | 0de2c0c-dirty | yes | generated 13-point FR/FL/front-sum LUTs from regulated-supply fixture data at `2 + 5*n` mm and selected PCHIP; host test confirmed exact knot values, monotonic conversion over every calibrated ADC count, and `FR(1680)=FL(2050)=FSUM(3730)=7.000mm`; changed normal search and mode1 case0 sub4 front matching to use unwarped front-sum distance for translation and FR-FL distance for yaw with a 7.0mm target; built F413 and both F405 targets; flashed the application ELF through ST-LINK SN `003B00273234511537333934` with verify/reset, without protected NVM writes; non-motor UART `:` on `/dev/cu.usbmodem211202` measured `FR=774 -> 21.83mm`, `FL=981 -> 21.84mm`, `FRONT_SUM=21.85mm`, `valid=0x15`, and no extrapolation/saturation; NVM-warped front distances were negative, confirming the control path must remain on unwarped LUT values until warp anchors are recalibrated; no motor, fan, run, search, shortest, mode1 test, calibration write, maze reset, or trace-destructive command was executed; build dirty flag came only from the unrelated untracked `hardware/mini_r2_0/cad/kicad/WeAct-Adapter-5V/` directory |
| 20260719-231916 | 20260719-231916-f413-staged-front-match | F413前壁位置合わせを静定・保持付き状態機械へ刷新 | 2c921b5-dirty | yes | added the pure-C `f413_front_match` controller and connected both exploration front matching and `mode1 case0 sub4`; the controller aligns yaw and translation separately, inserts 80ms yaw and 150ms final zero-command settling intervals, enters zero-command hold, and requires a 1.8mm translation or 1.2mm yaw-distance error to persist for 100ms before reacquiring; a host static-friction/noise simulation reached hold in 766ms at `pos=0.937mm yaw=0.597mm`, issued no command for 1000ms of bounded `+/-1.0mm` noise, rejected a one-sample disturbance, and reacquired a persistent disturbance; `cmake --build --preset Debug-stm32f413`, `cmake --build --preset Debug-stm32f405`, `tools/solver_host/run_solver_host.sh --explore-sim --max-steps 512`, and `git diff --check` passed; flashed the application ELF through ST-LINK SN `003B00273234511537333934` with verify/reset; non-motor UART `i,:` on `/dev/cu.usbmodem211202` passed IMU WHO_AM_I and measured `FR=22.17mm FL=21.54mm FRONT_SUM=21.84mm valid=0x15` without extrapolation/saturation; firmware is ready for the user-run `mode1 case0 sub4` motion test, but Codex did not run motors, fan, search, shortest, calibration writes, maze reset, or trace-destructive commands; build dirty flag came only from the unrelated untracked WeAct Adapter directory |
