# board/mini_r1_0

このディレクトリは `mini_r1_0` 機体のボード依存情報を管理します。

## 役割

- ピン割り当て（論理名と物理ピンの対応）
- 搭載デバイス有無（センサ、モータドライバ、FRAM など）
- ボード初期化の差分（将来的に `platform/` から切り出し）
- 機体能力フラグ（将来的に機体識別ブロックと整合）

## 現在の状態

現時点では `platform/stm32f405/` から未切り出しです。
Phase 1〜5 で段階的に移行します。
